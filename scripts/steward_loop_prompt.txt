This legacy prompt entry point is retired and intentionally inert.

Use scripts/steward_control.py session for credential-confined cooperative
sessions, or project for an explicitly leased source-first projection
generation. The legacy begin/heartbeat/finish path is compatibility-only and
must use stdin rather than argv for any held credential. Scheduling remains
external to the controller.
