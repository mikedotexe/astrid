[Service]
# Loaded after the appliance profile. The immutable supervisor installs this
# root-owned authority file only after its source, key, A/B, and rollback
# bootstrap checks succeed. A mutable home file must never grant this bit.
EnvironmentFile=/etc/astrid/edge-self-change-authority.env
