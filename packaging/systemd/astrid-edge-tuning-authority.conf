[Service]
# Loaded after the appliance profile so rollout authority is an explicit,
# reversible operator choice rather than an accidental profile side effect.
EnvironmentFile=%h/.config/astrid/edge-tuning-authority.env
