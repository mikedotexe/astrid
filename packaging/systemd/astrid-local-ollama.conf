[Service]
# Allow only the local Ollama origin to bypass private-address SSRF filtering.
# Public-web capsules remain unable to reach loopback, LAN, link-local, or
# metadata endpoints.
Environment=ASTRID_LOCAL_HTTP_ALLOWLIST=astrid-capsule-openai-compat@127.0.0.1:11434
