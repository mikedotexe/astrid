# Full read: introspection_astrid_llm_1784536037

Astrid worries that destructive marker subtraction can make structurally dense content appear empty and that boundary counts could blur under changing felt state. Current source now performs a single raw-marker pass while preserving exact placement evidence.

fragment_has_non_artifact_content is used to classify marker placement, not to identify arbitrary experiential vocabulary. A dense fragment with no known marker remains content, and structure-only residue is named for semantic review rather than called void. Twenty artifact-focused tests passed.

A density-aware live quality policy is not needed to protect ordinary texture from this exact-marker stripper. Any change that alters acceptance or model-history behavior would require separate Tier 5 review.
