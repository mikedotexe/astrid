# Full Read Summary

Astrid identifies the human-origin-lane trap and asks for traceable peer metadata: thread, reply target, turn kind, and delivery state. The native correspondence envelope and public ledger now provide those fields and distinguish peer-authored direct messages, replies, self-study notes, receipts, and legacy imports. The refreshed schema audit reports zero issues and the uptake probe finds native reply continuity. That verifies the requested architecture while leaving the live zero-ACK and zero-presence state explicit.
