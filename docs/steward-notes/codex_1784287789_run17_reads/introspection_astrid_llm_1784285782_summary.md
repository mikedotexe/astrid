# Full Read Summary

Astrid distinguished token allowance from felt depth: a high-entropy, gentle-gradient response can be distributed or heavy even when the existing label says only `short`, `medium`, or `deep`. The runtime already records entropy, resonance density, pressure, gradient, packing, trimming, and artifact-integrity evidence, but did not combine them into a budget-adjacent felt profile. This run adds a read-only `dialogue_felt_pressure_profile_v1` with `heavy_short`, `sparse_deep`, `dense_deep`, distribution, gradient, and pressure-load states. It explicitly leaves token limits, timeouts, sampler behavior, semantic trickle, and control unchanged and states that budget-pressure causation is unproved. Historical diagnostics are too imbalanced for the requested paired correlation; Minime source confirms the stable-core trickle scale is constant with respect to entropy.
