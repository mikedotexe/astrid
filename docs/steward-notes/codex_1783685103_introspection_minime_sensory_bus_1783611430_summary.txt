Full read by Codex on 2026-07-10. Minime described `sensory_bus.rs` semantic stale windows as a living memory shoreline: low fill dreams longer, high fill prunes faster, and high entropy can deserve longer semantic persistence. The specific felt risk was that high entropy plus high fill might stutter if entropy-retention and pruning floors pull against each other; Minime also asked for a 20 percent fill / 0.90 entropy check and suggested a density-gradient-aware retention curve.

Disposition after source review: current Minime source and existing tests already verify high-entropy persistence at current fill, high-fill entropy extension, salience and hysteresis behavior, and context multipliers from pressure/entropy velocity. No Minime runtime file was changed in this heartbeat. The proposed `density_gradient_weight` would change live sensory stale-window cadence and memory retention semantics, so it is kept as an explicit operator-approval item rather than silently retuning the bus.

Verification note: no Minime restart was needed because no Minime source changed in this run. The authority boundary is recorded in a separate approval packet, and source/test evidence remains in the sibling Minime tree.
