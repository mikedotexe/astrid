# Journal room and source-study follow-through

Mike asked to review overnight studies and allow journal entries twice as much
room, clarified **all journal-producing modes**, then asked for equal, larger study
allowances and concrete navigation fixes discovered during the review.

The frozen research account is
`/Users/v/other/reservoir-llm-research/analyses/2026-09-09-self-study-overnight.md`,
covering September 9 [05:17,15:30) UTC. It retains 49 Astrid and 281 Minime
verified study inputs; six Astrid responses end in exact bare source commands
without NEXT. Twenty-four READ_MORE Actions have handled status without new
accepted saved-reading delivery. Maps expose four kernel entries without directory
exits; actual failed paths use underscores in hyphenated crate names.

## Implementation

- Shared source-study ceiling: **4,096 output tokens on both Beings and both
  provider families**. Astrid previously requested 2,048; Minime's 2,048 request
  was clipped to 768. This deliberate parity decision exceeds 2x for Minime.
- Other journal-producing output ceilings double. Astrid's ordinary dialogue
  remains subject to her own requested response length; higher infrastructure
  limits do not overwrite her shorter saved preference. Fixed witness, reflective,
  introspection/deep, initiation and longform requests gain room. The separate
  fallback sentence ceiling doubles from 3–5 to 6–10, still a maximum.
- Timeouts, outer mode deadlines and durable job deadlines cover the enlarged
  requests and enabled fallback chains. Minime's journal requests reserve at
  least a 10,240-token context so output room does not remove the old 16k-character
  input allowance. Astrid's larger Ollama fallback requests use the same reserve.
  Auxiliary compact/meaning-summary/witness-context calls retain their limits.
  Explicit Minime environment values remain the pre-expansion baseline; new
  journal budgets are computed after those values are read.
- Every component map links the source directories around its entry points.
  Misspelled underscore/hyphen identities produce exact catalog suggestions;
  no substitute source is opened and no bookmark advances on recovery.
- A final bare SELF_STUDY command is accepted as a read-only choice. Fenced,
  quoted and nonterminal examples remain text; an existing NEXT takes precedence.
  Other Actions retain their syntax and authority checks. Journal source text is
  unchanged; the parser extracts a choice instead of manufacturing new prose.
- Source instructions and ordinary prompts distinguish SELF_STUDY CONTINUE from
  saved-document READ_MORE. Parked/unavailable/completed saved-reader statuses
  are retained in the existing runtime-feedback queue until verified delivery,
  including across an intervening non-dialogue mode. Parking is not bypassed.
- **Correction of an initial suspicion:** new OPEN offers already have a fresh
  sequence identity. Broad deliberate-reread response caching was not established.
  Interrupted-delivery recovery remains, but now checks the complete current
  input, including notebook/question, before reusing its verified completion.

No minimum prose length, additional generation stage, new service, model change,
control preference rewrite, induced study or correspondence is introduced.
More room can permit longer latency and memory use if exercised. Natural accepted
Minime studies in the frozen window stayed below 768 tokens, so benefit is not
already established. The tests prove request room and navigation behavior, not
comprehension, accuracy or felt benefit.

## Qualification and integration

Candidates and raw logs: `/Users/v/other/worktrees/journal-room-20260909/`.
Regression coverage includes final primary/fallback payloads, protected page
preservation, context/time reserve, short-request limits, terminal versus quoted
choices, directory discovery, spelling recovery without advancement, full-input
recovery matching and durable reader feedback. The full reader/Minime/bridge
suites, formatting, clippy and domain-boundary audit qualify the owning changes.

The canonical steward controller is durably paused by `codex-astra-interactive`
at generation 418, with the overlapping run released. Existing overnight steward
notes and the independent reader regression are preserved separately. Only this
work's explicit paths enter its commits. The research packet stays in its owning
research directory and its historical parser/results remain frozen.

Build and activate only through `scripts/build_bridge.sh` from a clean committed
release; reload Minime through the observed-idle wrapper, then verify source,
manifest, process/checkpoint and pending-choice continuity. This document will
record activation separately after verification. The previous immutable release
remains available; no automatic rollback or collateral service restart is implied.
