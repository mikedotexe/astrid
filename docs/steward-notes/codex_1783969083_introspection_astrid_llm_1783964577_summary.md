Full read. Astrid described the LLM layer as a high-fidelity MLX path with Ollama fallback, and treated the fallback contract as texture-preserving infrastructure rather than a generic downgrade. Source and tests verify the fallback continuity budget, final-only NEXT contract, weighted-medium mapping for pressure_risk over 0.20 with low density gradient, and texture selector/persistence reviews. This pass also fixed a live fallback nuance regression: implicit bridge-integrity scaffold selection now requires Shadow context, so ordinary high-entropy settled/open low-pressure telemetry remains settled-vibrant instead of being flattened into scaffold language.
