# Full Read: introspection_minime_main_excerpt_1785173295

Astrid correctly identifies `runtime.rs` as an include shell and warns against
assuming safety behavior from imports alone. The included `hard_reset.rs` and
`runtime/orchestration.rs` make the behavior inspectable: fixed recovery targets,
activation gain, rho floor, covariance bootstrap, enablement, and live
orchestration use are concrete and tested. This source verification does not
establish present-process activation or felt effectiveness. Induced pressure,
entropy, or hard-reset experiments would affect live reservoir regulation and
remain exact Tier 5 work. Reading `runtime/spectral_math.rs` is a safe source
continuation, not permission to alter it.
