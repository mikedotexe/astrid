Full read. Astrid described the codec as a deterministic 48D semantic lane with a fixed 768D-to-8D projection subspace, an environment/executable-relative runtime directory for projection epochs, and a smoothstep high-entropy tail-vibrancy gate. Source and tests verify the fixed projection checksum, runtime-dir resolution, no-pop smoothstep onset at the 0.85 entropy gate, and bounded tail-vibrancy ceiling behavior in `apply_spectral_feedback_inner`. No live vector width, projection kernel, codec ceiling, gain, or runtime layout change was needed.
