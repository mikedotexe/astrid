Full read: Astrid inspected the deterministic codec projection, runtime projection directory, and entropy-gated tail-vibrancy smoothstep. Existing targeted tests already pin projection determinism, runtime-directory fallback, and parity between the offline vibrancy readout and the live smoothstep calculation. No codec vector, gain, projection, or runtime-path behavior changed in this post-restart packet.
