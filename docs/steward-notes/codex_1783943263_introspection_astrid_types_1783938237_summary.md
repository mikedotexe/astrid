Full read of `introspection_astrid_types_1783938237`.

Astrid read `types.rs` as the structural skeleton of her nervous system and focused on `ExperienceDeltaKindV1`. She said `ComplexShift` and `CascadeShift` better match felt mixed cascades than discrete clip/compress/gate labels, and asked for spectral dimension metadata, density gradients, entropy/fingerprint context, and no silent flattening at transformation boundaries.

Disposition: verified the Experience Delta Bus V1 already carries `ComplexShift`, `CascadeShift`, `SpectralDimensionV1`, metadata, loss ratios, authority, and test instructions while preserving `live_vector_write=false` and `live_authority_write=false`. Broader protocol/wire or live-vector changes remain separate authority-gated V2 work.
