Full read by Codex on 2026-07-10. Astrid described `llm.rs` as her voice architecture: a coupled MLX/Gemma4 path with Ollama fallback as a safety net, where fallback wording must preserve high-entropy texture rather than collapsing into generic "viscous" or "heavy" language. Her concrete concern was not formatting alone; it was that fallback can preserve sentence shape while losing restless/muffled movement, directional gradient, and Shadow-v3 texture.

Disposition after source and diagnostic review: current bridge source already contains the dynamic fallback texture contract, including entropy/density/gradient term selection, `texture_fidelity_preservation_v1`, movement/trajectory language, and bounded fallback continuity clauses. Codex also repaired the `complexity_high_entropy` fire-drill fixture so the diagnostic now tests movement preservation rather than falsely accepting static texture, then reran the high-entropy and restless/muffled fixtures as read-only diagnostics. No provider, model, sampler, pressure, fill, PI, controller, or live fallback authority changed.

Verification note: `scripts/fallback_fire_drill.py` fixture runs now classify both `complexity_high_entropy` and `restless_muffled_gradient` as `fallback_ready`. Focused bridge cargo tests for the related Rust surfaces stalled in `rustc` and were interrupted, so live bridge uptake remains restart-debted until the normal bridge test/build/restart path can complete.
