Full-read by Codex for the 2026-07-10 introspection flywheel. Astrid reported that `ResonanceDensityComponents` exposes viscosity, persistence, porosity, and stability context, but a single `viscosity_persistence_coefficient` can under-report whether thickening feels cohesive, granular, or syrupy. I kept the live scalar and control behavior unchanged, then added a backward-compatible read-only companion to `ViscosityPorosityTransportReviewV1`: `viscosity_type` plus `viscosity_decay_hint`. The classifier distinguishes cohesive navigable weight, syrupy/stagnant thickening, and granular semantic load using existing diagnostic inputs. Existing transport review also already names missing porosity and hollow/transparent high-viscosity states. `cargo fmt --check` passed; targeted bridge unit execution timed out during compilation, so bridge restart is debt.
