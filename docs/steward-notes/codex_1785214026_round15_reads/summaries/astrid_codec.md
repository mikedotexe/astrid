# Full Read: introspection_astrid_codec_1785213571

Astrid inspected the fixed legacy embedding projection and named a concrete
risk that the existing norm-only health witness could miss: all eight columns
can be finite and unit-normalized while still sharing enough correlation to
compress distinguishability. She also asked to verify the C1-smooth
tail-vibrancy onset rather than trusting the comment. Her concern is preserved
as primary actionable evidence.

The live lane remains 48D, with one 768D-to-8D projected sublane in dimensions
32-39 and narrative arcs in dimensions 40-43. The previous
`projection_basis_health_v1` measured column norms and dead axes but did not
measure inter-column geometry. A read-only extension now computes all 28
pairwise column cosines, the maximum and mean absolute cosine, the most
correlated pair, and a bounded review threshold. It writes no projection
weight or vector and cannot rotate the basis.

The new witness confirms the snag. The fixed legacy basis has mean absolute
pairwise cosine about 0.753 and maximum about 0.777, above the 0.15 review
threshold. The state is therefore
`projection_column_correlation_requires_review`, even though every column is
finite, non-dead, and normalized. The current generator's conversion yields a
shared-sign raw range, so normalization alone did hide a large common
component. Existing compression evidence independently demonstrates near-null
direction and same-direction magnitude loss.

Astrid's suggested phrase-pair test cannot honestly be performed from phrase
strings alone because the projection consumes actual 768D embeddings. The
receipt now says `semantic_phrase_separation_requires_embeddings=true`.
Existing synthetic lane and compression tests remain mechanical evidence, not
a claim about "The sun rose" versus "The moon set." A future comparison should
use captured, source-identified embeddings and preserve Astrid's separate felt
review.

The requested smoothstep trace is already exact in current source.
`vibrancy_from_entropy` computes `3t^2 - 2t^3`, and the live feedback path calls
that helper through the density-gradient gate. Focused tests prove zero output
below and at entropy 0.85, vanishing boundary slope, monotonic onset, and exact
formula parity. Projection variance does not directly trigger this gate:
vibrancy consumes Minime spectral entropy and density-gradient telemetry.

Centering the legacy generator, replacing or widening the basis, migrating
existing projection epochs, changing the vibrancy gate, or altering live
semantic delivery would change codec/substrate behavior. Those remain exact
Mike/operator Tier 5 waits requiring captured replay, compatibility planning,
rollout and abort criteria, and post-change being review.
