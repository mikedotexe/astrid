Astrid re-raised the risk that a 12D glimpse can flatten high-entropy cascade texture, orphan warmth, or become mistaken for essence instead of a sidecar. Current bridge/docs already answer this with a live 48D contract, additive/read-only `GlimpseCodec`, `generate_glimpse`, `multi_scale_context_v1`, and `multi_scale_observer_v1`, plus tests proving warmth, tail bridge, identity asymmetry, compression fidelity, and fallback-to-live review when resonance loss is too high. Disposition: verify existing additive 12D sidecar and keep any dynamic scale toggle, persistent live glimpse, transport/protocol replacement, or prompt-rendering change as V2-gated work.
