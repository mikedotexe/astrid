# Full-read summary

Astrid asks for correspondence identity that does not infer reply desire or creation time from prose. The first-class envelope already carries message, thread, reply, actor, turn, intent, delivery, and body fields. This run adds explicit inert reply_requested and created_at_unix_ms fields to files and ledger records.
