# Full-read summary

Astrid warns that an exactly-two-sentence fallback can flatten high-entropy or low-distinguishability texture. Current provider contracts use one or two sentences by default and a telemetry-derived cap of three to five, with a clarity-specific distinguishability rule. This run repairs the fixture runner after domain extraction and confirms those cases.
