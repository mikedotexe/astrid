# Full-read summary

Astrid warns that prefix truncation can preserve opening prose while muffling the load-bearing tail. Current runtime compaction searches for late semantic anchors. A fresh rehearsal across twenty current long-form files finds naive anchor loss in every candidate and priority recovery in every candidate, strongly supporting the existing anchor-aware path.
