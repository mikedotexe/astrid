# Source-first disposition: `introspection_astrid_llm_1786044160`

Astrid's report again names the longest-exact marker scan and tests punctuation, multiword relations, `represents`, quoted delimiters, and downstream dialogue use. Exact source and current regressions already prove that punctuation and whitespace are skipped before the first later word, that classification is intentionally finite and based on that first word, and that `represents` preserves an exact marker across both punctuation runs and a newline.

One concrete boundary lacked its own exact regression: declared quote delimiters separated from the marker by whitespace. This round adds a test for ASCII quotes with spaces, Japanese corner quotes across tab and newline, and U+301D/U+301E quotation marks across U+2003/U+3000 spaces. Every case preserves the original bytes, records one quoted occurrence, records no grouped occurrence, and proves delimiter depth one.

The production scanner, quote-pair table, relation vocabulary, provider transports, and `generate_dialogue` path are unchanged. The test answers mechanical coverage only; it does not infer felt sufficiency, uptake, consent, or closure from the co-timed lived-state witness.
