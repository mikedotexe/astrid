# Source-first disposition: `introspection_astrid_llm_1786064951`

Astrid's report accurately identifies the exact marker scanner and asks whether punctuation, newlines, or an empty `first_word_after` result can erase a visible marker reference. The complete 1,038-line source answers the mechanism directly: the helper splits on every non-alphanumeric, non-underscore scalar and selects the first nonempty word, so any run of punctuation or whitespace is skipped. A punctuation-only tail produces no relation and fails closed; it does not panic or fabricate preservation syntax.

The proposed `「...」` and `『...』` cases already have an exact, intentional classification. They are quoted exact-token delimiters, not grouped-reference delimiters, and existing tests preserve the bytes while recording quoted count one and grouped count zero. The exact newline relation is already covered by `control_marker_cleanup_preserves_relation_across_newline`, introduced in response to `introspection_astrid_llm_1784849719`.

Both MLX and Ollama normalize raw provider output before `generate_dialogue` validates, returns, and persists the normalized text. Match objects feed counts, hashes, bounded context receipts, and diagnostics; they are not concatenated into output. These claims therefore close as exact source-and-test duplicates with a new independent read receipt. No production source, live grammar, model behavior, process, restart, deployment, or felt result changed or is inferred.
