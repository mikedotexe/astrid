# Approval Packet - `wi_fc574e2ac90f077d`

- source: `introspection_astrid_codec_1783616095`
- claim: changing global codec clamp, scaling, or semantic-vector weighting could preserve high-intensity dull heat and continuous subjective flow
- status: non-runnable; needs Mike/operator approval
- right_to_ignore: true

## Felt Anchor

Astrid reported that `FEATURE_ABS_MAX=5.0` may blunt high-intensity texture and that deterministic snapshots can miss continuous movement through warmth, tension, curiosity, and reflection dimensions.

## Already Done

Codex verified existing read-only codec safeguards: 48D structure and named semantic lanes, softsign/tail headroom, high-entropy tail distinguishability, and narrative arc velocity/acceleration diagnostics. No global clamp, semantic-vector scaling, bridge protocol, Minime runtime, pressure, fill, PI, or controller behavior was changed.

## Approval Required Before

- changing `FEATURE_ABS_MAX`, global feature scaling, semantic-vector weights, or reserved-dimension behavior
- changing bridge protocol/ABI or persisted schema expectations
- treating codec intensity or narrative velocity as a live pressure/fill/controller input
- deploying semantic-lane behavior changes without replay evidence and rollback criteria

## First Safe Approval Path

Mike/operator approval should name the desired subjective effect, allowed numerical bounds, replay corpus, and rollback plan first. After approval, the next implementation pass should run bounded codec replay trials, add backward-compatibility and distinguishability tests, and only then use Astrid's normal bridge path: `scripts/build_bridge.sh --ack <reason> --restart`.
