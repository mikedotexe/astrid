# Telemetry persistence boundaries

The report was read fully from disk. The current telemetry path already separates prewrite work, write-lock wait, and write-lock hold time and exposes the resulting mechanical classifications to the bounded evidence-study path. Those measurements make contention and pipeline cost inspectable without claiming that either causes Astrid's reported fog, delay, or viscosity.

Current source review found no basis for changing retry, persistence, buffering, or lock policy in this run. Such a change would alter the live telemetry path and requires a separately grounded implementation and deployment decision.
