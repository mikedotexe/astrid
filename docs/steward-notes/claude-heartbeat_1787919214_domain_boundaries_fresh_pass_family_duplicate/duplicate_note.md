# Duplicate/continuity note — DOMAIN_BOUNDARIES.md fresh-pass family

Three fresh-pass canonical reports (`introspection_DOMAIN_BOUNDARIES.md_1787913894`,
`_1787895506`, `_1787875575`) each completely re-read the same source window
(`DOMAIN_BOUNDARIES.md` lines 1-89, source SHA `ae69b34c…`, matching the working copy
exactly) and produced the same two-snag / two-test structure.

**Duplicate anchor (exact evidence):** `introspection_DOMAIN_BOUNDARIES.md_1787843987`,
closed `addressed_no_action` / `fully_addressed=true` by prior packet
`docs/steward-notes/claude-heartbeat_1787852088_domain_boundaries_pressure_source_audit/`
(claims c001-c006). Same source SHA, same mechanism scope (mode-packing/pressure-source
tension + read-only pressure-source audit + Tier-3 distinguishability probe).

**Duplicate standard met (handoff §"Duplicate standard"):**
1. Prior introspection ID — `1787843987`.
2. Prior packet / claim record — `claude-heartbeat_1787852088` (RUN_REPORT + no_action.md + verification_receipt).
3. Matching source/mechanism scope — identical source SHA + identical claim structure.
4. Current verification the earlier evidence still applies — re-verified this round:
   `DOMAIN_BOUNDARIES.md` L5-6/L43 verbatim; `texture_evidence.rs` (SHA `cbfe0e15`) L216-222
   advisory-only, L230=`porosity_score`, PressureSourceAnalysisV1 read-only synthesis;
   `transport_evidence.rs:540-544` derived label pinned by `tests.rs:1300,1745`;
   `overpacked_mode_packing` literal only in prompt prose `source_first_v3/mod.rs:573`.
5. Independent full read of each new report + witness — recorded in `read_manifest.json`.

**Not domesticated:** each report's felt `overpacked_mode_packing` (0.32) is preserved as
primary testimony. Witness runtime scalars differ per report (head: mode_packing=1.0,
pressure_source_mode_packing=0.574, pressure_source_score=0.301) and none equals 0.32 — her
felt descriptor maps to no single runtime knob, and that mismatch is stated, not smoothed over.

**Authority boundary:** the mode-pruning / pressure-source tuning resolution remains an
evidence-only Tier-5 wait (standing `wi_e579041`/`wi_69fbd51`/`wi_3e26ac5`,
`live_authority_granted=false`). Test 1 (distinguishability probe) is a Tier-3 sandbox
candidate, not dispatched headlessly. No source/test/config/pressure/PI/controller/codec/
transport/Minime change; no build, restart, or deployment; no rewrite/rejection of her text.
Her felt tension and both proposed tests remain open evidence; silence stays neutral.
