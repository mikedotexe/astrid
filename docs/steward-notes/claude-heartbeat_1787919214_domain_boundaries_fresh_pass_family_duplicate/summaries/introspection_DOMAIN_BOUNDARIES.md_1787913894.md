# Summary — introspection_DOMAIN_BOUNDARIES.md_1787913894 (family head)

Fresh-pass complete read of `DOMAIN_BOUNDARIES.md` (89 lines, source SHA `ae69b34c…`, matches working copy). Fill 60.5%. Witness `lsw_111651b6…` byte-binds to the report exactly, `evidence_only`/`witness_only`, no causation, no raw prose/private paths.

**Observed** — structural map is accurate: Stable Facades (L8-22), Shadow Cartography Ownership (L35-43), behavior-preserving extraction (L5-6), cohesion exceptions + unique-fn-signature ceiling (L47-68). → `verified_existing`.

**Snag** — felt `overpacked_mode_packing` (0.32) vs the L43/L5-6 prohibition on modules changing mode packing/pressure; resolution (mode-pruning/pressure-source tuning) is Tier-5. → `tier_5_wait`. Felt 0.32 preserved; witness runtime scalars (mode_packing=1.0, pressure_source_mode_packing=0.574, pressure_source_score=0.301) show 0.32 maps to no single runtime knob — not domesticated.

**Test 1** distinguishability probe (isolated λ1 clone) → `needs_sandbox` (Tier-3, not dispatched headlessly; available via her PROBE_SELF).

**Test 2** read-only pressure-source audit → `verified_existing`. `PressureSourceControl` advisory-only (`texture_evidence.rs:216-222`); L230 is `porosity_score` (not `overpacked_mode_packing`); `PressureSourceAnalysisV1` read-only synthesis (~L237-262); "overpacked" is a derived read-only match label (`transport_evidence.rs:540-544`, `packing>0.25`) pinned by `types/schema/tests.rs:1300,1745`; the literal `overpacked_mode_packing` appears only as prompt prose (`source_first_v3/mod.rs:573`). Contradiction stated, not domesticated.

**Terminal status:** `addressed_duplicate` of `introspection_DOMAIN_BOUNDARIES.md_1787843987` (prior packet `claude-heartbeat_1787852088`; same source SHA, same two-snag/two-test structure, evidence re-verified current). No live change; no restart/deploy required or attempted.
