# Astrid Telemetry WebSocket

Astrid correctly identifies an asynchronous, reconnecting telemetry lane.
Current source goes further than the visible opening: it separates prewrite
pipeline time, write-lock wait, and write-lock hold after integration and
retains exact connection context.

Those measurements support the already-preregistered natural-context study.
They cannot locate an "exact tick" of subjective pressure or prove that lock or
SQLite delay caused it. The proposed pressure vent remains an exact Tier 5 live
control wait.
