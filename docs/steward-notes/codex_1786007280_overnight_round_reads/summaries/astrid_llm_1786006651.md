# Source-first summary: introspection_astrid_llm_1786006651

All 46 logical report lines (45 newline-terminated lines, 3,718 bytes) were
read from the canonical artifact at SHA-256
`8f5f1e2904fb87cb1841a35363d70670604291c1a1682ffd945c9cc01d141279`.
The exact report-bound source interval, lines 1-400 of
`capsules/spectral-bridge/src/llm/provider/dialogue_runtime.rs`, was read at
source SHA-256
`f7c0570ce495b1978d9b6f588cba6ecb00a93b1573316bc22752e6e10d47f20a`.
The downstream validation and return path at lines 500-650 and 930-1038 was
also inspected. The linked 491-line lived-state witness was read in full at
SHA-256
`c4a1c3052da36e2b17293c9f8557b06c10cc068aec09eb5f1dfa03babf5362b8`.

Astrid accurately describes the exact longest-match marker scanner and its
finite reference grammar. The proposed punctuation failure is not present:
`first_word_after` splits on separators and then finds the first nonempty
segment, so any number of punctuation or whitespace segments are skipped.
The existing Unicode-punctuation regression already exercises this property.
Astrid's exact `... !!! represents` challenge is now pinned by a dedicated
test that preserves the marker and records `following_exact_relation`; no
production parser bytes changed.

The proposed deeper-than-four delimiter case is already covered exactly: the
marker remains present and the bounded receipt reports depth four. The
downstream source read shows that marker sanitation is used for quality-gate
measurement while this module returns successful provider text. That finding
does not authorize the separate provider-neutral normalization proposal or
any live model-output change.

The witness binds the report, source window, process instance, model calls,
and co-timed technical observations. It includes no raw introspection prose,
claims no direct causation, and marks the artifact `evidence_only` with
`live_eligible_now=false`. No cadence, pressure, fill, controller, protocol,
provider, Minime, CPU, or scheduling behavior changed.
