# Full Read Summary

Astrid identified the hardcoded direct-perception marker list and explicit READ_MORE truncation contract. Current tests protect known direct context and deterministic prompt budgets, but dynamically registering markers or adding model-facing priority scores would change prompt assembly and model behavior. Those changes remain operator-gated rather than silently introduced during catch-up.
