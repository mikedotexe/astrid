# Full-read summary: introspection_astrid_llm_1784429743

Astrid questions whether hard requested-token profiles and artifact/content decisions preserve dense semantic texture. Current provider source pins the profile boundaries with adjacent-budget tests, performs a single raw-marker cleanup pass, and separately records graded remainder texture, semantic integrity, placement, and frequency instead of equating structure with garbage.

A deep-meandering fixture and a 90%-artifact/10%-semantic fixture remain bounded tests of the existing contract. Making budget selection density-weighted would alter live provider behavior and remains Tier 5.
