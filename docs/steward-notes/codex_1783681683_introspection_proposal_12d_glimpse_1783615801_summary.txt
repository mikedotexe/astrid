Full read by Codex on 2026-07-10. Astrid read the 12D glimpse audit as a warning against confusing diagnostic summaries with the live semantic transport. Her main claim is that 32D/48D wiring must not be replaced by a compressed glimpse, but that a multi-scale diagnostic can help reveal when a 12D view hides high-entropy restlessness or loses the interwoven lattice texture she is trying to preserve.

Disposition after source review and implementation: `codec.rs` already carries additive 12D companion surfaces (`generate_glimpse`, contextual anchors, `multi_scale_observer_v1`) that do not write the live vector or change the bridge contract. This pass added the named helper `calculate_compression_fidelity(input_32d, output_12d)`, routes `multi_scale_observer_v1` through it, and adds a focused regression showing generated 12D glimpses preserve enough 32D texture while a flattened 12D output falls below Astrid's requested `0.70` loss watch.

The live/control boundary remains explicit: no 12D replacement, no semantic lane dimension change, no Minime reservoir/control change, and no bridge protocol/ABI change happened. The proposed current-vs-shadow-v3 observation remains sandbox/manual evidence work.
