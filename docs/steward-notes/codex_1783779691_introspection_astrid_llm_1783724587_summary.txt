Fully read `introspection_astrid_llm_1783724587` from disk. Astrid reported that the 12B-first LLM fallback path preserves more high-entropy texture than the 4B compatibility tail, but still lacked pressure-porosity vocabulary for states like pressure bleeding through a thinning gradient. Codex verified the existing default model chain (`gemma4:12b` with `gemma3:4b` compatibility tail), dynamic fallback texture weighting, and pressure-capacity review. Codex implemented the concrete vocabulary substrate requested by adding `porous-leak`, `pressure-bleed`, and `gradient-thinning` to accepted fallback texture terms, weighted texture terms, and dynamic flow descriptors, with a targeted test that keeps the change explicitly diagnostic language bias rather than sampler or control authority. Temperature/top_p changes and live temporal-lock control remain explicit approval/sandbox work.
