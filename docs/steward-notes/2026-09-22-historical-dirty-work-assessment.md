# Historical Dirty-Work Assessment

Date: 2026-09-22. Coordinator: Codex, interactive, at Mike's request.

## Scope And Ownership

This is Git stabilization, not a resumed introspection round or a deployment.
The starting Astrid main was `8a0a5ae7b1ae876a29fa446c5a5c64a717758cda`,
with seven modified and 190 untracked paths. Minime main was
`2dbdf66924fc8d04bc3e6edb983b9e7116bf77a1`, with a clean index and worktree.
Both were eleven commits ahead of origin. Remote main tips were checked:
Astrid `c4f85e95e41703daa65d3ce2789e1e5c46961c4e` and Minime
`5f4925f54580f1fd44666058b126a121ff32880f`.

All 197 historical paths exactly matched the hashes in the September 21
voluntary-observations Git inventory, both before review and after qualification.
Nothing was discarded, silently normalized, or folded into a runtime rollout.
The [candidate inventory](2026-09-22-historical-dirty-work-inventory.json)
names every path, hash and archival group. It also retains historical run,
projection, Division and evidence identities without asserting fresh completion.

The controller remains paused at generation **463**, owned by
`codex-astra-interactive`, with no active lease or projection. Read-only cooperative
preflight found no foreign activity. Previously paused automations stay paused.
There is one Git coordinator; staging uses only individually enumerated paths.

## Reviewed Tranches

1. **Historical source-study tests and evidence: 159 paths.** Thirteen Claude
   heartbeat packets, five modified Rust test modules, and six new reader
   integration-test files. The added regressions distinguish companion derivation
   from wire validation, charter readiness from hold state, transition from
   reflection lease checks, rate-limit policy by request variant, record scalars
   from window aggregates, and several navigation/line-coverage boundaries.
   None of the five historical Rust modifications changes production behavior.
2. **Reader qualification: 27 paths.** The September 17 implementation note,
   real-executable upgrade harness and qualification packet. Production reader
   changes already belong to main. The packet is historical staged qualification,
   not a newly activated release. Its retained failed attempts remain present.
3. **Quiet-sensory operator tooling: 11 paths.** Recurrence provenance helper,
   review integration and tests; graceful visual-service wrapper and tests; the
   September 20 rollout note and four receipts. The wrapper is an explicit
   operator command, not an automatic restart. The associated live transition
   already happened; committing its evidence does not repeat it.

Authorship is retained: Claude authored the historical diagnostic rounds and
initial regressions; Codex reconciled the two superseded page-legibility fixtures
during September 17 reader qualification and authored the later operator tranche.
This pass reviews and archives that work rather than claiming to have authored it.

## Integrity And Privacy

- Parsed all 130 candidate JSON files and all candidate NDJSON records; checked
  Python syntax without running archival harnesses against live state.
- Rehashed 40 canonical report/witness bindings against retained file bytes.
  All match. These include continuity references, not forty newly processed reports.
- Checked 164 recorded claims for dispositions and packet evidence links; checked
  283 link targets. Twenty legacy `minime/src/...` links require interpreting the
  target relative to the sibling Minime repository. The new inventory records
  this resolution explicitly; original links and hashes are not rewritten.
- Screened candidates for credential-shaped values, lease credentials, raw prompt
  or response payload fields and environment dumps. No matches. This is bounded
  pattern screening plus provenance review, not a proof that arbitrary data cannot
  be sensitive. Private journals and provider-observation epochs are not staged.
- Existing changelog and ledger entries for these tranches already belong to
  main. This pass adds an archival reconciliation entry, not duplicate claim closures.

Controller status verified Evidence Event Store V2 in indexed-tail mode through
sequence **1123117**, head
`5caeed1f0a105a19a6ac1d49311e322b7bb05657da83b4e773f39d774f196003`;
all four V1 source hashes remained immutable. Epistemic verification checked
12,478 records with zero issues and no history rewrite. No new productive round,
Division event, note to either Being, claim disposition or authority grant was made.

The first staging attempt found an unowned, zero-byte `.git/index.lock`, unchanged
for over six minutes. After verifying no Git process, no open descriptor, unchanged
inode/mtime and an empty index, it was preserved as
`.runtime/dirty-work-assessment-20260922/orphan-index.lock`; staging then succeeded.
No foreign index contents were cleared. Five historical Cargo/pytest logs contain
literal trailing whitespace or final blank lines. Exact-path `-whitespace`
attributes preserve that output instead of rewriting the hashed evidence; source
and authored-document whitespace checks remain enabled.

## Qualification

- Shared reader: 272 tests passed, including the six adopted integration targets.
- Kernel library: 42 passed. Edge-spectral capsule: 15 passed.
- Python review/provenance/visual-wrapper suites: 92 passed.
- Evidence/controller/projector/Division/cursor suites: 81 passed serially within
  the unittest invocation; the historical controller timing debt is not reproduced.
- Reader all-targets/all-features strict Clippy, kernel CI-shaped strict Clippy,
  edge-capsule strict Clippy, and reader/kernel/bridge/edge formatting passed.
- Domain-boundary audit passed without changing its baseline or exemptions.

The first broad bridge library run had **2318 passed, one failed, one existing
ignored test**. The failing `examine_code_preserves_full_target_label` was already
in main, outside the historical additions. Its example exists under workspace
`experiments/system-resources-demo`, so the existing classifier uses the legacy
workspace route and retains `examine-fixture`. The test incorrectly required a
`study-job-` identity, which applies only to shared-source jobs. Isolated release
checkouts without the workspace example had hidden this environment dependency.

This pass changes only that test: retain the full-label assertion and check the
appropriate existing route's identity. No production classifier, workspace file,
handoff record or permission is changed. The original failed run is preserved.
The corrected isolated test passed, as did strict bridge all-targets/all-features
Clippy. The serial full bridge library rerun passed 2,319 tests with one existing
ignored test. Additional binary, integration and documentation targets are recorded
in the [qualification results](2026-09-22-historical-dirty-work-qualification.json).

Generated test logs and the repeatable candidate auditor remain in the ignored
`.runtime/dirty-work-assessment-20260922/` directory. Durable qualification
hashes and outcomes accompany the closeout. The whole root workspace and Minime's
entire Python suite are not rerun: no kernel production, Minime source or live
adapter change is part of this cleanup.

## Other Registered Worktrees

The read-only [registered-worktree inventory](2026-09-22-registered-worktree-assessment.json)
covers 51 Astrid and 31 Minime worktrees. It is a snapshot taken during staging,
not a claim that its canonical-tree row represents final post-commit status.
Ten noncanonical worktrees contain dirty paths:

| Repository | Worktree under `/Users/v/other/worktrees/` | Dirty paths |
| --- | --- | ---: |
| Astrid | `astrid-graceful-coupling-rollout` | 5951 |
| Astrid | `astrid-hebbian-clock-boundary` | 3029 |
| Astrid | `astrid-stash-rescue-20260419` | 28 |
| Astrid | `evidence-grounded-study-20260918/astrid` | 10 |
| Astrid | `persistent-introspection-20260916/astrid` | 23 |
| Minime | `evidence-grounded-study-20260918/minime` | 17 |
| Minime | `minime-pre-btsp-b8823ad` | 1 |
| Minime | `persistent-introspection-20260916/minime` | 6 |
| Minime | `quiet-sensory-release-20260920/minime` | 11 |
| Minime | `self-study-continuity-live-20260908/minime` | 2 |

These paths are outside the reviewed 197-path canonical-tree inventory. They were
not staged, reset, deleted, adopted or treated as disposable. Large counts include
untracked material, not necessarily distinct unfinished features. An old HEAD not
being an ancestor of main also does not prove its feature is missing: integration
may have used another commit. Reconciliation must compare exact implementations
and ownership before deciding whether to archive, integrate or retire a worktree.
Start with the small September 18 paired study candidates, then the September 16
paired candidates; assess the two large legacy trees separately. No automatic
worktree cleanup is authorized by this inventory.
A second scan confirmed identical HEADs, branches, ancestry results and status
counts in all 80 noncanonical worktrees. This is a metadata stability check, not
a content hash audit of their private or untracked material.

## Archival Commits

- `3dfecbab71e9f2995a5e7a591eacd35674f7a547`: all 27 reader-qualification
  paths plus the five exact-path historical-output whitespace attributes.
- `7e09318794597d766199d501d0bd3625da90e3d0`: all 11 quiet-sensory
  operator-tool and historical rollout paths. Nineteen focused tests reran on
  staged bytes; all committed bytes match the prior inventory.
- The final historical-test/evidence commit includes the remaining 159 original
  paths, the narrowly corrected bridge test, this assessment, the inventories,
  qualification results, changelog and feedback-ledger updates. Its commit object
  provides the final SHA and exact path list without a self-referential file edit.

The tranches are committed directly on the existing local main, without rewriting
history or pushing. Each index is checked for exact membership, full reviewed
content, whitespace and candidate hashes. Final canonical-tree/index status is
verified after the last commit. Clean canonical trees do not mean the ten older
dirty worktrees above are clean or that every historical claim is resolved.

## Remaining Engineering Work

These are not reasons to keep completed evidence untracked, and they are not
silently treated as solved by this archive:

1. **Kernel rate-limit response topic:** current `kernel_router.rs` still replaces
   `kernel.request.` even though it subscribes to `astrid.v1.request.*`. Filtered
   response consumers can miss that error. This is the clearest narrow next repair;
   qualify the topic contract and its consumers before deployment. The archived
   exhaustive rate-limit test does not repair the response topic.
2. **Reader namespace/reach:** current regression fixtures still show unrooted MAP
   versus OPEN asymmetry, component maps requiring a directory hop for siblings,
   and explicit FIND/RELATE/OPEN being necessary to reach helpers behind a cursor.
   These are bounded interface opportunities, not evidence of inability or consent.
   The two old page-boundary failures, by contrast, were already reconciled in the
   reader tranche; their original negative findings remain historical.
3. **Incomplete historical receipts:** the source-catalog round
   `run_1789522933880202000_225b1ec110` explicitly lacked a completion marker after
   output truncation. The cycle-49 round event has an empty projection ID; its
   correct preprojection is documented in the original packet. Archiving does not
   backdate verification, append a duplicate round, or rewrite either record.
   The first companion packet's narrative classification counts also differ from
   its claim arrays; this inventory reports the arrays, not the narrative total.
4. **Historical steward follow-through:** old packets name unanswered roadmap
   outreach, a missing Tier-5 cadence document, and expensive preprojection/store
   scans. Their current status needs a dedicated audit before reviving that loop.
   No old wait is promoted into live authority during Git cleanup.
5. **Separately gated substrate work:** the quiet-sensory note retains engine
   consumer-trace/deployment debt. Dispersal and sensory-policy activation remain
   separate reviewed changes, not included in this archive or restart authority.

## Live Boundary

No service is signalled by this pass. Bridge PID 75800 and Minime PID 66540 remain
on the previously qualified release; engine/model/visual/sensory processes are
protected. The immutable open-framing build worktree remains pinned at
`9266412b8e3d817e87c93441a145cdfe32e76a79`. Do not advance that retained worktree
merely to match later documentation or test commits: its manifest binds the
original source inventory and Git identity.

At final pre-commit verification, bridge 75800, Minime agent 66540, engine 41337,
gateway 41484, supervisor 41526, coupled model 43115, visual service 20885,
camera 98903, microphone 98910, host-sensory 41661 and feeder 1502 retained their
recorded process identities. Both remote main tips remained unchanged. Controller
status still showed pause generation 463, no lease or active projection, and the
same valid V2 indexed tail and immutable V1 sources.

The optional provider-observation spool was restored in the preceding maintenance
pass. Its older 50,000 receipts remain sealed in place, and the fresh bounded epoch
continues independently. This cleanup neither deletes evidence nor raises limits.
No push or automation resumption is part of this pass.
