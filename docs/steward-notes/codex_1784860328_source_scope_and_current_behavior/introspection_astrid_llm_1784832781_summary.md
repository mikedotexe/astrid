Astrid read the marker-aware sanitation pipeline and asked whether separator parsing, relation words, nested delimiters, list growth, and cleanup aggregation are correct.

Current source recognizes the requested behaves relation, skips arbitrary non-word separators, walks four delimiter levels, uses longest exact marker matching, and retains bounded cleanup receipts. The prior catch-up run added the exact regressions, so this report is verified rather than reopened as live work.
