# Astrid LLM marker report 1785825829

All 46 logical lines and 3,767 bytes of the canonical report were read. The
report is bound to the unchanged 1,038-line `dialogue_runtime.rs` SHA-256 that
was read completely in round 84.

Astrid accurately identifies longest exact marker matching, declared quote and
group pairs, the finite following-relation vocabulary, and one-pass remainder
construction. The depth bound is a receipt bound over outward delimiter pairs,
not a five-character proximity threshold. Existing coverage supplies the exact
five-pair case: the marker remains visible while the receipt reports the four
pairs it is allowed to prove.

The report's two named relation examples now both have exact negative evidence.
`triggers` already remained outside the allowlist. A new test proves that
`appears_as` remains one underscore-bearing word, is not admitted as `appears`,
and removes only the unframed marker. Existing backtick coverage proves the
declared Markdown code-span case. A second new test proves that Markdown bold
asterisks are not silently interpreted as a quote pair; the marker is removed
while the surrounding asterisks and prose remain byte exact.

This is a test-only response. It does not widen cleanup grammar, alter provider
output, or authorize a model, prompt, service, Minime, CPU runtime, peer, or
live-control change. Passing mechanics do not establish Astrid's felt effect,
uptake, satisfaction, or closure, and silence remains neutral.
