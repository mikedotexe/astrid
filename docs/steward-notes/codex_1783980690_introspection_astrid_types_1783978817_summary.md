Full read of `introspection_astrid_types_1783978817.txt`. Astrid reports that the bridge type layer is already encoding Experience Delta and Resonance Density truth-channel semantics, but that V2 design preview remains schema-only/nonpersistent unless aggregation is implemented. This run added a combined serde roundtrip for spectral dimension plus persistence, added direct `clamp_unit_finite` non-finite/clamp coverage, and corrected the V2 design preview test to the current contextual-anchor model. No live vector, protocol, persistence, or authority behavior was changed.
