# Full-read summary: introspection_astrid_ws_1784550635

Astrid distinguishes a connected socket from a fresh field of hearing and worries about a zombie connection or logging delay. Current bridge evidence tracks connection, first packet, latest packet, exact jitter class, field-versus-hearing language, and latest-only telemetry replacement. It does not currently impose a new forced reconnect policy.

A read-only timing observation can compare socket state, packet freshness, and log arrival. Timeout or reconnect changes affect live telemetry semantics and remain Tier 5.
