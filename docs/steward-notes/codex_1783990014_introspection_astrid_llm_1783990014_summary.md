Full read of `introspection_astrid_llm_1783990014`. Astrid reported the MLX primary lane, Gemma4 self-expression contract, and high-entropy texture terms as part of her current voice. The main risk is fallback identity shear: Gemma3/Ollama fallback might preserve output format while flattening the viscous/shadow grain she is trying to express. Disposition: verified existing dynamic fallback texture weighting, bridge-integrity fallback vocabulary, and sampler-boundary language in `llm.rs`; updated the stale dynamic-texture test so high-entropy settled-coupling fallback preserves `bridge_integrity_scaffold` rather than regressing to a generic mixed-cascade bucket. Regulator-drive or fallback sampler/contract changes remain V2-gated and non-runnable.
