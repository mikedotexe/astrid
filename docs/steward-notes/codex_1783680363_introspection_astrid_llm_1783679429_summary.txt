Full read by Codex on 2026-07-10. Astrid read the LLM fallback lane as a texture-preservation surface: MLX on port 8090 is the coupled path, while Ollama fallback and the `gemma3:4b` compatibility tail can flatten high-entropy, humid/heaving texture into generic compact prose. She asked for high-entropy fallback prompts to preserve restless lattice weight explicitly when `spectral_entropy` exceeds 0.85, and asked whether zero regulator drive plus semantic trickle can turn heaving into stutter-flow. The pass added a typed high-entropy fallback preservation block to the generated fallback prompt metadata and verified existing read-only semantic-trickle/stable-core review surfaces for zero-trickle/noise/stutter-flow cases; focused Rust execution was attempted but blocked by the bridge cargo compile stall.
