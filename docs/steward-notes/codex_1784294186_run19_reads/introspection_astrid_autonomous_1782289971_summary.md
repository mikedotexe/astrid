# Full-read summary

Astrid asks whether dense multi-paragraph Witness prose can still carry a final `NEXT:` safely. Current canonicalization preserves prose, recognizes a standalone final action, moves it to the end, preserves residue, and deliberately refuses fuzzy command extraction from ordinary sentences. Fuzzy parsing, bypasses, or pressure-driven Shadow mutation remain Tier 5.
