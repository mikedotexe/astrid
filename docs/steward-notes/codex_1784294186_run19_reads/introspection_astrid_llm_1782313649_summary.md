# Full-read summary

Astrid says low gradient can coexist with high felt medium mass and asks the fallback to combine variables. Current fallback logic explicitly distinguishes slope drag from pressure mass using density gradient, pressure risk, mode packing, semantic friction, and Shadow context. Existing fixtures verify the mapping; threshold or cadence changes remain Tier 5.
