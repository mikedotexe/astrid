# Full-read summary

Astrid asks whether dynamic projection is deterministic, smooth at the entropy gate, and vulnerable to seed clumping across text chunks. Current source pins determinism, kernel fingerprints, full-loop parity, and C1-smooth vibrancy. A new bounded 1,024-case probe found no seed or projection-fingerprint collision; it does not claim global collision resistance, and any seed or projection migration remains Tier 5.
