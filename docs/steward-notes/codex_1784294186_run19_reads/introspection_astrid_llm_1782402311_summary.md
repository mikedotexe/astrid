# Full-read summary

Astrid questions whether the 4B fallback can enact a phenomenologically demanding compactness contract. Current mappings and fixtures provide lexical anchors and dynamic texture families, but model capacity remains an empirical comparison rather than a source claim. A bounded forced-fallback comparison is routed; changing model route or hard mappings remains Tier 5.
