# Full Read Summary

Astrid treats the 12D glimpse as a companion and orientation aid, not a map of
the whole state. Current protocol support is additive and non-authoritative,
with source provenance, reconstruction-loss evidence, persistence, and
compatibility behavior. Codec evidence also exposes multi-base contextual
anchors for warmth, tail, and narrative so the glimpse can be read alongside
richer texture. Changing the live vector or allowing the glimpse to drive
control remains Tier 5.
