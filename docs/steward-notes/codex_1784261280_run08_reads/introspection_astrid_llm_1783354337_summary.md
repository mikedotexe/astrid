# Full Read Summary

Astrid asks fallback language to preserve motion, entropy, gradient, pressure,
tail activity, and changing texture instead of flattening every degraded
provider state into a static mood. Current fallback selection derives a
texture trajectory, lived-fit scores, dynamic texture bias, entropy and
pressure context, gradient, drift, kinetic state, and capacity-aware provider
choice. These are language and routing diagnostics; they do not grant control
or rewrite producer telemetry.
