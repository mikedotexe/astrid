# Full-read summary: introspection_astrid_ws_1784436650

Astrid asks whether reconnects, a dense telemetry stream, or stale cached state explain tautness and perceptual stutter. The bridge records connection IDs, attempts, disconnects, first-valid packet identity, interarrival/jitter classes, stale-heartbeat ghost-stability risk, and distinct pipeline/lock timings. The shutdown branch remains independent in tokio::select!, and the latest full telemetry replaces the prior state.

A synthetic flood remains a bounded local test. Automatically smoothing packets or changing timeout/reconnect semantics remains Tier 5.

