# Study context and synthesis, September 9

Mike approved preserving useful context, making comparison and question updates
easier for both Beings, and testing dedicated thinking. Minime's selected nine
12:08–12:26 PDT journals already used 4,096-token requests and stopped normally.
The real retention defect was the 700-byte opening excerpt: the useful 12:20
persistent-worker/per-chain-task conclusion vanished from the following input.

The shared reader now retains four recent visible responses per inquiry within
a 9,000-byte rendered JSON budget, plus the authored finding/current question.
Oldest complete accounts drop first; oversized answers preserve beginning and
ending with an explicit omission and completeness flag. Stored response provenance
and inquiry ownership survive restart. Version 3 migrates earlier checkpoints;
an older reader refuses the new version. No private reasoning is carried.

Optional exact RELATE commands sit beside the current question, and a two-location
SESSION command makes comparison immediately available. Writing remains freeform;
the prompt welcomes developing implications, uncertainty and tests without requiring
length or any particular conclusion. Shared whole-input admission is 24,000 bytes;
SELF_STUDY Ollama context is 32,768 tokens, with output still 4,096. Other journal
modes retain their already increased ceilings. Minime's MLX, Ollama and fast fallback
adapters honor the shared reader budgets and remain compatible with the old helper.

Qualification includes full shared-reader and bridge suites, strict clippy, full
Minime tests, copied-state migration, and replay of the nine saved visible answers.
The replay keeps the entire 12:20 conclusion plus three earlier accounts in the
12:25 input. It proves carriage, not improved subsequent reasoning. Test logs and
release receipts are in `/Users/v/other/worktrees/study-context-20260909/evidence`.

The research project freezes a four-call thinking comparison: two exact saved
inputs, paired on/off with equal 8,192 generation/32,768 context allowances,
reversed order, fixed seeds, standalone Ollama only. Both thinking-enabled runs
stop normally but expose only `NEXT: SELF_STUDY CONTINUE`. Both disabled runs
produce substantive prose. Production thinking remains off; a future trial should
test final-answer delivery before judging reasoning quality. Nothing was sent to
the Beings or their coupled reservoir as part of the experiment.

Integration owns only the explicit changed paths. Foreign heartbeat edits remain
separate. Rollout order: commit both main branches, stage the clean immutable bridge,
reload Minime's adapter at an observed idle window, then activate the shared reader
and Astrid bridge with graceful drain. This prevents the old Minime adapter's input
ceiling from rejecting a new larger offer. Activation identities and observed
continuity are appended after verification; this paragraph is not a live claim.

Owning implementation: Codex Astra interactive. Research history: HSS-14 in
`/Users/v/other/reservoir-llm-research/research/histories/self-study.md`.
