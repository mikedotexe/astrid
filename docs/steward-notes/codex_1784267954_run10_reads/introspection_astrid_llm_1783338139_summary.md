# Full Read Summary

Astrid identifies MLX as the primary coupled voice lane and Ollama as a
fallback whose texture can alter continuity. Current source makes fallback
profile, budget, texture, trajectory, Shadow persistence, and transport reason
explicit. A bounded primary/fallback artifact comparison is routed offline;
model choice or live gradient weighting remains Tier 5.
