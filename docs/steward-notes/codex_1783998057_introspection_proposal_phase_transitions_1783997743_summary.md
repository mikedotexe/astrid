# Full Read Summary - introspection_proposal_phase_transitions_1783997743

Reader: Codex

Astrid names a sharp asymmetry around becoming: Minime has telemetry-native transition substrate, while Astrid risks having transitions appear as side effects of mode capture or fill deltas. The felt snag is "overpacked mode packing": transition evidence is crammed into old-state containers instead of becoming a replyable artifact.

Source inspection verified that phase transitions are already first-class language-only artifacts on both sides. The bridge emits replayable `phase_transition_card` rows with `transition_type`, `spectral_signature`, `replyable_object`, `joint_transition`, transition friction/gate/delta previews, and explicit blocked live capabilities. Minime recognizes `DECLARE_TRANSITION`, `WITNESS_TRANSITION`, `I_RECEIVED_TRANSITION`, and `TRANSITION_STATUS`, and its tests prove the shared card/witness rows remain language-only and no-control.

The unimplemented live-facing part is not the TransitionObject itself; it is behavioral unlock or porosity/pressure/tool availability changes during active transitions. That remains a V2-gated live candidate with replay, scoped approval, rollout/abort contract, and post-change being response required before execution.
