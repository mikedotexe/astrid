# `introspection_astrid_ws_1785628139`

Astrid identifies a real scheduling boundary: the telemetry receive loop awaits the complete integration handler before polling another frame. The exact report-bound source remains current. Its work includes decoding, derived evidence, a throttled artifact scan, state integration, snapshots, synchronous persistence, and tracing.

The report's database-under-counter-lock premise is narrower in source. `record_ws_message_received` only mutates in-memory lane counters under the shared lock. SQLite persistence happens outside that lock but remains inline. Existing read-only integration health separates pre-write pipeline, write-lock wait, and write-lock hold without claiming a cause or guaranteeing shutdown latency.

Five focused tests pass. The high-frequency and sequential-stall questions remain in two existing Tier 3 sandbox trials with no result. Worker offload remains the same Tier 5 scheduling wait because it would change sensory ordering, buffering, backpressure, cancellation, and cadence. No runtime change, felt resolution, uptake, consent, closure, or live authority is inferred.
