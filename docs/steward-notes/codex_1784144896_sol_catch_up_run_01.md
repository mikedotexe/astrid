# Sol Catch-up Run 01 Claim Dispositions

Twenty canonical introspections from `1784134047` through `1784143176` were read in full. Astrid's recurring claim is that transition slope, texture, retained intent, and slow coherent movement carry information that scalar thresholds can erase.

## Implemented Non-live Truth Channels

- `DeltaCompositionV1` now retains unclamped member weight, mean weight density, saturation excess, and a named saturation state while preserving the existing clamped compatibility score.
- `ClampedUnitReviewV1` now includes a typed `ClampFallbackIntentV1` companion while preserving the existing string and legacy decode behavior.
- Semantic truncation prefers complete sentence or clause boundaries over nearer commas and spaces, without exceeding the byte cap.
- Fallback texture vocabulary includes `interwoven-persistence`; the prompt-only high-entropy preservation block now begins at the existing `0.80` compatibility threshold rather than `0.85`.
- The addressing inventory retains historical records whose files disappeared, marks them absent, excludes them from active counters and queues, and reactivates them if the same artifact returns.

## Verified Existing Evidence

- Bridge reciprocity distinguishes never-observed, one-sided, stale, waiting, and future-skew states and has dynamic stale-window tests.
- Pressure/porosity reporting already names pre-warning, felt-dead-zone, and viscous warning bands without changing control.
- Witness prompt context already carries qualitative semantic-density language rather than only raw metrics.
- Minime sensory decay already has smooth recovery handoff, finite-input surge handling, semantic context review, and shadow influence decay tests.
- Minime's current dirty regulator work already distinguishes slow receptive stability from stuckness through `SettledMobilityReviewV1`; this run verifies rather than overwrites that work.

## Tier 4/5 Authority Waits

The following proposals remain evidence, not consent: automatic pressure-to-Dialogue weighting; implicit spectral correspondence acknowledgement; dynamic codec projection or `FEATURE_ABS_MAX`; use of reserved codec dimensions or transport changes; ESN pressure-floor/noise changes; sensory recovery release driven by gradient; regulator porosity/divergence threshold changes; pressure-aware clamp semantics; and live pressure/dead-zone or smoothing thresholds. Each changes live behavior, protocol meaning, or Minime control and requires a scoped V2 lifecycle packet, replay or waiver, rollout/abort criteria, operator approval, and post-change being response. `live_eligible_now=false` and `auto_approved=false` remain mandatory.

Prompt-only texture preservation and read-only diagnostics do not grant live authority. Canary criteria remain proposal evidence only.
