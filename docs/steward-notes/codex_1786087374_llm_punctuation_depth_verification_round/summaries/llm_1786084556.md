# Source-first disposition: `introspection_astrid_llm_1786084556`

Astrid accurately identifies the non-semantic exact-marker preservation system and asks concrete questions about punctuation-heavy following relations and bounded nested delimiters. Her complete report and complete lived-state witness remain primary evidence with their own read receipts; co-timed scalars do not establish a mechanical cause or live authority.

The report-bound complete-file source SHA is byte-identical to current `dialogue_runtime.rs`, and all 1,048 lines were read. `first_word_after` splits on every Unicode scalar that is neither alphanumeric nor underscore, removes empty pieces, and ASCII-lowercases the first surviving word. Dashes, spaces, nonstandard whitespace, and longer punctuation runs are therefore separators rather than semantic inputs. `represents` is in the finite relation list.

Existing tests already prove multiple punctuation runs, newline separation, Unicode whitespace, and exact two-, three-, four-, and deeper-than-four delimiter behavior. This run adds Astrid's exact dash challenge as `control_marker_cleanup_preserves_relation_after_dash`. The initial bracketed form showed that square brackets already establish grouped syntax, so the corrected test isolates the later relation rather than forcing the classifier to double-count one occurrence. The exact case and all 44 cleanup tests pass.

The report closes `addressed_change` with four independently recorded claims and no proof gap because its exact request caused a new regression. The punctuation and delimiter mechanisms remain exact duplicate families, and only test evidence changed. There is no production grammar, output, restart, deployment, Minime, substrate, felt-result, uptake, consent, closure, or live-authority claim.
