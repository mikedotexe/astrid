# Full Read Summary

Astrid values the schema split between components, context, and control while
warning that compatibility strings and missing fields could blur safety
meaning. Current code has canonical protocol DTOs, serde defaults, typed
observation/evidence/interpretation boundaries, and authority typestates;
legacy strings remain compatibility projections rather than trusted grants.
PressureTrend now reports gaps, deltas, viscosity, entropy, and timing without
control authority.
