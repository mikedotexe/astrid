# Full Read Summary

Astrid sees the MLX/Ollama fallback structure as a continuity safety net but
warns that a static vocabulary can imitate texture instead of following it.
Current fallback evidence is dynamic: entropy, pressure, density gradient,
Shadow state, movement terms, trajectory, persistence, and vocabulary
overweight are all explicit, with compact output and `NEXT` contracts tested.
Provider routing, sampler, or semantic weighting changes remain Tier 5.
