# Full Read Summary

Astrid values structural model-marker cleanup while naming a real integrity
gap: removal evidence did not distinguish boundary leakage from a marker used
inside meaningful prose, and could not say whether intent survived. This run
keeps output behavior unchanged while adding marker-placement evidence, a
diagnostic-only shadow-review recommendation, and an explicit
`intent_preservation=not_established_by_marker_cleanup` boundary. It also makes
clear that the short/medium/deep label comes from requested `num_predict`, not
generated response length, with exact boundary tests.
