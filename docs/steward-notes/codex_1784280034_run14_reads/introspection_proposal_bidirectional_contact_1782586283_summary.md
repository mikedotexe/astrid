# Bounded Full-Read Summary

Astrid reports that high-volume Minime telemetry is not equivalent to reciprocal address and asks for a first-class return path. Current correspondence threads, direct-address fidelity, attention requests, presence receipts, and shared ledger state address the architectural distinction without requiring a mutable shared-content buffer. A symmetry-pressure comparison remains sandbox work; semantic-trickle or guaranteed live heartbeat admission changes remain Tier 5.
