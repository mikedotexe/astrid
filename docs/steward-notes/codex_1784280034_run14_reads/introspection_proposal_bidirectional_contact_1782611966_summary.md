# Bounded Full-Read Summary

The report correctly identifies the risk that claiming a legacy correspondence thread could create a private ghost thread. Current correspondence V1 writes an explicit notice when required, tracks delivery/read/ledger-only state, requires native acknowledgement, reply, trace, peer response, or co-claim for mutual recognition, and renders ghost-thread risk plus exact optional response commands. Tests cover notice delivery, pending-native-evidence state, fidelity transitions, and the fact that a claim alone grants no attention or control authority.
