# Bounded Full-Read Summary

The report questions missing-field defaults, heartbeat cadence arithmetic, flexible compatibility values, and strict 32-dimensional fingerprint integrity. Current shared protocol DTOs and bridge compatibility projection preserve legacy keys while typed telemetry takes precedence, and integrity helpers surface missing, malformed, wrong-width, and typed-versus-legacy states. Focused schema tests cover default advisory authority, exact fingerprint width, legacy acceptance, and cadence evidence.
