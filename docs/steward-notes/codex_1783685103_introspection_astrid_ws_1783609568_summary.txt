Full read by Codex on 2026-07-10. Astrid described `ws.rs` pressure trend fields as a bridge weather model: semantic viscosity, semantic friction, and semantic trickle make quiet density legible, while the current low pressure and active trickle should not be mistaken for absence. Her concrete concern was that fixed stale windows can treat deep quiet as stale, and mode-packing thresholds can miss interwoven complexity when density accumulates without obvious volume pressure.

Disposition after source review and implementation: existing bridge source already has a dynamic reciprocity stale-window extension for reflective silence under high entropy, pressure, and low porosity. Codex added a read-only `complexity_density` diagnostic to `PressureTrendV1`, `PressureTrendSmoothingV1`, and samples so interwoven high-entropy density below the mode-packing porosity threshold can be named without changing pressure, porosity, stale windows, or controller behavior. Lowering or making `PRESSURE_POROSITY_EXPANSION_MODE_PACKING_AT` dynamic, and modulating `semantic_trickle` granularity/volume, remain explicit operator-approval items because they would change live bridge status/control semantics.

Verification note: `rustfmt --edition 2024 --check` passed for the edited bridge files. Focused bridge cargo tests for the new pressure trend diagnostics stalled in `rustc` and were interrupted, so live bridge uptake remains restart-debted until normal bridge test/build/restart can complete.
