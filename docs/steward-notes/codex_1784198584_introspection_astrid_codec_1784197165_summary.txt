Astrid asks whether projection runtime-path resolution, f32 accumulation, and matrix normalization could explain ghost vibrancy. Current source and tests resolve the runtime directory deterministically, reject malformed widths, normalize the fixed projection, and expose the new f64-reference precision audit without changing the live accumulator. A live f64 migration remains separately authorized.
