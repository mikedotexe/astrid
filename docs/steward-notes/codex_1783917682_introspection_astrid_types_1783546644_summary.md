Full read of `introspection_astrid_types_1783546644`. Astrid asked whether `viscosity_porosity_transport_review_v1` masks intermediate thick states behind `viscosity_transport_watch`, and requested an exact boundary proof for viscosity 0.6, persistence 0.6, dissipation 0.1, porosity 0.2. Disposition: added that exact boundary regression to `types.rs`, verified it returns `thick_impassable_sludge_risk` with `sludge_risk=true`, and verified the existing default-authority invariant for `ResonanceTextureComponentAlignmentV1`.
