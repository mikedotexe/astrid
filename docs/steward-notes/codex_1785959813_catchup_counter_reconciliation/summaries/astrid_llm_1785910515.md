# Full-read summary

Astrid raises a concrete UTF-8 slice-panic concern. Exact occurrence start and end positions come only from a scan that advances by each Unicode character's byte length and exact ASCII marker lengths, so the slices remain character boundaries; the multibyte exact-byte regression passes. The contextual test suggestion is also covered because grouping itself preserves a bracketed marker. No defect is reproduced.
