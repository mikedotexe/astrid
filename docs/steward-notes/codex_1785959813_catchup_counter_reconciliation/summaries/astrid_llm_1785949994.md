# Full-read summary

Astrid revisits marker reference syntax, nearest non-whitespace delimiters, curly-brace grouping, and the finite relation allowlist. The exact source intentionally admits only a matched nearest delimiter pair; intervening punctuation is not silently treated as quoting. Existing regressions cover whitespace-separated braces and `echoes`, while `contains` is absent from the allowlist. This is source-and-test verification, not a parser rewrite.
