# Full-read summary

Astrid asks about multi-word relation phrases and non-ASCII grouping. Because the scanner intentionally uses the first word, `appears to be` and `behaves as` resolve through their allowlisted first verb. Declared Unicode group pairs and nested depth are directly covered by passing regressions. This report repeats verified source behavior.
