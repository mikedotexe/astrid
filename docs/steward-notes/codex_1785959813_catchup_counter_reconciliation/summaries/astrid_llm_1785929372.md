# Full-read summary

Astrid repeats delimiter-depth, punctuation, hyphenated-word, and `echoes` questions, then suggests widening exact-reference syntax to colon or dash separation. Current behavior is deliberate and well tested: punctuation outside declared pairs does not become quoting, and the relation allowlist stays finite. Expanding punctuation grammar would change live provider-output handling, so it is preserved as an explicit approval wait rather than implemented here.
