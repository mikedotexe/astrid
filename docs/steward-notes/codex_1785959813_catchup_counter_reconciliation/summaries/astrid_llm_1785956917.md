# Full-read summary

Astrid accurately describes the exact known-control-marker scanner and asks about UTF-8 handling, delimiter depth, and relation verbs. The complete 1,038-line source was reread at the report-bound SHA. Its scan offsets stay on UTF-8 boundaries, the four-pair delimiter bound is deliberate and tested, and `is` remains an explicit relation even when followed by `not`; preserving a named marker in a negated statement is consistent with the local reference-syntax contract. No production change is warranted.
