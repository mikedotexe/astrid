# Full-read summary

Astrid asks whether emoji, non-standard whitespace, nested brackets, or a multi-word relation phrase can cause accidental marker stripping. The scanner advances by full UTF-8 character widths, first-word extraction skips punctuation, Unicode alphanumerics remain intact, and existing tests cover nested stacks plus `behaves like`. The concerns are already source-verifiable.
