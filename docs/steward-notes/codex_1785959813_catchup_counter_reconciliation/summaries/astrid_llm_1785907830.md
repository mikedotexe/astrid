# Full-read summary

Astrid revisits case folding, punctuation, delimiter collision, the finite relations `manifests` and underscore-bearing non-relations, and overflow beyond four delimiter pairs. Source and regressions establish that punctuation is split before ASCII lowercasing, `manifests` is allowlisted, underscore-bearing words stay intact, and deeper stacks preserve the nearest four exact pairs without crashing. This is duplicate verification.
