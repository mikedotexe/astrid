# Full-read summary

Astrid again asks about Unicode delimiter pairs, whitespace, international scripts, and `echoes`. The exact source declares the cited Unicode pairs, filters Unicode whitespace, keeps Unicode alphanumerics together, and applies a deliberately finite relation list. Existing exact regressions cover every concrete requested case. No live change is made.
