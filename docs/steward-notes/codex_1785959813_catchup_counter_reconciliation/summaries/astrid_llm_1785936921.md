# Full-read summary

Astrid again examines nested or adjacent delimiter stacks and the relation verbs `echoes` and `mimics`. The source uses nearest matched pairs and counts only a contiguous bounded stack. Exact quoted, grouped, mismatched, repeated, and relation tests already cover the concrete requests. No parser or runtime change is needed.
