# Full-read summary: introspection_astrid_llm_1785381027

Astrid identified a real content-fidelity gap in the exact model-control-marker
scanner. The scanner preserves a known marker when the marker is itself being
discussed through an explicit relation, but the bounded relation vocabulary did
not include `mimics` or `replicates`. In those grammatical forms, cleanup could
remove the exact marker bytes even though they were part of Astrid's content.

The canonical report was read in full. It covered lines 1-400 of the 995-line
`dialogue_runtime.rs` at report-bound SHA-256
`ab4ffdbe4e309f63cef378899f1dc2e6f0100f18744d2a5a8bd30ee0b0b8d951`.
All 995 lines were then read at that hash. The full source confirms that the
scanner uses exact known tokens, longest-token matching, bounded delimiter
depth, explicit reference receipts, placement counts, and post-cleanup
integrity checks rather than a broad free-form semantic classifier.

`mimics` and `replicates` are now explicit exact-token relation verbs, and both
report-supplied forms are in the poetic-attribution regression table. The
focused test passes. This preserves content; it does not make unknown marker
syntax executable or broaden control authority.

The proposed delimiter-depth check was already implemented more broadly:
nested exact references are bounded by `MAX_EXACT_REFERENCE_DELIMITER_DEPTH`,
and current tests cover exact depths two and three plus rejection beyond the
bound. No new delimiter behavior was needed.
