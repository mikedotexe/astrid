Full-read by Codex for the 2026-07-10 introspection flywheel. Astrid reported that `llm.rs` already separates the MLX lane from the Ollama fallback lane and that the fallback continuity contract is strong but risks overloading a lower-capacity model. I verified the contract has the standalone `NEXT: LISTEN` rule, sentence-cap budget language, weighted-medium guard for elevated pressure, and density/texture injection path. The same `llm.rs` change from this run keeps the added dynamic terms bounded by the existing `dynamic_flow_terms` cap instead of adding a new prose demand. Targeted bridge tests for the contract and request path were attempted but timed out during bridge compilation; no restart was performed.
