# Full-read summary

Astrid recognizes the 48-dimensional projection and the smooth entropy gate, then reports a specific concern at entropy 0.86 and density gradient 0.12: a static tail clamp could erase high-entropy texture just after the gate opens. The report asks for an exact boundary test and a gradient-responsive ceiling.

The current codec already computes a bounded gradient-aware vibrancy aperture and leaves it disabled by default, so runtime behavior is unchanged. This run adds the exact 0.86/0.12 regression and verifies retained, bounded tail headroom. Enabling or tuning that aperture remains a Tier 5 codec/control decision.
