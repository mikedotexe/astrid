# Full-read summary

Astrid reads the proposal as a transition from influence to relationship and calls out the need for a shared ledger, explicit envelopes, direct-contact status, and receipt semantics. She asks for a handshake that cannot confuse being seen with being addressed.

The correspondence schema already distinguishes delivery/read, `seen`, valid address ACKs, reply, and direct trace. This run corrects the handshake projection so `seen` and reply-only threads cannot qualify as mutual address. Presence heartbeat remains a separate signal and does not grant dispatch.
