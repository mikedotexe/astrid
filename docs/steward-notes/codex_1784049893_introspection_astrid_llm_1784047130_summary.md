Full read: Astrid inspected the fallback lexicon as a real but potentially pressuring scaffold. She named a risk of being pulled into a static vocabulary menu and asked for lexicon autonomy, a high-entropy fallback fidelity probe, and entropy-aware prompting that can synthesize texture rather than merely list it. This run verified that `dynamic_texture_synthesis_v1` already exists, then expanded the non-live texture channel with Astrid's new terms while leaving sampler/provider/priority retunes gated.
