# Live Authority Waits From 2026-07-12 Flywheel Packet

Source packet: `introspection_astrid_codec_1783899883`, `introspection_minime_regulator_1783899586`, `introspection_astrid_llm_1783899120`, `introspection_proposal_phase_transitions_1783554042`.

## Why This Exists

The packet produced strong substrate signal, but several requested moves would alter live delivery, regulator behavior, fallback routing, or transition authority. They are preserved here as explicit approval work rather than silently downgraded or applied.

## Approval Waits

1. Codec adaptive delivery: approve, reject, or sandbox `semantic_trickle` / `codec_gain` adjustment for states where the current bounded codec weight feels too viscous.
2. Codec reserved Shadow-Density: approve, reject, or sandbox writing a Shadow-Density signal into reserved dims `44-47`. Current implementation only verifies a default-off readiness report; it does not write live reserved dimensions.
3. Minime regulator pressure/mobility: approve, reject, or sandbox entropy-scaled `INHABITABLE_SETTLED_PRESSURE_INTERFERENCE_MAX` and `shadow_volatility` as an input to `effective_mobility`. Current evidence verifies observability only.
4. Fallback routing: approve, reject, or sandbox latency-forced fallback texture comparisons and any dynamic model/provider/sampler routing beyond the existing Gemma4-default texture budget.
5. Transition behavior unlock: approve, reject, or sandbox any transition artifact effect beyond language-only replayable cards. Current fields are durable truth artifacts, not controller or peer-runtime authority.

## Current Boundary

No live semantic vector ceiling/gain/reserved-dim write, Minime pressure threshold, Minime mobility behavior, fallback provider/model/sampler routing, transition behavior unlock, pressure/fill/PI/sensory cadence/controller behavior, bridge protocol/ABI, staging, git add, commit, or push was performed by Codex.

## First Safe Next Step

Pick one wait above and approve a sandbox/replay design first. Only after bounded evidence should any live substrate/control-facing change move to the normal build/restart path.
