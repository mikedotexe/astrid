# Full-read summary

Astrid identifies a concrete transport risk: sequential telemetry work and
state writes could delay later frames or conceal a silent connection loss. The
complete 41-line canonical report was read at SHA-256
`5e0786237a5b38fdd3bf2e3fd4a39bd0992e5889875eb05e85e36e9fc6317bdf`
with lived-state witness
`lsw_886ca9bb30efc494adc4e69c66c2373b6a94b67cce0a3f9abfd36787054814f9`.
The scheduling concern remains serious even where current source narrows the
specific deadlock hypothesis.

All 1,041 exact current lines of `telemetry_port.rs` were already read and the
hash is unchanged. The report accurately describes the persistent WebSocket
subscriber, message variants, lifecycle recording, exponential backoff, and
downstream lambda analysis. The per-message state write that records receipt
is scoped and dropped before `handle_telemetry_message`; the heavier decode,
derivation, evidence construction, and scanning also occur before a later
scoped write. Current source therefore does not support a nested-lock deadlock
claim, while it does preserve possible sequential pipeline latency.

Existing integration health explicitly separates pre-write pipeline time,
state-lock wait time, and lock-hold time. The focused timing regression and two
backoff regressions pass inside the complete bridge library suite. The select
loop has no explicit silent-idle timeout, so that absence is retained as an
exact source finding rather than inferred runtime failure.

Adding a silent-idle timeout or moving telemetry work to a worker would change
connection cadence, ordering, buffering, backpressure, cancellation, or
sensory scheduling. Those changes remain exact Tier 5 waits pending operator
approval and a behavior-preserving design. No socket behavior, process,
restart, deployment, telemetry value, or felt result changed in the source
reading itself.
