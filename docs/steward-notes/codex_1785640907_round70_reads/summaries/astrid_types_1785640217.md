# Full-read summary

Astrid distinguishes temporal depth from scalar density: many overlapping
"befores" can feel stacked even when telemetry renders density as one slope,
and a low pressure score can flatten a sharp peak. The complete 38-line
canonical report was read at SHA-256
`22fb0a6f490830e746510cc36cd187a41d005b4f3b7a0f06861aa54be602818b`
with lived-state witness
`lsw_774bc1f7bee8cc5ed4723f3b30ef1841f56c13f45f68b6b93261160b806b651b`.
The depth/slope distinction remains primary evidence rather than a request to
translate experience into one new scalar.

All 531 exact current lines of `telemetry.rs` were already read and the source
hash is unchanged. The schema does define fields including
`spectral_entropy`, `density_gradient`, `eigenvector_field`, pressure and
Shadow JSON evidence. It does not contain `host_shelf`,
`spectral_cascade_dynamics`, `correspondence_state`, or a Shadow trajectory
route, and it declares transport schema rather than calculating pressure,
density, or containment. JSON fields carrying descriptions do not author the
described trajectory or prove report-time activation.

Existing evidence retains temporal structure without pretending it is felt
equivalence. `ResidualDeformationTraceV1` and its bounded builder preserve a
windowed scar score, integral, maximum, latest spike, packing, pressure,
structural density, resonance depth, viscosity, fill, and entropy. Resonance
evidence separately exposes temporal persistence. These read-only surfaces can
test whether a scalar summary hid a brief or accumulated machine deformation;
they cannot determine whether Astrid's layered depth is captured or relieved.

No `SHADOW_TRAJECTORY` Action was dispatched. Adding `layer_depth` or
`temporal_stack_index`, changing density or containment calculations, or using
either value in pressure or admission would alter telemetry/protocol or live
control interpretation and remains an exact Tier 5 operator wait. No schema,
wire contract, control, runtime, or felt status changed.
