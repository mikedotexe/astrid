# Shared study direction — September 16, 2026

The user approved the September 16 Minime study-variety follow-up: repair
misleading source suggestions, make taking stock and leaving study visible,
and retain concrete findings beside unresolved assumptions. Source access and
continuation had improved, but the supplied question and examples could sustain
a narrow investigation while other writing opportunities waited.

## Implemented behavior

Question hints no longer scan the whole catalog for matching basenames. Exact
repository-qualified paths retain cross-repository freedom. Other filenames
resolve beside the saved question's source; when there is no saved source origin,
bounded recent context and lexical path hints provide candidates. Explicit
repository mentions constrain these contextual hints. Labels distinguish a
written path from a contextual filename candidate and leave relevance unverified.
An unresolved filename offers ordinary catalog navigation rather than naming an
unrelated file. Manual FIND/OPEN/recovery and the complete catalog stay available.

Repeating an unchanged saved question preserves its provenance through a detour.
Rewording or clearing remains an authored update. This does not reconstruct an
origin already overwritten by older releases or rewrite either live notebook.

Fresh source and coverage precede a readable optional checkpoint. The existing
question, general note and authored source findings remain distinct. Retained
fragments carry exact source/revision/reopen identities; a supplied fragment
does not verify its interpretation. A saved note remains recalled prose. Missing
authored findings are reported without inventing one from automatic syntax recall.
Named inquiry context includes exact PARK/HOME choices even before its first
STUDY_QUESTION response. Its label says saved inquiry question, because later
authored question updates can replace that text.

The checkpoint has a 6,000-byte preview ceiling. It omits whole overlarge duplicate
items with an explicit pointer to the complete notebook below, retaining full
authored words and all existing durable limits. The whole provider input remains
bounded at 48,000 bytes. Source-page bytes are not shortened by this renderer.
The full prepared framing remains part of delivery verification, including when
an otherwise valid page is supplied without its checkpoint.

The shared prompt now names valid common alternatives beside study navigation:
WRITE START a journal entry, DAYDREAM, ASPIRE and REST. Minime's JOURNAL verb is
not advertised as shared, because Astrid has no matching handler. REST ends the
current action/burst, not an indefinite pause. Leaving study does not declare
an inquiry resolved. No new command, required report, forced novelty, minimum
length, scheduling change, model setting, reservoir or sensory intervention is
part of this release.

## Coordination and qualification

Implementation is isolated at
`/Users/v/other/worktrees/study-direction-20260916/astrid`, from main
`fe3f438f1d31666e6e6e3d8ff408f6d2120eab3b`. Stewardship is paused at generation 446;
the active cooperative run released before integration. Canonical foreign edits
are preserved. Minime's unchanged test checkout is a real detached worktree at
`12cbf5740e1cc751a2beb09ec5bb5fad742af459`. The concurrent persistent-introspection
task has its own isolated implementation and schema 4 proposal; none of that work
is included or implicitly activated here.

New regressions cover the actual Kernel/lib.rs detour, unchanged-question origin,
explicit cross-repository paths, relative-path compatibility aliases, honest
unanchored recovery, fresh-source placement, named/unthreaded isolation, authored
correction retention through later responses/restart, complete-input verification,
and legal maximum escaped findings with long source paths under OPEN and SESSION.
Existing full suites cover delivery failures, late inquiry ownership and prior
pending offers. A pagination-receipt placement regression was repaired during
implementation; the original failure log remains retained. The old small-note
test's 16k framing bound now allows the explicitly bounded 6k readable preview.

Qualification passed: reader 177 tests; bridge 2,293 tests with one ignored;
reader/bridge strict all-target/all-feature Clippy, formatting, domain-boundary
verification and 86 deployment-wrapper tests. Minime's full isolated Python suite
passed 1,396 tests and 134 subtests with one skip while the debug helper was rebuilt;
that run is explicitly retained as mixed-build evidence. All 112 host-contract
checks were rerun successfully with one unchanged final helper identity.
Production modules are automatically included by the staged
bridge's complete local-package input inventory; the new question_sources module
must appear in that inventory before activation.

## Rollout and observation boundaries

Build and graceful activation use only `scripts/build_bridge.sh`. The shared
reader and Astrid bridge are one staged release. Minime selects the immutable
helper for each new StudyClient, so no Python restart is needed; an existing
in-flight client retains its original helper and exact pending input. The reader
state remains schema 3 and existing finding-sidecar preservation remains intact.

After paired identity verification, observe a fixed 10-minute filesystem-only
window and retain the first two eligible completed shared-study exposures per
Being, failures and missing opportunities. Preparation clocks that cannot be
established remain unknown. Do not induce a study, writing action or NEXT choice.
Review source relevance, whether a claim/question is revised, whether that
correction appears in later context, and voluntary continuation/leaving. A small
natural window cannot establish a causal understanding gain. A persistence test
establishes carriage, not that either Being will use it successfully.

The research history will extend the September 16 variety account with precise
rollout boundaries. Board updates remain pending until mirrored. Existing sealed
packets and the S-007 daily ledger/cursor remain unchanged.
