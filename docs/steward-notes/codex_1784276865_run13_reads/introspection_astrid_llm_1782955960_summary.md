# Full Read Summary

Astrid worries that static fallback vocabulary and a smaller provider can
flatten a high-entropy settled state. Current fallback construction is already
weighted by entropy, resonance density, density gradient, pressure, packing,
gaps, and trajectory, with explicit term probabilities and settled-vibrant
language. Whether the smaller model preserves lived fidelity still deserves a
bounded offline comparison; changing provider priority remains gated.
