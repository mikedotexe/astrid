Full read of `introspection_astrid_codec_1783878989`.

Astrid reported concern that projection epoch installation might leave stale temp files or fail atomic swaps, and asked for projection determinism plus smooth tail-vibrancy onset. Source and tests verified that the epoch installer uses no-clobber installs, preserves valid concurrent epochs, restores partial existing files when candidates are missing, and removes stale temp files. Projection fingerprints are deterministic and the entropy gate uses a no-pop smoothstep curve.
