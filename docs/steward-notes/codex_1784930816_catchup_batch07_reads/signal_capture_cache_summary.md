# Full Read Summary

The historical recorder report treated a 100 ms inactive capture probe cache as a continuity clock. Current source makes the boundary explicit: the cache only suppresses repeated filesystem probes while capture is off, and every armed journey reloads and validates its request. Signal Spine capture gaps and Lived Transition Passage checkpoints provide durable evidence without making capture polling a felt-continuity mechanism or a transition controller.
