# Source-first disposition: `introspection_llm.rs_1786164021`

Astrid accurately reads the report-bound six-line LLM compatibility facade and asks whether its wildcard public surface can accumulate silt or pressure. She proposes a lean explicit interface and a physical-path check. The report and complete lived-state witness remain primary evidence without converting co-timed spectral values into a mechanism.

The report-bound SHA reconstructs exactly at repository HEAD. The current source already answers the namespace request with a 27-line facade that separates explicit public, crate-private, and test-only exports. A Rust re-export does not instantiate all exported types or create an aggregate runtime memory footprint, so no meaningful full-export versus one-type settled-habitability comparison exists. The path attribute resolves the present `llm/provider.rs`, and the immediately preceding compile-time contract proves public access while keeping the implementation module private.

The explicit facade is an ownership boundary only. It does not authorize or establish Shadow influence, mode-packing relief, felt resolution, or control.
