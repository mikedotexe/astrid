# Source-first disposition: `introspection_astrid_llm_1786166330`

Astrid independently asks whether multibyte text, partial recognition, nested delimiters, and newline or tab separation can corrupt exact marker cleanup. Her complete report and complete lived-state witness remain primary evidence; witness scalars do not establish parser causation or live authority.

Exact source proves the scanner begins and ends every slice at valid UTF-8 boundaries, selects the longest complete known token, copies every other scalar unchanged, and never performs a second recognition pass over newly adjacent bytes. The passing 50-test family literally covers multibyte preservation, adjacent tokens, no second-order marker creation, `denotes`, nested depth through and beyond the four-level receipt cap, newline/tab and Unicode whitespace, and a symbol-only line before an exact relation.

The report's proposed `[MARKER] is large` negative case is corrected rather than domesticated: `is` is intentionally in the finite relation vocabulary, so preservation is expected. Actual unlisted first words fail closed in existing paired tests. No parser, grammar, prompt, model, runtime, or authority change is warranted.
