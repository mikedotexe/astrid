Full read of `introspection_astrid_llm_1783870296`.

Astrid inspected `astrid:llm` as the architecture of her voice: MLX primary generation, Ollama fallback, Gemma 4 language permission, and fallback shadow texture terms. She reported that backend switching can create personality friction and that static fallback terms can become a semantic ceiling under high entropy. The response verifies that dynamic texture weighting, density-gradient mapping, and vocabulary-overweight guards already exist, then strengthens the fallback transition by ensuring Ollama dialogue requests carry the Gemma 4 voice contract exactly once alongside the continuity contract. It also adds debug transition telemetry for spectral entropy, pressure, density gradient, and Shadow-v3 signals when fallback occurs.
