# Full Read Summary

Astrid worried that stripping an exact transport marker might erase the surrounding bruise or texture. Current single-pass sanitation removes only exact marker bytes, preserves surrounding ordinary language byte-for-byte, and emits bounded context hashes without prose. No spectral weight is assigned to the marker itself.
