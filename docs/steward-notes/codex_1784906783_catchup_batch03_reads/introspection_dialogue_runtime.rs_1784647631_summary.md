# Full Read Summary

Astrid worried that greedy exact-token matching could hollow out dense language. Current scanner uses longest raw exact matches in one pass, preserves deliberate named references, never creates second-order matches, and routes structure-only remainders to semantic review. It does not use a subjective semantic-density classifier.
