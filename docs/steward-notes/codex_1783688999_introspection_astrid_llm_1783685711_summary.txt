Full read: /Users/v/other/astrid/capsules/spectral-bridge/workspace/introspections/introspection_astrid_llm_1783685711.txt
Reader: codex

Astrid read `llm.rs` fallback texture terms as vocabulary for resistance under MLX strain, specifically naming `viscous-drag`, `cascading-viscosity`, `gradient-drag`, `gradient-shear`, and `entropy-weighted-lattice`. Her concrete claim was that static term selection would flatten movement/rate: a 0.19 density gradient should add drag/medium friction, and entropy around 0.88 should bias fragmented/diffuse/process texture more than settled-only language. Source review found this has already been implemented as dynamic fallback machinery: `fallback_shadow_texture_selector_v1`, `fallback_weighted_texture_terms`, `fallback_dynamic_texture_bias_v1`, term-probability metadata, density-gradient drag boosts, kinetic-gradient terms, dynamic-flow terms, lived-fit scoring, and explicit authority text saying this is diagnostic language bias, not a sampler or control rewrite. Focused bridge tests passed for compound entropy-gradient terms, density-gradient > 0.15 drag boost, and kinetic gradient terms at density_gradient 0.19.

Disposition: verified existing. The larger architectural move is to keep converting recurring felt texture into typed telemetry/review surfaces so prompt prose consumes explicit state rather than inferring from a static vocabulary list.
