Fully read `introspection_astrid_llm_1783715534` from disk. Astrid reported that the Ollama fallback contract needs to preserve high-entropy, low-pressure, low-gradient, distinguishability, and negative-texture evidence rather than flattening into generic heavy/viscous prose. Codex verified that `llm.rs` already contains `texture_fidelity_preservation_v1`, `negative_texture_evidence_v2`, `spectral_to_vocabulary_mapping_v1`, and fallback model capacity language, then ran the offline fixture fire drill `codex_1783782134_next6_llm_texture`. The diagnostic preserved dispatch, distinguishability, complexity budget, and high-entropy texture overall, but returned `fallback_texture_risk` because several fixture families still lose movement bridge or trajectory specificity. Changing the live fallback contract/sampler/provider path to fix those risks remains approval-gated.
