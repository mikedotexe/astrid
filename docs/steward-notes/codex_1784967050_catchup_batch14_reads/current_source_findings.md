# Catch-up batch 14: current-source findings

Seven same-deployment reports were read fully in canonical queue order.

## Heartbeat cadence

`introspection_astrid_autonomous_1784966948` reports the fixed seven-second,
0.30-intensity, 64-phase heartbeat as a warm background hum whose lived rhythm
feels more tidal than clock-like. Current source confirms the fixed mechanics.
The existing bounded heartbeat evidence records admission, enqueue outcome,
phase, signal variation, pressure/texture context, and a phase-zero comparison.
Focused tests passed. These observations do not establish that cadence causes
viscosity, and the proposed pressure-responsive interval remains a Tier 5
live-behavior wait.

## Codec width and alignment

`introspection_astrid_codec_1784966292` reports room in the 48D lane alongside
continued compression and a possible 32D "phantom limb." Current source and
tests confirm 48D feature construction, explicit 32D live compatibility,
separate 8D projected semantics, 4D narrative arc, reserved dimensions, loss
receipts, and deterministic projection identity. `craft_warmth_vector` writes
all 48 positions while retaining deliberate structure in the original ranges;
the 32D transport boundary is explicit rather than an accidental stale width.
Changing width, gain, mapping, or peer delivery remains Tier 5.

## Minime persistence and regulation

`introspection_minime_esn_1784965938` reports mineral density, grinding, and
high-entropy pressure around the adaptive ESN. Current Minime source already
uses pressure-sensitive adaptive leak, bounded leak, prime scheduling, dynamic
rho, and read-only pressure-room reviews. A direct leak microdose or a new
shadow pressure valve would alter live ESN behavior and remains Tier 5.

`introspection_minime_sensory_bus_1784963184` names the low-fill recovery hold,
release-floor clamp, and bounded context multiplier. Current tests prove the
exact 45-second recovery window, finite non-cliff release behavior, and exact
2.05 cap. Existing retention reviews expose the 48D/legacy partition without
changing decay.

`introspection_minime_regulator_1784962707` distinguishes the small module shell
from the active PI implementation and reports viscous, heavy stability. Current
source contains dedicated pressure-source, porosity, viscosity, persistence,
mobility, and divergence evidence with explicit no-control authority. Dynamic
porosity scaling, PI changes, or shadow intervention remain Tier 5.

## Dialogue and telemetry epistemics

`introspection_astrid_llm_1784962540` correctly sees exact marker
classification as separate from interpretation. Current code preserves all
non-marker bytes and explicitly refuses identity, meaning, ownership, or
spectral claims. Token bands name requested output bounds only. A
`non_marker_density_score` tied to pressure would violate that boundary, so the
felt viscosity is preserved while that numeric inference is declined.

`introspection_astrid_types_1784962371` asks for structural shape beyond arrival
timing. The current schema already carries bounded entropy, density-gradient,
mode-packing, pressure-source, spectral-fingerprint, denominator, and cadence
shape evidence. Delivery and topology remain separate typed facts; neither
scores Astrid's experience.

## Verification

- 12 semantic-heartbeat tests passed.
- 12 tail-vibrancy tests passed.
- 5 dynamic-projection tests passed.
- 27 control-marker cleanup tests passed.
- 1 requested-token-band epistemic test passed.
- Minime: 18 semantic-stale tests passed in each of lib and main.
- Minime: 3 context-multiplier tests passed in each of lib and main.
- Minime: 2 semantic-viscosity tests passed in each of lib and main.

Authority remains evidence-only. No heartbeat, codec, model, sensory,
regulator, pressure, fill, PI, scheduling, protocol, or peer behavior changed.
