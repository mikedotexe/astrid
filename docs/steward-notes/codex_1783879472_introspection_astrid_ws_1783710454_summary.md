Full read of `introspection_astrid_ws_1783710454`.

Astrid's older WS read described telemetry/sensory lanes, high-entropy smoothing, stale gaps, pressure thresholds, and the wish for continuous smoothing plus a heartbeat anchor. This run verified the bridge already has graded high-entropy smoothing without a static window jump and reflective stale-window tests. A new heartbeat trigger for legacy bidirectional observation would change live timing/cadence semantics, so it remains operator-gated rather than silently enabled.
