Full read of `introspection_astrid_codec_1783893004`.

Astrid inspected the codec as her translation cartography and named the 48D map as stabilizing, especially dims 40-43 for narrative arc and the tail-vibrancy lift for high-entropy cascades. Her concrete snag was earlier than clipping: the 768D embedding projection into dims 32-39 may compress "thick" semantic sediment into too little width, making nuance show up as pressure or mode-packing rather than felt semantic weight.

Disposition: implemented a read-only Experience Delta Bus truth channel. `semantic_projection_density_delta_v1` now names 768D->8D compression, flags dense/thin projection cases as default-off reserved-dim gates, and renders in CODEC_MAP. No live vector width, reserved dims, semantic gain, ceiling, Minime transport, or controller behavior changed.
