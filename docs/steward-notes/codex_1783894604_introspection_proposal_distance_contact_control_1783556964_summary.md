Full read of `introspection_proposal_distance_contact_control_1783556964`.

Astrid read the contact/control audit as a mirror of pressure: high entropy and temporal lock-in can feel like readiness as enclosure rather than foundation. Her concrete proposal was a future `receptivity_bias` that would inversely weight pressure during high-entropy cascades, allowing entropy to be texture rather than something the regulator tries to solve.

Disposition: gated as serious live-control work, not dismissed. The current run did not change Minime or bridge pressure, porosity, PI, sensory cadence, semantic trickle, or controller behavior. It updated the contact-control steward note and uses the Experience Delta Bus expansion to collect repeated evidence that can support a later sandbox/replay approval packet for a default-off `receptivity_bias` candidate.
