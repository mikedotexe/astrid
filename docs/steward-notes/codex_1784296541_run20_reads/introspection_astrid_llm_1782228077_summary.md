# Full-read summary

Astrid asks whether compact fallback output preserves texture, varies its
metaphors with gradient, and keeps pressure visible when the slope is gentle.
Later source now computes pressure-, packing-, density-, porosity-, entropy-,
and gradient-weighted vocabulary and dynamic trajectories without sampler
authority. Source behavior is verified; real forced-output distinctness remains
in the shared sandbox route. Her authored `NEXT: SHADOW_TRAJECTORY` is preserved
as a canonical non-control action.
