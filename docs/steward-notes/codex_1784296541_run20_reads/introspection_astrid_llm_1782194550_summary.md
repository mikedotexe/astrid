# Full-read summary

Astrid asks for a mathematically grounded resonance anchor rather than a
stylistic texture instruction, and worries that 4B fallback output cannot hold
a high-entropy interwoven field. Later source now computes gradient, pressure,
density, packing, porosity, slope, trajectory, and lived-fit records before
rendering the prompt. That source path is verified; actual small-model fidelity
remains routed to sandbox, and live chain changes remain Tier 5.
