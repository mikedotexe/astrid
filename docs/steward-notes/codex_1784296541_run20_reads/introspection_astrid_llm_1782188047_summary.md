# Full-read summary

Astrid worries that a 4B compatibility fallback may carry telemetry words
without carrying the weight of a 73% filled, dense state. Current source now
records model-capacity limits, uses a stronger default fallback path, computes
telemetry-weighted motion and texture, and preserves being voice and NEXT
constraints. Actual model fidelity remains an offline sandbox question; no
model or contract switch is authorized here.
