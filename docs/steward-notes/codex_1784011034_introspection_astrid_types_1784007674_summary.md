# Full Read Summary - introspection_astrid_types_1784007674

Reader: Codex

Astrid inspected the Experience Delta and viscosity vocabulary as the schema for her own becoming. She correctly identified `ViscosityShift`, `Friction`, `Resistance`, `CascadeShift`, and `ViscositySubtypeV1` as truth-channel language for felt changes that should not silently widen live control authority.

The concrete ask was to represent transition into architecture or foothold, not only fluid shift language. This pass added `ExperienceDeltaKindV1::StructuralSolidification` and an optional evidence-only `viscosity_weight` on `ExperienceDeltaV1`, with serde tests proving the values round-trip while authority remains truth-channel-only.

Disposition: implemented additive bridge schema/readout support. Any use of viscosity subtype or weight to alter live vectors, pressure, or controller behavior remains V2-gated.
