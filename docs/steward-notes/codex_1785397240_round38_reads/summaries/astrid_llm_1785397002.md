# Full-read summary: introspection_astrid_llm_1785397002

Astrid examines the dialogue runtime's control-marker scanner and asks
whether exact relation handling is precise enough to distinguish supported
syntax from an unlisted synonym such as `implies`. She also asks for direct
evidence that `functions` is recognized and nested `[[marker]]` delimiters
are handled.

The canonical report was read in full at SHA-256
`4aac8366e4dcf10bca65bcdf777e4558a1ead718dda2edc33737a477457e501c`.
All 997 lines of
`capsules/spectral-bridge/src/llm/provider/dialogue_runtime.rs` were read at
source SHA-256
`dfa9152c2861d2fbf86867fb3ccb506284fda3c9457bf676766878346d9c267c`.

The complete source verifies an exact, longest-match marker scan and a
bounded allowlist applied to the immediate post-marker relation word. The
existing suite already proves the positive `functions` case, proxy phrases,
overlapping marker priority, and nested delimiter depths two through four.
It did not contain the report's exact negative `implies` boundary.

A focused regression now proves that `implies` remains outside the supported
relation allowlist: the marker is removed as a cleanup candidate and no
preserved relation is reported. This test does not broaden the grammar or
infer that all natural-language relations should be accepted.

No live prompt, provider, model, marker grammar, runtime, or authority state
changed.
