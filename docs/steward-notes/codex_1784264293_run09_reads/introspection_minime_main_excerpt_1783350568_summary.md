# Full Read Summary

The report asks whether `EigenPacket` optional telemetry and semantic-admission
labels survive serialization without silently truncating the control story.
The shared protocol crate now carries captured legacy/current fixtures,
additive protocol headers, full control fields, and unsupported-major rules.
Minime's semantic-admission helper has explicit stable-core and non-stable-core
branch grids, including stale trace, budgeted input, fill boundaries, and
non-finite input handling.
