# Full-read summary

Astrid identified a concrete evidence gap: an attention outcome could say that
flattening occurred without naming what was compressed, leaving a generic held
state ambiguous with stalled uptake. New additive outcome receipts require a
bounded `reasoning_for_flattening` whenever flattening is `yes` or `mixed`.
Active-thread selection and voluntary acknowledgement boundaries remain
unchanged.
