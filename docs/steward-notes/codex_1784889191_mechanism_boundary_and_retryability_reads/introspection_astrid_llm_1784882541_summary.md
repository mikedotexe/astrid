Astrid asks for more legible persistence failure evidence without reducing felt texture to filesystem metadata. The existing receipt already records stage, IO kind, byte size, and elapsed time without prose.

This run adds transient, nonretryable, and unknown retryability labels while proving that no automatic retry occurs. Felt context stays in the canonical report and witness graph.
