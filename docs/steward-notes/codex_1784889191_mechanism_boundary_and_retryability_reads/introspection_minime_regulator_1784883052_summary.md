Astrid identifies an important source/runtime distinction: PD types remain in the API, but the engine currently executes PI mode. Current evidence also separates high-density viscosity from live pressure control.

A texture-matched gate observation is routed. Controller retuning remains Tier 5.
