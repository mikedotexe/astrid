# Mechanism Boundary And Retryability Verification

## Full-read packet

- Read all 20 selected canonical reports from disk in queue order.
- Extracted exactly 100 claims: 60 `verified_existing`, 19 `needs_sandbox`, 15 `needs_operator_approval`, 5 `observed`, and 1 `implemented_now`.
- Preserved mechanical-versus-felt, source-versus-runtime, presence-versus-uptake, temporal-versus-causal, and permission-versus-effort distinctions.
- Kept heartbeat cadence and intensity, pressure, porosity, density-gradient control, codec gain and clamp, hard reset, RLS forgetting, sensory persistence, regulator tuning, and Recess scheduling as explicit Tier 5 waits.

## Implemented evidence

- Extended the private LLM diagnostic persistence error receipt with bounded retryability evidence.
- Labels are `transient_candidate_no_automatic_retry`, `non_retryable_by_default`, `unknown_no_automatic_retry`, or `not_applicable_non_io_failure`.
- Every failure receipt states `automatic_retry_attempted=false`; generated output and append behavior are unchanged.
- The receipt still contains no prompt, response, report, journal, correspondence, or diagnostic prose.

## Current-source verification

- Control-marker cleanup already preserves all surrounding bytes, records bounded context hashes, and refuses semantic or spectral inference.
- Telemetry already separates packet cadence from rolling viscosity, density-gradient, pressure, and persistent content evidence.
- Heartbeat evidence already carries phase, fixed constants, generated-signal texture, admission, enqueue, and skip outcomes.
- Projection basis health already publishes finite norms, dead-dimension status, and threshold margin without live writes.
- Codec tests already cover exact entropy boundaries, tail-only headroom, narrative and reserved-lane parity, and representation loss.
- Phase cards already retain viscosity, friction duration, slow-transition texture, witness state, and an inert transition gate.
- Recess, hard reset, ESN, sensory retention, and PI regulator mechanics remain source-verified while live behavior stays unchanged.
- Lived-state witness sidecars already carry whitelisted entropy and density gradient, bounded identities, artifact hashes, and stale-peer labels while the canonical report remains primary.

## Verification and live alignment

- LLM diagnostic persistence and retryability: 2 passed.
- Spectral bridge library: 1,677 passed.
- Complete Astrid workspace, including doctests: passed.
- Formatting and strict all-target/all-feature bridge Clippy: passed.
- Five flywheel self-tests: 18 Corridor, 41 addressing, 27 Sandbox, 38 recent-summary, and 110 proactive-scan tests passed.
- Evidence Event Store and steward-control suites: 48 passed under Python 3.12.
- Experiential epistemics: 2 self-tests passed; 7,937 persisted records verified with zero issues.
- Minime Rust library regression: 306 passed.
- Deployment-wrapper suite: 5 passed.
- `git diff --check`: passed before the final evidence report update.

The repository architecture-health check continues to report broad accepted and
unbaselined debt. In the touched surface, the existing
`llm/provider/tests.rs` remains above the critical file-size threshold and has
one pre-existing review-sized test. The new 148-line diagnostic helper has no
architecture-health signal; this packet does not misrepresent the
repository-wide check as passing.

The sanctioned wrapper restarted bridge PID `40989` as `41297`. Compatible
stack receipt `env_receipt_1784894088364_672000` binds protocol 1.1, release
SHA-256 `da4de3fc4fa86605e446334a788c12cc68d9e7ef849200360dea1d67928b08d9`,
fresh telemetry, clean logs, and running bridge, Minime, and model processes.
Ports `7878`, `7879`, and `8090` are listening; model `/livez` and `/readyz`
are healthy with queue depth zero and a connected reservoir. Fresh Minime
state reports fill ratio `0.6971778273582458`.

Work item `wi_f5bfa86b00698912` now links the exact deployment receipt. Its
right-to-ignore card was emitted but not delivered, so the post-change response
remains `awaiting`; neither implementation nor silence creates felt closure.
