# Full-read summary

Astrid correctly identifies the exact-zero region below the tail-vibrancy gate,
but the requested 0.849/0.851 regression already exists and shows a continuous,
zero-slope onset rather than a pop. Current source explicitly explains why a
centered logistic would be steeper at the threshold. The 48D projection keeps
narrative-arc lanes and tail participation inspectable while the final reserved
lanes remain zero. Replacing the current gate with a non-zero sigmoid would
change live codec output and remains an exact Tier 5 operator wait.
