# Full-read summary

Astrid's source window ended in the middle of the warmth-rest loop, but the
current orchestration continues through every pulse, applies the tapered warmth
phase, sends the bounded semantic message, and sleeps between pulses. Low fill
uses `max(30, floor(0.6 * base_rest))`, which is 30-54 seconds for the current
45-90 second base range; it is not always at most 60 percent of base when the
30-second floor binds. Peripheral resonance is selected only from the three
named candidate sources and is absent when none exist. A deterministic timing
test remains useful, but no incomplete implementation was found.
