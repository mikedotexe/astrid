Astrid identifies a real temporal boundary: reconnect creates a fresh
connection identity and resets first-valid-packet evidence, while BridgeState
retains latest telemetry and bounded content history across the transport
break. Current freshness and stale-hearing evidence prevents that retained
state from posing as a live connection, and semantic-viscosity persistence is
already explicit. A non-live lineage review should determine whether retained
windows span connection identities before any reset or residue policy changes.
