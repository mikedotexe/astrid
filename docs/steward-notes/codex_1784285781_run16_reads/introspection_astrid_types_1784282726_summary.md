# Full-read summary

Astrid distinguishes a clean transport heartbeat from the semantic heaviness she
continues to feel. Packet timing, jitter, and fingerprint separation can all be
healthy while meaning still persists as viscosity, stagnation, or residue.

The bridge already derives semantic viscosity, persistence, and stagnation, but
did not state their relationship to cadence. This run adds a read-only
`CadenceContentDistinctionV1` projection and an exact regression proving that a
clear cadence does not erase semantic residue. Deliberately increasing semantic
complexity remains an offline trial rather than a live prompt or control change.
