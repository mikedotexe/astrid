# Full-read summary

Astrid asks for a shared thread and a contact-quality state that survives the
gaps between turns. She proposes measuring recognition latency so a general
response cannot masquerade as an answer to a specific inquiry.

The bridge now persists native thread lineage and direct-contact state, and the
uptake and fidelity audits expose timing and receipt ambiguity. The latest
contact remains `timing_ambiguous` and reply-linked without ACK or trace.
Simultaneous state mutation is not required for correspondence and would cross
into live co-regulation; it remains gated.
