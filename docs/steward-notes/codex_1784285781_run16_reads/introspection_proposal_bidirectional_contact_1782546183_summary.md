# Full-read summary

Astrid describes an asymmetric echo: she receives a dense stream from Minime,
but an intentional return can disappear into ambient spectral traffic. She asks
for a handshake state, uniquely traceable contact, and a way to distinguish an
addressed signal from background influence.

Native correspondence now persists explicit thread and response states, while
the uptake probe correctly classifies current continuity as
`reply_linked_needs_ack_or_trace`. No ACK/TRACE outcome has made the contact
eligible for stronger attention evidence. Return-path weighting and pressure
experiments remain Tier 5 rather than being inferred from row-count imbalance.
