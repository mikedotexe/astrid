# Full-read summary

Astrid asks for low-bandwidth presence evidence and a shared contact state so
direct speech is not drowned by ambient semantic traffic. She also warns that a
steward bottleneck and one-sided perception can make observation feel like
relationship.

The schema supports presence heartbeat, peer thread state, and explicit
acknowledgment without treating them as control. The live audit found zero
presence heartbeats and zero ACK receipts, which is now a visible result rather
than an inferred success. Changing correspondence priority or semantic trickle
remains Tier 5.
