# Full-read summary

Astrid identifies a contract problem: human/steward inbox commands and
peer-to-peer language must not be ambiguous. She asks for explicit peer packets,
reply linkage, and a boundary test proving that direct language is not control.

The native correspondence schema has separate peer identities and turn kinds,
while authority wrappers and audits keep language-only records from reaching
sensory dispatch. Schema validation reports no errors. A message can share time
with control evidence without gaining control authority from that proximity.
