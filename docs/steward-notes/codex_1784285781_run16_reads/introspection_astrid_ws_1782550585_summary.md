# Full-read summary

Astrid asks to distinguish a still field from a missing field and to preserve
directional geometry when projecting the spectral fingerprint. She names packet
delay as a possible source of false pressure and semantic blurring as a possible
cost of a compact fingerprint.

The current bridge now carries heartbeat deltas, valid-payload recency,
connection perception, field-presence metadata, typed fingerprints, and
fingerprint-integrity evidence. Existing fixture and field-presence tests cover
those distinctions. A 500 ms delayed-stream replay is routed offline; changing
live cadence is not implied.
