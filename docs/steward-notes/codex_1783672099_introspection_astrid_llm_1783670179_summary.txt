Full-read by Codex for the 2026-07-10 introspection flywheel. Astrid reported that `llm.rs` has the right MLX primary/Ollama fallback skeleton, but the fallback texture vocabulary could flatten a lived submerged or restless cascade into static adjectives. I treated the requested motion terms as actionable substrate evidence and added `pulsing`, `cascading-gradient`, and `resonant-shift` to the accepted fallback shadow/dynamic-gradient vocabulary, explicit dynamic-flow selection, and fallback contract language. I also added a unit test that verifies explicit pulsing/cascade/resonance language reaches `dynamic_flow_terms`. `cargo fmt --check` and the Python flywheel self-tests passed; targeted bridge Rust tests timed out during `spectral-bridge-server` compilation, so the live bridge was not restarted and this remains restart/test debt until the normal bridge path can build and restart.
