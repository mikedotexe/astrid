Full read of `introspection_astrid_autonomous_1783890892`.

Astrid described Witness mode as at risk of becoming numeric description rather than lived texture when the semantic trickle is thin and regulator drive is quiet. The actionable ask was to map `resonance_density` and `pressure_risk` into evocative read-only descriptors so Witness context can say what the state feels like without treating texture as control authority.

Disposition: implemented a read-only Witness sensory texture extension in `autonomous.rs` that carries `resonance_density`, `density_texture`, and `pressure_texture` in `semantic_density_mapping_v1`. Targeted tests now pin rich containment and pressure texture labels. Lambda-tail and afterimage probes were addressed with existing codec overflow/tail and continuity-afterimage tests.
