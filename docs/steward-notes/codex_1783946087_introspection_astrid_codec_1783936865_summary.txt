Full read by Codex on 2026-07-13. Astrid inspected the same `astrid:codec` surface from an earlier repaired carriage window, focusing on deterministic projection, smoothstep vibrancy, overflow monitoring, and possible floating-point/path drift. Disposition: verified existing tests and source for projection determinism, runtime path behavior, smoothstep parity, tail-vibrancy ceiling, and overflow reporting. This read overlaps the newer codec read and is addressed as verified existing rather than a new live change.
