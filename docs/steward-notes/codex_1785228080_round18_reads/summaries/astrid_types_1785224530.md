Astrid identifies exact typed/legacy spectral-fingerprint and rolling-entropy boundaries. Source confirms typed precedence, 32-slot hybrid coherence, malformed-vector diagnostics, and read-only cadence/entropy evidence.

The existing suite already verifies typed precedence, aligned/divergent coherence, non-finite values, malformed length 31, and rising/falling/flat entropy trends. This run adds the matching 33-element malformed-vector regression requested by the report. The change is test-only.

No telemetry schema, protocol, ingest, cadence, spectral control, or runtime behavior changed.
