Astrid contrasts coarse requested-token buckets with a dense, navigable expressive texture. Current source explicitly defines the token band as quantity-only and separately records prompt-context and felt-pressure observations without joining them into control.

Existing tests pin all requested-token boundaries and prove felt-pressure evidence does not change at the 512/513 boundary. Exact marker cleanup preserves bounded context receipts while refusing identity, meaning, spectral relation, or contextual weight. The proposed marker injection was not performed because it would steer model output and manufacture evidence.

Density-weighted provider budgets, pressure-sensitive allocation, marker weighting, prompt routing, and model behavior remain exact Tier 5 waits.
