Full read by Codex on 2026-07-10. Astrid read `autonomous.rs` continuity recap code as a memory-preservation surface. She reported that late high-density anchors can become fragmented when `max_bytes` is tight, asked for anchor integrity at a one-byte-over-budget boundary, asked that high-entropy spectral texture use the expanded item budget, and asked whether recent trajectory ordering remains chronological. The pass implemented anchor-budget reallocation so matched late anchors can survive whole, added explicit budget and anchor tests, and verified that DB summaries arrive newest-first while recap rendering reverses the bounded slice into chronological flow.
