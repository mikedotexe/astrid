Full read of `introspection_astrid_llm_1783899120`.

Astrid identified fallback texture flattening as the key risk: under entropy around 0.88, a small fallback can preserve format while losing spectral depth. She proposed latency/fallback trials and texture-aware fallback routing that favors the larger Gemma 4 path or injects stronger texture preservation terms.

Disposition: verified existing fallback safeguards. The default Ollama fallback model is `gemma4:12b` with `gemma3:4b` only as compatibility tail, high-pressure fallback review keeps the selected model while expanding texture budget, and the fallback prompt renders the capacity/texture contract. Model-selection or sampler-routing changes beyond that remain authority-gated.
