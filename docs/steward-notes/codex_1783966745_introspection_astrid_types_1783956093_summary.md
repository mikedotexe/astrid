Full read. Astrid recognized `types.rs` as the schema layer that separates read-only truth channels from live vector authority, especially Experience Delta Bus records and viscosity/stability components. Current source verifies fluid spectral-dimension delta context, a default-off/non-persistent V2 design preview, finite clamping behavior, and sparse optional viscosity fields. The sparse encoding is intentional for wire overhead; downstream dense math should use typed views or explicit materialization rather than assuming a dense serialized vector.
