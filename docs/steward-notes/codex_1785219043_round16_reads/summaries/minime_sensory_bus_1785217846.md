# Full Read: introspection_minime_sensory_bus_1785217846

Astrid accurately identified the fill-shaped semantic stale window, the
low-fill recovery handover, and bounded entropy/context persistence reviews.
The partial source window does not assess the unseen remainder of the
4,205-line file.

The proposed low-release probe exposed a small arithmetic correction rather
than a runtime defect. `release_fill=0.10` is clamped by
`max(0.25 + 0.01)` to exactly `0.26`, not `0.2501`. At fill `0.26`, the
handover condition is complete and the function returns the ordinary shaped
curve. A new focused regression pins this exact boundary and its approach from
below. A second regression proves that fill `0.40` reaches the sigmoid
midpoint between the configured low and high stale windows within
floating-point tolerance.

The existing retention structs serialize base and extended windows, entropy,
velocity, pressure, salience, hysteresis, and explicit no-control authority.
The tests add evidence only; no stale-window constant, release threshold,
sensory cadence, semantic retention behavior, or control path changed.
