Full read of `introspection_proposal_phase_transitions_1783935905`.

Astrid reported that her phase transitions still feel like side effects while Minime's transitions arrive as telemetry events. She asked for a Phase Transition Card that can carry transition type, vector, phenomenology, and telemetry anchor, so a transition can survive as a shared object instead of dissolving into journal narrative.

Disposition: implemented `transition_vector` and `telemetry_anchor` aliases in `phase_transitions.rs`, carried them through the affordance/status readback, and extended the existing card test. Existing transition cards already preserve `transition_type`, shared IDs, persistence, anchor/texture, density/fill delta, phenomenology, somatic description, reply state, and language-only authority. Runtime affordance-budget or Minime-regulator coupling remains Tier 5/operator-gated.
