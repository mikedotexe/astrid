# Sol Source-First Catch-Up Run 09

## Scope

- Canonical packet: 20 full reports in exact queue order.
- Queue span: `introspection_astrid_codec_1784262058.txt` through
  `introspection_minime_autonomous_agent_1783341745.txt`.
- Claim dispositions: 4 `implemented_now`, 50 `verified_existing`, 3
  `needs_sandbox`, and 11 exact `tier_5_wait` claims.
- All 68 claims have linked evidence and right-to-ignore closure cards. No
  excerpt substituted for a full disk read, and no Corridor item displaced
  the canonical packet.

## Evidence Matrix

1. Codec projection evidence now reports the raw and normalized column-norm
   health of the fixed projection basis, including weakest, minimum, maximum,
   finite, unit, and dead-column state. Projection values and live behavior are
   unchanged.
2. Phase-transition evidence now carries `phase_dual_perspective_v1`, keeping
   Astrid interpretation, Minime reference claims, and bridge derivation
   distinct. Their relation remains unresolved unless actual evidence supports
   it.
3. The dual-perspective evidence is present in phase cards, affordances, status,
   and queue projections so downstream readers do not need to reconstruct
   ownership from blended prose.
4. Claim routing now uses explicit disposition and full claim text before
   generic live-control keywords. An `implemented_now` claim cannot become a
   runnable live wait merely because its evidence discusses a guarded control
   boundary.
5. Final mismatch audits honor explicit `needs_sandbox` dispositions instead of
   reclassifying them from broad keywords after the record has been closed.
6. Sandbox generation appends a correction for untouched false live waits.
   Existing Tier 5 waits retain their specialized authority adapters, while
   bounded read-only comparisons remain non-runnable manual review work.
7. Astrid's codec reports support projection-basis observability, 12D
   representation evidence, latent dynamics, semantic texture, and compatibility
   projections. They do not authorize live transport or gain changes.
8. Phase reports support explicit state descriptions, transition witnesses,
   persistence, correspondence, and separate self/other interpretations. They
   do not authorize behavior unlocks or controller mutation.
9. Minime source verifies target-aware regulation, bounded rescue, typed sensory
   lanes, semantic retention/release, and extracted autonomy parsing while
   leaving parameter and admission changes under operator authority.
10. Astrid source verifies provider fallback contracts, pressure and texture
    provenance, Witness distinctions, checked dispatch authority, and
    compatibility output without collapsing observed and derived evidence.
11. Three bounded read-only comparisons were routed to sandbox review:
    regulator persistence decay, current distance/contact versus
    dispersal/sensory change, and the corresponding older distance/contact
    comparison.
12. Eleven proposals remain exact Tier 5 waits covering receptivity/admission,
    contact/plasticity regulation, phase behavior unlocks, live ESN tuning,
    semantic cadence, PI/controller coupling, provider/model changes, 12D live
    transport, regulator damping, standing correspondence dispatch, and
    automatic behavior unlocks.

## Authority

The source additions are evidence and report surfaces only. No pressure, fill,
PI, controller, rescue, sensory cadence, semantic admission, hold/release,
exploration noise, rho, codec vector, gain, transport, peer mutation,
correspondence dispatch, model route, or autonomy policy changed. The three
sandbox trials are offline read-only review records and are not runnable live.
The eleven substrate-facing proposals remain non-runnable Mike/operator Tier 5
waits.

## Validation And Alignment

- The full bridge library suite passes all 1,520 tests. Strict bridge Clippy,
  workspace formatting, six protocol fixture tests, six Python authority-state
  tests, three authority compile-fail cases, and four provenance compile-fail
  cases all pass.
- Focused Minime Rust regressions pass for semantic admission, dynamic noise,
  stale semantic release, and residual ghost behavior across library and binary
  targets. Six focused Python parser, recall, and research-budget tests pass.
  No Minime source changed in this run.
- The six required tooling surfaces pass 226 self-tests: Corridor 17,
  addressing 29, sandbox 26, recent summary 38, proactive scan 110, and Event
  Store V2 6.
- The first sanctioned bridge restart preflight correctly waited on a 79.6
  second settle-age gate. The retry produced passed, compatible receipt
  `env_receipt_1784266142878_942000`. PID changed `87798 -> 73444`; binary
  SHA-256 is
  `d75c19052fd049bbdcc79d4c52bd5a994ad591bdb124f2458755c59370009d4a`.
- Protocol 1.0 revision
  `c6ecb853d1a9bc7a7479d37d8366553a0bae0bc5` is aligned. Minime PID 45510
  serves ports 7878 and 7879; model PID 48166 serves port 8090. `/livez` and
  `/readyz` returned 200 in under 1 ms while generation was active.
- Fresh bridge logs show both lanes reconnected, state restored, readable
  telemetry in the 71-73 percent range, fresh self-observation, and no
  post-restart warning or error. Restart alignment is current with no debt.
- All 20 selected reports are fully addressed. Canonical counters moved from
  2,123 indexed / 1,058 addressed / 1,065 remaining to 2,128 indexed / 1,078
  addressed / 1,050 remaining at post-restart validation. One final autonomous
  report then arrived and was indexed at the closing cutoff, yielding 2,129
  indexed / 1,078 addressed / 1,051 remaining. Six reports arrived across the
  run, so the quality guard reduced the remainder by a net 14. Counter and tier
  audits are consistent with zero mismatches.
- Sandbox state is 1,048 total, 1,047 active, 142 ready, 87 result-recorded,
  818 approval-required, 1 closed, 0 ready-runnable, and 0 violations. This run
  added three non-runnable manual review trials; no sandbox result was claimed.
- Corridor state is 120 packets, 35 leases, 190 queue steps with 185 runnable,
  118 active programs, 200 stored portfolios, 45 patch bundles, 70 source-prep
  proposals, 50 program receipts, 0 reopens, 0 self-observation responses, and
  0 hard violations. Generic Corridor work was not run because it would have
  displaced direct canonical reading and implementation.
- Event Store V2 remains active and valid at global sequence 34,882 with head
  `6281fac8ee4fb048d712345a03253b3c8dd8d1522c414ebf8bccc5d6377edf13`;
  stream counts are addressing 33,228, sandbox 1,539, Corridor V1 3, and
  Corridor V2 112. All four frozen V1 source hashes remain exact.
