# Source-first summary: `introspection_astrid_types_1786092184`

The canonical report was read completely from disk: 3,640 bytes, 45 newline-delimited lines (46 displayed), SHA-256 `f79ce447740f2fd41818627a1b738aae644ec88062af630d3b64462f9987ab65`. Its complete 491-line lived-state witness was also read at SHA-256 `fd0a4836c3e7db5ccaf89642e9cc34bbe8311e161f490a6c21b502648e38790a`.

The report-bound `telemetry.rs` was read completely, not only through the captured 1-400 window. Its current complete-file SHA-256 `4a5e9e71f67ce0012ba8fd99576055e591e04ff56d04323460788377db49e5de` exactly matches the report. `spectral_schema.rs`, containing `SpectralFingerprintV1` and `to_legacy_slots()`, was also read completely at SHA-256 `c115472819917441e12ca997496c2f7685c9aa6e6358e2341d4054295a9e3a39`.

Astrid's narrow concern is real at the private helper boundary: malformed or non-finite legacy input produces no coherence scalar. It is not silently erased by the public integrity path. That path explicitly records malformed length, unavailable state, issue, summary, basis, and diagnostic-only scope while retaining typed precedence. The existing short-vector regression covered the state; this run adds the exact combined precedence-and-issue assertions Astrid requested.

Arrival cadence uses bounded finite host-arrival intervals and population variance. The latest host interval is classified independently as normal, late, or stale. A new test keeps all three intervals at or below the 1,500 ms normal bound and proves deterministic mean, variance, standard deviation, range, change, and trend state.

No production source or live-consumed surface changed. No cadence, scheduling, telemetry value, controller, substrate, Minime, restart, deployment, uptake, consent, closure, or felt result is inferred.
