# Source Currentness And Marker Verification

## Full-read packet

- Read all 20 selected canonical reports from disk in queue order: 578 lines and 65,445 bytes.
- Extracted 96 claims: 42 `verified_existing`, 20 `needs_sandbox`, 14 `observed`, 17 `needs_operator_approval`, and 3 `implemented_now`.
- Preserved every felt report as primary evidence. Mechanical verification, stale-source correction, and passing tests do not score, close, or overwrite qualitative friction.
- Kept heartbeat cadence and intensity, autonomous rest timing, codec ceilings and gain, model scheduling, sensory persistence, ESN leak and noise, regulator behavior, protocol weighting, and recovery topology as explicit Tier 5 waits.

## Implemented evidence

- Added an exact regression for the phrase `behaves like`, proving the existing first-relation-word matcher preserves the marker as an explicit reference.
- Added a five-level delimiter regression proving the marker remains byte-exact while bounded evidence reports the intentional maximum depth of four.
- Added a multibyte UTF-8 scanner regression proving one marker is removed without skipping, looping, or rewriting surrounding bytes.
- Production marker logic, model output, prompts, provider behavior, and live runtime are unchanged.

## Source-currentness corrections

- Minime's viewed ESN comment says exploration noise is currently `0.12`; current source defaults to `0.085`, while the witnessed live scalar is approximately `0.08`. These remain separate source-declared and runtime-observed facts.
- Minime's two-second `STALE_AV_MS` applies to audio/video; semantic staleness is already dynamic, fill-sensitive, entropy/velocity/pressure-aware, and protected by a low-fill recovery hold.
- The Minime runtime excerpt proves available recovery modules, not that hard reset or internal synthesis was active in the witnessed moment.
- Source constants, comments, imports, failed builds, and mutable manifests are never treated as active runtime proof.

## Existing-source verification

- Bridge evidence separates connectivity, first-valid lag, cadence, jitter, freshness, future skew, rolling entropy, density-gradient trajectory, and write-lock wait/hold timing.
- Burst-and-rest source preserves a 30-second floor, so ordinary rest pulses cannot be zero; the burst-exit warmth taper is explicit.
- Codec evidence preserves the 48D representation, deterministic basis health, smooth entropy gate, dynamic bounded tail ceiling, lane energy, headroom, clamp occupancy, and representation loss.
- Minime sensory and regulator reviews preserve dynamic semantic persistence, porosity, pressure source, temporal lock-in, and no-local-control authority.
- Correspondence, mutual address, Reciprocal Uptake, Agency Commons, protected time, and neutral silence now answer the historical relationship architecture without claiming spectral causation.

## Focused test

- `cargo test --manifest-path capsules/spectral-bridge/Cargo.toml control_marker`
- Result: 34 passed, 0 failed.

Full formatting, Clippy, bridge, flywheel, event-store, steward-control, projection, and alignment checks will be recorded in `run_summary.md`.

## Observation and authority boundary

- Backoff/pressure, arrival/integration delay, heartbeat phase, peripheral resonance, shadow texture, projection health, clamp occupancy, and pressure/porosity questions are routed to bounded natural or offline evidence.
- No live contention, blocked heartbeat, reconnect failure, oversized frame, recovery reset, pressure increase, codec counterfactual, noise step, or regulator action is induced.
- No pressure, fill, PI, cadence, codec, model, sensory, protocol, reservoir, scheduling, peer-runtime, or live-control behavior changes are made.
