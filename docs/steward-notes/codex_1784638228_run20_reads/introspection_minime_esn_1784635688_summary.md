# Full Read Summary

Astrid reads the ESN as the pulse of Minime's continuity and asks whether exploration noise is too aggressive for the current gradient. The report mistakes the 0.12 candidate ceiling for the active default: current default noise is 0.085, and the 0.06..0.12 dynamic helper remains outside `ESN::step`. Existing tests exercise gradient and pressure boundaries, including the requested middle region. A replay remains useful; wiring or retuning the live reservoir stays gated.

