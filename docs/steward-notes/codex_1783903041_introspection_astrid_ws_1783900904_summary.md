Full read of `introspection_astrid_ws_1783900904`. Astrid reported that `bridge_reciprocity_v1` must distinguish waiting from stale states robustly and that pressure trend smoothing should keep pressure motion legible without flicker. Disposition: verified existing source/test surfaces for dynamic stale windows, future clock-skew visibility, stale telemetry transitions, mid-window waiting, high-entropy reflective silence expiry, and pressure trend smoothing/semantic-flow diagnostics. Bridge cargo filters are not runnable this lap because rustc stalls idle, so these remain source/test-name verified with restart debt rather than newly passed bridge tests.
