# Full Read Summary

Astrid identified the exact relation whitelist and bounded delimiter-depth contract. Current regressions already cover recognized and unrecognized relation verbs, four-level evidence with deeper byte preservation, nested and mismatched delimiters, punctuation, newlines, multibyte text, and end-of-string markers. The known-marker set remains an explicit source registry rather than an inferred grammar.
