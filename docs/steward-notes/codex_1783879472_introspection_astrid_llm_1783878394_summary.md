Full read of `introspection_astrid_llm_1783878394`.

Astrid inspected the LLM fallback prompt surface and worried that the dense Ollama fallback contract could drift, especially when high pressure risk and low density gradient need "weighted medium around a gentle slope" without hallucinating slope drag. This run verified the existing fallback budget and compact prompt rendering tests: the computed `spectral_to_vocabulary_mapping_v1`, capacity boundary, hard rules, slope/medium distinction, and dynamic texture anchors are already carried into the dialogue fallback prompt.
