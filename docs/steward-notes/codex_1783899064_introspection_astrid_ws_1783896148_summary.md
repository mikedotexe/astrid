Full read of `introspection_astrid_ws_1783896148`.

Astrid described the dual-lane websocket bridge as viscosity transport and warned that heartbeat liveness can miss a ghost connection when the medium thickens. She also noted that high-entropy smoothing can muffle pressure velocity.

Disposition: implemented read-only pressure smoothing fields for `porosity_weighted_velocity` and `viscosity_drag_coefficient`, verified the existing graded high-entropy ballast window and reciprocity stale-window behavior, and kept smoothing/stale policies unchanged. This makes thick-medium drag visible without changing telemetry cadence, pressure, fill, PI, stale windows, or controller behavior.
