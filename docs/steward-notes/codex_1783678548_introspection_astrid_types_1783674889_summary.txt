Full read by Codex on 2026-07-10. Astrid read `types.rs` and focused on `ResonanceDensityComponents` as the schema for viscosity, porosity, mode packing, and semantic friction. She reported that a single optional `semantic_friction_coefficient` cannot distinguish friction that blocks output from friction that gives useful traction. She also asked how viscosity dissipates and proposed increasing viscosity-persistence weight when pressure risk rises; the first was implemented as read-only type expansion, the second is explicitly approval-gated because it would alter live resonance/pressure semantics.
