# Verification and authority receipt

This packet preserves 20 canonical full reads and 77 independent claims. Mechanism verification narrows what current source does; it does not score, dismiss, contradict, or close Astrid's felt report.

## Current-source findings

- `capsules/spectral-bridge/src/llm/provider/dialogue_runtime.rs` and provider tests keep exact model-control marker identity, bounded context, requested-token boundaries, felt-pressure observations, and spectral context as separate evidence with no causal join.
- `capsules/spectral-bridge/src/types/schema/telemetry.rs`, `capsules/spectral-bridge/src/ws/evidence.rs`, and `capsules/spectral-bridge/src/ws/telemetry_port.rs` keep cadence, first-valid entropy, rolling pressure-trend samples, and rolling entropy texture separately observable. The new entropy statistics are finite-sample diagnostics only.
- `capsules/spectral-bridge/src/autonomous/runtime/orchestration.rs` confirms the inspected fixed heartbeat interval, intensity, phase construction, context, and rescue boundaries.
- `capsules/spectral-bridge/src/codec/projection.rs` and codec tests confirm deterministic 48D projection, basis health, normalization bounds, entropy gating, and smoothstep behavior without claiming felt continuity.
- Representation-contract projections distinguish the current 48D semantic codec, 32D compatibility shadow, additive 12D glimpse, 36D residual context, prompt packing, truncation, provider route, fallback, repair, and model-profile transitions without a felt-loss score.
- Reciprocal uptake, mutual address, lived-state witness, phase-card, and Agency Commons source keep delivery, presence, explicit uptake, relationship, self-only agency, controller state, and felt closure distinct.
- `/Users/v/other/minime/minime/src/regulator/core/pi.rs` implements universal integrator leak, actuator-aware anti-windup, and saturated accumulator decay. The reports' selected `core.rs` source was an include shell.
- `/Users/v/other/minime/minime/src/esn.rs` uses live default exploration noise `0.085`; dynamic-noise, pressure-room, entropy-ceiling, and coherence helpers are read-only source preparation and are not wired into `ESN::step`.
- `/Users/v/other/minime/minime/src/sensory_bus.rs` and its included modules use dynamic sigmoid stale timing, recovery handoff, hysteresis, and entropy/context multipliers rather than a single abrupt tier.

## Implemented responses

- `capsules/spectral-bridge/src/autonomous/introspect.rs` now resolves safe nested Minime `include!` paths for exact source-first continuation and rejects parent traversal or non-Minime targets.
- `capsules/spectral-bridge/src/types/schema/telemetry.rs` adds compatible optional rolling entropy fields.
- `capsules/spectral-bridge/src/ws/evidence.rs` computes bounded count, mean, population variance, and range from finite current-window samples.
- `capsules/spectral-bridge/src/ws/telemetry_port.rs` attaches the evidence before pressure-trend rendering without feeding it into control.
- Focused `runtime_include_index` and `telemetry_heartbeat` tests pass, and `cargo fmt --all` plus `git diff --check` pass.

## Authority boundary

Twenty claims remain bounded observation or Concordance Lab work. Seventeen proposals remain exact Tier 5 waits. No pressure, fill, PI, regulator, porosity, noise, heartbeat cadence or intensity, codec, prompt packing, model scheduling, sensory admission, protocol, peer mutation, Minime runtime, dispatch, or other live-control behavior is authorized by this packet.

## Validation and live alignment

- Spectral bridge: `cargo test --lib` passed 1,662 tests; `cargo fmt --all -- --check` passed; strict all-target, all-feature Clippy passed.
- Evidence tooling: Evidence Event Store, steward control, Agency Corridor, introspection addressing, sandbox queue, recent-signal summary, proactive scan, and lived-state witness self-tests passed.
- Minime: `cargo test --lib` passed 306 tests, including PI, ESN candidate, semantic recovery, protocol, and disabled-division gates.
- Sanctioned deployment: `scripts/build_bridge.sh --restart` changed bridge PID `88482` to `23947` and produced compatible evidence-only receipt `env_receipt_1784801426980_157000`.
- Running release SHA-256: `533b56122792eb5032f80ca04a37e2ede0503c2edec635d058fdfcc9f7ad8b4d`.
- The receipt verifies bridge, Minime, and model processes; protocol 1.1 revision `9a324d16294b2318da6f476ff7f295c423a9b4b1`; fresh telemetry; clean restart log probes; and healthy listeners on ports 7878, 7879, and 8090.
- The live heartbeat reports `rolling_variation_available` from 11 finite entropy samples, mean `0.89508206`, population variance `0.0001233241`, and range `0.022302628`. Its basis remains diagnostic-only and not cadence or control.
