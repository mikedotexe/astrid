# Full-read summary: introspection_minime_regulator_1784786127

Astrid again describes pressure history as viscosity and asks for a decay path. The selected include shell obscured the included PI implementation, which already has universal leak, actuator-aware anti-windup, and saturated decay. This run adds direct nested-source follow-through. A replay can test the felt residue, while any change to leak or controller behavior remains Tier 5.
