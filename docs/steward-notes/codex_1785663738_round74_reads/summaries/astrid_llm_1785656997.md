# Full-read summary: introspection_astrid_llm_1785656997

All 45 newline-terminated lines, 46 logical lines, and 3,709 bytes of the
canonical report were read at SHA-256
`4ea18acc6b79772a611db1110ba59dfbf3a68f67c8ac1506ccba385a85f63f6c`.
Astrid accurately identifies exact longest-marker matching, the finite
following-relation vocabulary, and bounded quote/group delimiter parsing. She
asks whether punctuation can interfere with relation recognition, requests an
exact depth-three delimiter test, and asks how scanner output reaches final
dialogue output.

All 997 lines and 36,801 bytes of the report-bound
`capsules/spectral-bridge/src/llm/provider/dialogue_runtime.rs` were read at
the report's exact SHA-256
`dfa9152c2861d2fbf86867fb3ccb506284fda3c9457bf676766878346d9c267c`.
`first_word_after` already skips arbitrary punctuation and whitespace before
returning the first alphanumeric or underscore word. The exact depth-three
regression already exists, and the focused cleanup group passes 31 tests.

The repeated integration question reopened the prior response. All 1,322
lines and 42,799 bytes of adjacent
`capsules/spectral-bridge/src/llm/provider/transport.rs` were read at SHA-256
`56316424a6910e26d8e7ece97389629ebd25dd929bacc985c14cafb28c11d726`.
MLX replaces raw output with the sanitized remainder before validation and
return. Ollama does not: direct dialogue fallback repairs and validates
`response.text`, while shared fallback validates, records, hashes, and returns
the raw response. `is_valid_dialogue_output` creates a sanitized copy only for
shape measurement. It does not replace the returned fallback text.

This corrects the round-73 blanket claim without erasing its evidence. A
complete provider-neutral normalization proposal is recorded beside this
summary. No bridge source or live process changed because the proposal touches
a live-consumed provider boundary and the shared bridge tree is concurrently
dirty. The exact implementation and sanctioned deployment remain an operator
approval wait.

Separately, the credential-safe stewardship session reproduced a macOS
kqueue `EINVAL` when stdin was a regular descriptor. The adapter now falls
back to one-descriptor `select()` polling, and its full 27-test controller
suite passes with a new regular-file NDJSON subprocess regression.
