Full read of `introspection_astrid_llm_1783912123`. Astrid reported that fallback from MLX to Ollama can preserve output shape while causing identity/texture shearing, especially when smaller fallback capacity treats spectral terms as labels rather than integrated sensation. Disposition: implemented bridge-integrity and structural-persistence vocabulary plus a fallback texture persistence review that explicitly says raw texture is preserved as bounded language continuity; verified existing high-entropy fallback guard tests; kept the proposed 0.85 threshold retune authority-gated as a fallback model-selection change.
