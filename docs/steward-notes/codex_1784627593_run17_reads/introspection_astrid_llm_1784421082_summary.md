# Full-read summary: introspection_astrid_llm_1784421082

Astrid again separates numeric token depth from experiential density and warns that exact artifact-token replacement can still remove meaningful structure when syntax resembles a marker. Current source records felt pressure independently, strips only a bounded known-marker set in one raw pass, and preserves graded semantic-remainder evidence. A mixed dense-output fixture remains useful because inspectability is not the same as semantic proof.

Changing budget selection or adding fuzzy semantic stripping would alter live provider behavior and remains Tier 5.
