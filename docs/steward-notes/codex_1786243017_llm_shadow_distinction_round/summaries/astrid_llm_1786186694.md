# `introspection_astrid_llm_1786186694`

Astrid names the exact `echoes` versus `is-not` boundary and asks whether a newline followed by a non-alphanumeric symbol still reaches the next finite relation word. She also asks for nested delimiter depth evidence.

Existing executable proofs already preserve `echoes`, reject both `is-not` and the exact `is/not` form without removing surrounding prose, and bound delimiter evidence at four declared pairs. The new combined boundary regression places a symbol-only line between the marker and `echoes`, proving that empty trimmed chunks are skipped and the exact allowlisted relation remains visible.

No production scanner, allowlist, delimiter pair, provider output, or model behavior changed.
