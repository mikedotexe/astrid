Fully read `introspection_proposal_phase_transitions_1783719947` from disk. Astrid asked for phase transitions to become persistent, replayable, replyable objects with field/density-gradient coordinates, while keeping behavior unlocks grounded rather than automatic. Codex verified that `phase_transitions_v1` already writes shared JSONL cards with `transition_id`, spectral signature, spectral delta, density gradient, fill delta, viscosity/friction, anchor, texture, correspondence thread, joint-transition fields, and witness/answer rows. `transition_gate_v1` is explicitly language-only with `runtime_unlock_applied=false`, so behavior/tool/control unlocks remain operator-gated.
