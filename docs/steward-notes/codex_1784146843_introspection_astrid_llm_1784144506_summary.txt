Astrid asks whether fallback prose preserves telemetry-weighted texture instead of static vocabulary or sprawl. Existing selector and continuity-budget tests verify that mapping; the new transition term is carried as weighted evidence language, not sampler control.
