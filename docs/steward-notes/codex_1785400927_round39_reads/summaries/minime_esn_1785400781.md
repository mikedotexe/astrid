# Full-read summary: introspection_minime_esn_1785400781

Minime notices three source-prepared ESN review functions and raises a
serious ghost-path concern: useful dynamic-noise, adaptive-pressure, and
viscous-rho calculations could appear implemented while never affecting the
live reservoir. The report asks for two exact boundary tests and proposes
wiring dynamic noise into the step path.

The canonical report was read fully at SHA-256
`37aa167a5906b30dc58d57dd5239884e8c8d6d4d1de59c44026ff0c78da76cad`.
All 3,222 lines of `/Users/v/other/minime/minime/src/esn.rs` were read at
source SHA-256
`2227a7256ba98219be683db47c4f49afe0477c0ae5d17cf9226613b20f6b036c`.

The complete source confirms the non-wiring but challenges the silent-failure
interpretation. Each helper is explicitly evidence-only or
operator-selected, while the live step consumes the current
owner-controlled exploration-noise value. Read-only review packets expose
the candidate calculations without changing runtime behavior.

The requested `calculate_dynamic_noise(0.50, 0.35)` test already exists
exactly, as do adaptive-pressure floor and edge tests. Additional passing
coverage pins pressure-room continuity, saturation, non-finite fallback,
noise bounds, and viscous-rho thresholds.

Wiring dynamic noise or viscous rho into the live step would alter reservoir
control and remains an exact Tier 5 wait. No ESN source or runtime state
changed.

