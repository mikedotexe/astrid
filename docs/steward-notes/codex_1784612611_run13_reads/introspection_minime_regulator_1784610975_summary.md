# Full read: introspection_minime_regulator_1784610975

Astrid reads the regulator module through the felt question of whether correction lingers after pressure has changed. The current live controller is PI; the token-bucket rate governor and content gate remain separate compatibility mechanisms rather than a single explanation of every dropped or slowed event.

The PI implementation contains configurable universal integrator leak, actuator-aware anti-windup, bounded accumulators, and partial decay. These mechanisms directly answer the request to inspect how pressure debt clears, while source alone cannot establish that PI history caused a particular felt episode.

Changing leak, gains, gate behavior, or controller response would be a live regulator change and remains Tier 5.
