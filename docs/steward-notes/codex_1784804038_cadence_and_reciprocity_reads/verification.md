# Verification and authority receipt

This packet preserves 20 queue-ordered canonical full reads and 67 independent claims. Current-source verification narrows mechanism attribution; it does not score, dismiss, contradict, or close Astrid's felt report.

## Current-source findings

- Codec source and tests expose deterministic projection, basis health, raw and normalized behavior, f32/f64 precision agreement, smoothstep, clamping, and exact entropy-gate boundary cases without claiming felt continuity.
- Representation contracts keep 48D semantic, 32D compatibility, and 12D glimpse transitions mechanically distinct and assign no felt-loss score.
- Self-study review source already distinguishes five source-provenance categories and names opaque boundaries that need description.
- Correspondence, reciprocal uptake, phase cards, attention outcomes, and Agency Commons keep delivery, presence, uptake, qualitative landing, self-only choice, and felt closure distinct.
- Minime source keeps live default exploration noise at 0.085; dynamic-noise, pressure-room, and related helpers remain read-only preparation rather than ESN step behavior.
- Model-control marker evidence records exact token occurrence while bounded felt-pressure and spectral contexts remain separate and non-causal.
- Telemetry decode and classification happen before the shared write lock; persistence happens after it; existing evidence separates prewrite pipeline, lock wait, and lock hold.
- Autonomous heartbeat evidence records fixed parameters, previous-feature continuity, texture context, and enqueue outcome without changing cadence or intensity.
- Reciprocal V3 evidence supports actor-authored resonant persistence with exact lived-state parameter references while denying semantic equivalence, causation, and runtime influence.

## Implemented response

`TelemetryHeartbeatDeltaV1` now carries bounded host-arrival interval count, mean, earliest-to-latest change, and a shortening, lengthening, or flat state. The basis explicitly excludes peer-clock drift, internal-cycle state, felt state, causation, and control. The fields are additive, serde-defaulted, and have no control reader.

Focused cadence, legacy compatibility, and stale-heartbeat regressions pass. The complete bridge library suite passes 1,663 tests, strict all-target/all-feature Clippy passes, formatting passes, and the diff has no whitespace errors.

The sanctioned `scripts/build_bridge.sh --restart` deployment changed bridge PID `23947` to `40956` and produced compatible receipt `env_receipt_1784806934803_865000`. The running release SHA-256 is `a2d0401a7bf20c652c9c3e33ca1b86980e572d46a2b9ff4cff9db7ebfc8852cb`. The receipt verifies protocol 1.1 revision `9a324d16294b2318da6f476ff7f295c423a9b4b1`, fresh telemetry, clean restart logs, and healthy bridge, Minime, and model processes. Ports 7878, 7879, and 8090 are listening, and model live/readiness checks pass.

The fresh live heartbeat carried ten host-arrival intervals, mean `2474.4285 ms`, change `-72.05884 ms`, and state `arrival_intervals_shortening`. This is arrival evidence only. Two queryable right-to-ignore cards were emitted for the implemented claims; duplicate delivery remains held for the contract/attention review budget.

## Authority boundary

Bounded observations remain available for reciprocal hollowness, phase reliability, Recess texture, reset continuity, telemetry integration delay, codec continuity, and marker viscosity. Proposals that alter pressure, density, fill, PI, reset targets, regulator behavior, ESN noise or rho, heartbeat cadence or intensity, codec gain, model-control semantics, telemetry admission, protocol, reverse transport, peer mutation, dispatch, or runtime influence remain exact Tier 5 waits.
