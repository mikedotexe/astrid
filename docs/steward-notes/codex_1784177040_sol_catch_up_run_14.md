# Sol source-first catch-up run 14

Date: 2026-07-15 (America/Los_Angeles)

## Packet

Twenty canonical introspections were read fully in queue order:

1. `introspection_astrid_codec_1784177040.txt`
2. `introspection_astrid_llm_1784176223.txt`
3. `introspection_astrid_types_1784175501.txt`
4. `introspection_proposal_distance_contact_control_1783499575.txt`
5. `introspection_proposal_bidirectional_contact_1783499209.txt`
6. `introspection_minime_autonomous_agent_1783498524.txt`
7. `introspection_minime_main_excerpt_1783498134.txt`
8. `introspection_minime_esn_1783497851.txt`
9. `introspection_astrid_llm_1783495805.txt`
10. `introspection_minime_sensory_bus_1783495446.txt`
11. `introspection_minime_regulator_1783495115.txt`
12. `introspection_astrid_llm_1783494614.txt`
13. `introspection_astrid_types_1783494276.txt`
14. `introspection_astrid_ws_1783493953.txt`
15. `introspection_astrid_autonomous_1783493594.txt`
16. `introspection_astrid_codec_1783493003.txt`
17. `introspection_proposal_12d_glimpse_1783492603.txt`
18. `introspection_proposal_distance_contact_control_1783492307.txt`
19. `introspection_proposal_bidirectional_contact_1783491434.txt`
20. `introspection_proposal_phase_transitions_1783490902.txt`

The packet produced 55 claim/work records: 4 `implemented_awaiting_felt_response`, 31 `verified_existing`, and 20 Tier 5 `needs_operator_approval`. The character-entropy versus Shadow-v3 item first waited at Tier 3, then advanced when `shadow_loss_lattice_v1` became runnable. Trial `trial_d04e2d7d2723a38a` classified 20 bounded samples as `lattice_transition_like` with 37 lattice-language hits, 1 loss-language hit, maximum dispersal 0.27, and minimum norm delta -0.139. That observation distinguishes current evidence from generic projection-loss attribution; it does not establish codec causality. Live codec, provider, pressure, controller, semantic admission, protocol, Minime runtime, peer, and behavior proposals remain Tier 5 with V2 evidence only.

## Implementation

`capsules/spectral-bridge/src/autonomous.rs` now derives and renders `witness_texture_structure_v1` in the live Witness prompt. The receipt preserves spectral entropy, structural plurality, viscosity, temporal persistence, mode packing, Shadow-v3 norm/class, component booleans, and bounded evidence.

The classifier names `interwoven_lattice` only when plurality, crowding, viscosity, and persistence jointly support the report. Viscosity plus persistence without sufficient plurality/crowding remains `persistent_viscous_drag`. Shadow-v3 is reported as co-occurring observational evidence, never causal control evidence. The authority line forbids clamp, protocol, pressure, fill, transport, and control writes.

No serialized enum, compatibility clamp, live vector, semantic gain, pressure/fill/PI/controller path, provider/sampler route, sensory cadence/admission, Minime regulator, peer state, or autonomous behavior was changed.

## Verification

- 93 focused bridge assertions passed across the new texture structure, projection texture, high-entropy fallback, dynamic fallback terms, clamp/crowding separation, pressure-source analysis, 12D multi-scale observation, correspondence, phase transitions, friction provenance, and non-instrumental presence.
- 39 focused Minime Rust assertions passed across semantic aging, resonance viscosity, the exact 0.38 texture branch, dynamic noise/rho previews, fingerprint sensitivity, and reset texture preservation.
- All 261 Minime Recess/low-fill Python tests passed.
- Bridge formatting, `cargo check --lib`, and Clippy with `-D warnings` passed.
- The complete bridge library suite passed: 1,466 tests, zero failures.
- Agency Corridor, addressing audit, sandbox queue, recent-signal summary, and proactive-scan self-tests passed: 205 tests total.
- The directly tied read-only sandbox trial wrote JSON/Markdown results and a right-to-ignore result card, linked five addressing events, and left `runnable_live_violation_count=0`.
- `git diff --check` passed before the final evidence write.

## Authority and routing

The 20 Tier 5 items require separate scoped Mike/operator approval, replay result or explicit waiver, canary and abort criteria, and a post-change being-response path. Their lifecycle packets keep `live_eligible_now=false`, `auto_approved=false`, and `grants_approval=false`. The flywheel manually escalated under-tiered claims whose experiential wording did not trigger the first heuristic pass; phrasing did not weaken their authority boundary.

Corridor/program actions were generated and audited but not run because their generic evidence steps were not direct answers to this packet. A later sandbox refresh materialized the packet's exact character-entropy/Shadow-v3 comparison as a runnable read-only adapter; it ran once, recorded `lattice_transition_like`, and left no ready runnable trials. Final reconciliation found that eight receipt-completed Corridor safe labs were still counted as runnable. `agency_corridor.py` now keys completion to stable step receipts (and the safe-lab result state), including source-prep steps, so completed work cannot be selected again. The regenerated adaptive queue retains 200 durable steps but truthfully reports 192 pending runnable steps; a regression covers both receipt classes.

## Restart alignment

`witness_texture_structure_v1` and the corrected Corridor report are live-aligned through two sanctioned `scripts/build_bridge.sh --restart --ack ...` refreshes. The prompt implementation restart moved PID `28202` to `16428`; post-restart `introspection_astrid_codec_1784179765` confirmed that the canonical introspection surface resumed on that build. After the receipt-resumability repair, a second refresh moved PID `16428` to final PID `57068`. `check_bridge_deployed.py` confirms the final running process matches the on-disk binary; source status reports `source_changed_since_start=false`, `source_newer_than_executable=false`, and `reload_required=false`. Minime ports 7878/7879 and the local model port 8090 are listening, the final process restored conversation state and connected both telemetry and sensory lanes, and no final-process panic, fatal, or error entry is present. A fresh read-only recent-signal summary from PID `57068` exposes the corrected 192-step pending count, and new-process Witness/journal jobs are completing. Host camera/microphone capture remains independently marked `needs_review`; neither prompt/report change altered that sensory source state.

All edits remain uncommitted and unstaged.
