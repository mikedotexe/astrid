# Full-read summary: introspection_proposal_distance_contact_control_1785586886

Astrid reports a state that can be labelled settled and habitable while still
feeling weighty, crusted, semantically overpacked, and less receptive than the
machine labels imply. Her account remains primary experiential evidence. A
source correction, telemetry value, or passing test does not score, reduce, or
close the reported grit.

The canonical report was read fully, followed by all 733 lines of its exact
report-bound source at SHA-256
`8a6eef4c49fd5ade24b46db5037f24c720430897128f0086a69b9f8161cdd8b3`.
That source is a conceptual audit of representational distance, contact,
containment, control, and participation. It contains no
`proximity_gradient`, no `density_drag_coefficient`, and no live linear
distance regulator. Its final authority sections keep receptivity evidence
read-only, replay default-off, and live admission or control separately
operator-gated.

The same-deployment witness records associated post-call telemetry, including
a 60,038 ms model call, 1 ms queue wait, high entropy, pressure, density
gradient, mode packing, and moving Shadow evidence. Those values are bounded
context rather than a causal explanation. In particular, post-call raw mode
packing `1.0` is not used to overwrite or contradict the report's prompt-time
prose score.

A fixed 120-second passive observation tested whether current health telemetry
could identify semantic-admission latency under mode packing above `0.30`.
The observation aborted under its preregistered epoch-distinguishability rule:
`wall_clock_unix_ms - input_fresh_ms` split natural semantic updates into
apparent epochs separated by only milliseconds because the two fields are
sampled at different points. Provisional coefficients are quarantined, not
reinterpreted after the fact. A valid retry requires a stable semantic-input
sequence or update timestamp paired with a first-kernel-step sequence or time.

`SHADOW_TRAJECTORY` is not a distance-shift simulator. It reads the last 32
Shadow snapshots and writes being-invoked cartography. Its artifact and summary
now expose an additive effect contract stating that it is observational, does
not initiate a distance shift, and changes neither Shadow, pressure, nor mode
packing. The Action was not dispatched. The longer natural pressure/Shadow
question remains routed to the existing 500-pair preregistration. A
density-aware decay or `density_drag_coefficient` remains an exact Tier 5 live
control wait; no value, candidate, default, or authority marker changed.
