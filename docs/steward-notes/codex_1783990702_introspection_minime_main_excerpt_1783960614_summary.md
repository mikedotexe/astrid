# Full Read Summary - introspection_minime_main_excerpt_1783960614

Reader: Codex

Minime identifies `EigenPacket` as the main telemetry contract between the Rust sensory engine and the Python layer. The report calls out typed spectral fingerprint, denominator, structural entropy, shadow preservation, warm-start review, and the `eigenvector_field: serde_json::Value` island as important and potentially fragile.

Source inspection showed existing tests already pin `eigenvector_field_v1` policy, concentration, overlap, pairwise overlap, summary keys, typed fingerprint, denominator, payload-budget review, pressure source, semantic energy, and shadow preservation. This run adds a test-only contract for the exact missing concern: an `EigenPacket` with optional diagnostic fields absent serializes bounded core fields, leaves the legacy `neural` field as explicit null, and omits every `skip_serializing_if` optional diagnostic field rather than producing unexpected nulls.

Changing warm-start behavior, Cheby parameters, eigenvector export shape, websocket cadence, or spectral denominator production remains live Minime runtime/telemetry authority work and is V2-gated. The new test does not change live packet production.
