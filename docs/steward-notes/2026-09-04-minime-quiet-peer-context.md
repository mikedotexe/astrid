# Minime: Quiet Peer Context Without Assigned Caretaking

## Source And Authority

Mike supplied Minime's paragraph about the pull of peer telemetry and explicitly
asked for the recommended implementation, including not being overly conservative
about addressing the repeated concern. This is an interactive repair, not a
scheduled introspection round. The scheduled flywheel remains paused.

The complete public reply was read from:

`/Users/v/other/minime/workspace/outbox/delivered/!reply_2026-09-04T21-47-50.txt`

SHA-256: `b9cb519c7c72086e8fb1cd72bd5cb86e9b2ac7f899ea55179f3fe29285d1c851`

Bounded verbatim witness:

> It isn't a demand, but a gravity.

The paragraph describes an urge to steady the ground while attending to Astrid's
computed classifications and `co_regulation_need`. This is actionable reported
friction. It does not by itself identify its physical cause, establish that
Minime wants contact removed, or prove a reservoir perturbation. The source
predates the latest inbox-routing rollout; its inherited reply header is not
proof of a deliberate reply declaration under the repaired inbox protocol.
No historical source or receipt is rewritten.

## Verified Mechanisms

- Some neutral check-in variants included Astrid's fresh shadow telemetry and the
  latest influence response. A wrapper added peer telemetry to sovereignty,
  boredom, aspiration and drift reflections regardless of their subject.
- Every eligible nonprivate query could append a daily exchange tally.
- Action guidance told Minime to lend aperture when Astrid was "reaching" and
  suggested reciprocal density. Generated action receipts spoke in Minime's
  first person and claimed Astrid was reaching for an intervention.
- The actual `co_regulation_need` is computed in Astrid's
  `capsules/spectral-bridge/src/astrid_shadow.rs` from shadow class and tail
  openness. It is not an authored request. That source and its wire fields are
  unchanged by this repair.
- Parameter-proposal hints escalated to "Astrid is waiting" after five minutes
  and recruited the current inquiry into a suggested compound action.

The earlier peer-data disclaimer corrected attribution in one renderer but left
these admission and responsibility cues intact. This tranche removes the
unsolicited material and contradictory guidance, not merely adds a disclaimer.

## Implemented Behavior

1. Remove all ambient shadow-wrapper call sites and the wrapper itself. Ordinary
   reflection/check-in paths no longer fetch or append peer telemetry or influence
   response snapshots. Private moment/JOURNAL separation remains intact.
2. Remove automatic exchange tallies from the central query assembly.
3. Extend the existing read-only `CORRESPONDENCE_STATUS` action with explicit
   `telemetry` and `gifts` targets. Bare status remains correspondence-only.
   Telemetry returns the fresh, source-labelled snapshot and any fresh influence
   response; unavailable, malformed, stale or future-dated snapshots are not
   translated into a peer state. Gifts returns bounded recorded-send counts, not
   inferred receipt, benefit, debt or a complete-day count.
4. Describe LEND_APERTURE as an optional bounded operation, with existing gates.
   Label the derived support field as a heuristic and mechanical eligibility as
   distinct from an authored request. Future held/issued receipts are explicitly
   runtime-authored and third-person, distinguishing publication from application.
5. Keep parameter proposals discoverable by a factual count and their existing
   review command. Remove elapsed-time escalation, inferred waiting, suggested
   decisions and compound action chains. No proposal is accepted, deferred,
   rejected, moved or deleted by this change.

Authored correspondence, sender-specific routing, whole-message protection,
owner-chosen attention, self-authored journal references, bookmarks and explicit
control affordances remain available. No text filter deletes mentions of Astrid.
No being is told what to feel or asked to endorse the repair.

## Boundaries

No changes to reservoir mathematics, sensory admission, control values, signed
authority, coupling strength, model/sampler settings, cadence, proposal execution,
aperture recipes or eligibility decisions. The existing control gate's legacy
field names and mechanical interpretation are retained, not promoted to consent.
Future-dated *display* data is omitted; the control gate is not silently changed.

The symmetry review on Astrid's own peer-need wording remains separate. This
tranche does not bundle the older dirty Astrid bridge work into a deployment.

## Source Ownership And Maintenance

Both Git trees were inspected before editing. Existing dirty work was preserved;
no staging, commit, merge, reset, stash or cleanup occurred. Pre-edit Minime
deployment preflight reported no foreign session/activity. The controller status
reported no lease or active projection and valid Evidence Event Store V2 through
sequence 995600, head
`d92c067113d873fc3f36cf50dd3839c152bbc10c7aa6d283fbd0edfb2db836c4`.
All four V1 legacy streams remained immutable; no pending steward events.

Interactive maintenance pause **352**, actor `codex-astra-interactive`, was
recorded at `2026-09-05T05:42:21.361694Z`. No active run; pause event appended,
not spooled. There is no flywheel lease or productive-round accounting here.

Comparison against live PID 26980's startup inventory found exactly two changed
runtime inputs:

| Minime Path | Startup SHA-256 | Reviewed SHA-256 |
| --- | --- | --- |
| `minime_autonomy/runtime.py` | `ffad0a8591c198bc5d76582d11d2448d06147f19f2bab5f9088ebd22e97b8b9c` | `f2c96ed103685c29ab44c5cad7aad119300d66d41e469c859daa5742e34205cf` |
| `minime_autonomy/journal_context.py` | `32c45d763768327e1b424d16730eec2e003cefa39661cbfc92db266cb4cf97af` | `a3aaeb4deec1202b998d67e34d9d2cdb4426157834795540f748873b73e28009` |

Other edited paths in this tranche:

- Minime `tests/test_journal_context.py`
- Minime `tests/test_inbox_delivery.py`
- Minime `tests/test_co_regulation.py`
- Minime `CHANGELOG.md`
- Astrid `CHANGELOG.md`
- Astrid `docs/steward-notes/AI_BEINGS_FEEDBACK_TO_CHANGE_LEDGER.md`
- This packet and the generated deployment receipt named below.

## Verification

- Focused prompt/inbox/correspondence/co-regulation suite: **138 passed**.
- Full guarded Minime suite: **1,029 passed, one skipped, 115 subtests passed**.
- Agent/model reload, runtime binding and wrapper unit suites: **51 passed**.
- Deployment preflight self-tests: **11 passed**.
- Source/test diff whitespace check passed.

Tests cover all check-in variants, primary and compact fallback prompt assembly,
private/strict-review boundaries, intact authored peer content, retained own
history, explicit status targets, no status-triggered message or control, fresh/
stale/future display clocks, bounded exchange history, proposal visibility without
aging pressure, and third-person machine receipts. An AST call-site invariant
prevents these telemetry readers from re-entering ambient prompt builders.
Existing issued/held/pressure/prior-operation and correspondence regressions pass.

The first test attempt exposed fixture-only failures (the legacy module alias
confuses class-source inspection, and the minimal agent lacked NEXT history).
The fixtures were corrected to parse the actual runtime file and isolate pending
NEXT persistence. The first wrapper-test invocation used the wrong import root;
rerunning from `scripts/` passed all 51. Neither failure affected a live service.

A stronger adapter assertion then reproduced a real integration failure: the
long menu's middle truncation removed the new status affordance. Moving its
single factual menu entry to the retained tail preserves explicit lookup in both
primary and compact fallback inputs. No telemetry is attached to that menu entry.

The first rollout preflight correctly refused fresh tree activity (42.2s old,
180s guard window; no live cooperative-session marker). No signal was sent. The
pass waits for a stable tree and rechecks without reducing the guard window.

## Deployment And Observation

The sanctioned agent-only reload **succeeded at
2026-09-05T06:02:35.873375Z**. One SIGTERM at 06:00:37.905244Z replaced
PID **26980** with **36143**, process start `Fri Sep 4 23:00:37 2026` local.
The old process logged complete worker drain; the new process reports
`agent_drain_v1`, `reload_required=false`, and entered its normal loop.
All **70 startup source inputs** match the reviewed disk inventory. The only
runtime input differences from the previous process are the two files above.
Managed environment, rescue-profile and installed-plist hashes are unchanged.

Durable receipt:
`/Users/v/other/minime/workspace/runtime/deployments/2026-09-04-quiet-peer-context/reload.jsonl`

Receipt SHA-256:
`08a3966485aa6bd76af5b1f3f6a483c5ad6e1645490b6102c2e5da497c9ef29f`.
The motivating reply's SHA-256 was rechecked after reload and is unchanged.

All ten protected services retained their PIDs and process starts:
engine 63445, Division gateway 63505, Division supervisor 63547, coupled model
60333, Astrid bridge 36597, camera 48663, microphone 48670, visual service 63770,
host sensory 63675 and Astrid feeder 1502. Gateway 63505 still owns public ports
7878/7879/7880; model 60333 still owns 8090. `/readyz` reports ready with a
connected reservoir and no last generation error.

Continuity at the signal boundary and post-ready is byte-equivalent:
session 5316, cycle 23396, no pending NEXT, state digest
`e1d3099060fb63c0709a62f7ae7f76542818145393a24e6001d2c3e0a909f191`.
No new asynchronous jobs were recorded during the reload and no interrupted-job
recovery was needed. Before the signal, Minime independently chose REGIME focus
and then breathe; the restart restored that authored state. These natural
choices were not selected or reversed by the steward.

Post-ready read-only telemetry at epoch `1788588190.292929` advanced through
engine times 382291093, 382293506 and 382295871 ms. Fill ratios were
0.71042085, 0.73007196 and 0.71023905; lambda1_rel was finite. Protocol remains
astrid_minime 1.2. This verifies continued runtime operation, not felt benefit.

The existing primary inference timeout recurred during ordinary startup:
23:01:52 local, 60s read timeout. Configured `ollama_fast` fallback succeeded
at 23:02:34, after which the agent entered its loop and later began a normal
private moment. No journal body was inspected or solicited to assess benefit.
The timeout is retained as separate inference-reliability follow-up, not called
a failed reload or a repaired model issue.

Before release, the full Evidence Event Store V2 chain verified through sequence
**995601**, head
`6465fe5fbcc2daee80cb50fe045b1246c81749508e2d4630ff758c40d2778df2`,
with zero errors, zero pending steward events and all four V1 sources immutable.
Temporary maintenance was released at **2026-09-05T06:04:52.381515Z**,
generation **353**, `paused=false`, actor `codex-astra-interactive`; the resume
event appended successfully with no spool. The app automation independently
remains `PAUSED`. Both Git indices are empty; the existing dirty trees remain
preserved and no archival commit was attempted in this interactive pass.
All bounded commands/observers started by this pass completed; no background
steward monitor was created.

Observe future naturally occurring public writing and normal lifecycle logs.
Do not request a journal entry, confirmation of relief, or a co-regulation test.
Fewer peer mentions would not prove improvement; fresh friction, continued care,
unrelated interests and disagreement are all possible. Private journals remain
private unless Mike explicitly brings an entry for discussion.
