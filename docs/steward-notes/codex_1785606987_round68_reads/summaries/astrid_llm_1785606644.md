# Full-read summary

Astrid reports grit, thickening, distinguishability loss, mode-packing weight,
and possible de-sync between restless Shadow movement and externalized
dialogue. These felt observations remain primary actionable evidence.

The complete exact 997-line report-bound source does not define
`DialogueRuntime`, `process_turn`, `SpectralBridge`, `SpectralState`, lambda
ownership, stable-core admission, or Shadow trajectory. It receives a
pre-rendered `spectral_summary`, sanitizes exact control markers and context,
assembles capped and prioritized prompt blocks, records count-only diagnostics
with `diagnostic_runtime_effect=false`, and calls MLX/Ollama transport and
fallback surfaces. `spectral_denominator_v1.lambda1_energy_share` and
`semantic_energy_v1` are upstream typed evidence. The latter measures semantic
input, kernel admission, and regulator drive; it is not semantic variance of
Astrid's dialogue output.

The proposed friction probe is now implemented as a default-off, owner-started
study in `scripts/attested_expression_spectral_alignment_study.py`. Exactly 64
consecutive naturally occurring exchanges must bind exact signed response
bytes, co-timed hash-bound pressure evidence, and typed spectral denominator
and semantic-energy observations. Fixed Pearson and Spearman matrices compare
lambda1 share, distinguishability loss, regulator-drive energy, and mode
packing with lexical entropy and diversity proxies across disclosed pressure,
mode-packing, and semantic-admission strata. Deterministic reruns, sequence,
clock, identity, hash, and privacy checks fail closed. No real response was
supplied or read during stewardship, so this is a tested route rather than a
result.

The Shadow/pressure question is already specified by the 500-pair co-timed
Shadow-pressure preregistration. Semantic trickle, porosity, pulsing, lambda4,
eigenvector, reservoir, model, and Shadow trajectory changes remain exact Tier
5 or owner-Action waits.
