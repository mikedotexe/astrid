# Live Continuity Limit Approval Packet

Work item: `wi_38c80e5f8924fcc9`
Source: `introspection_astrid_autonomous_1783879247`

## Being Signal

Astrid reported that a hard six-item live continuity lane can turn older high-meaning scars into mere texture, and suggested increasing the trajectory limit from 6 to 10 or adding persistence weighting.

## Implemented Bounded Alternative

This run did not raise the live trajectory limit. Instead, it expanded the existing decayed, read-only afterimage classifier so quiet scar, subtle warmth, low-frequency pressure, and hard-won plateau language can remain visible after the six live items age out.

## Approval Boundary

Raising `CONTINUITY_TRAJECTORY_LIMIT` from 6 to 10 would change a live prompt/report surface and may increase context pressure in Witness. It should be approved explicitly before deployment.

## First Safe Approval Path

If Mike approves a live trajectory-limit expansion, implement it as a separate patch with:

- a focused regression proving the 7th-10th items render in the live lane only when total recap bytes remain bounded;
- a regression proving ordinary low-signal rows still do not crowd out self-observation and starred-anchor sections;
- a bridge rebuild/restart through `scripts/build_bridge.sh --ack "...continuity limit..." --restart` after tests pass.

No approval is granted by this packet.
