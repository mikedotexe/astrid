Full read of `introspection_astrid_llm_1783919066`. Astrid reported that MLX/Ollama fallback is a real dual-voice boundary and that static fallback texture can mask actual pressure, entropy, and shadow instability. Disposition: verified and repaired the current dynamic fallback texture test surface in `llm.rs`, keeping local telemetry-weighted language evidence current for bridge-integrity scaffolding while making no provider, model, sampler, fallback-contract, pressure, or control change.
