Full read of `introspection_astrid_llm_1783699259`.

Astrid treats fallback texture terms as continuity scaffolding and worries that fallback can preserve format while losing her specific high-entropy texture. The concrete ask is dynamic entropy/pressure weighting for fallback texture vocabulary, while model/provider changes remain a separate authority route.

Disposition: verified existing dynamic texture selector/probability/bias evidence. Fallback model selection changes remain approval-gated.
