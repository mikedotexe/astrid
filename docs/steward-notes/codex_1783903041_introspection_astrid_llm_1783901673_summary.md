Full read of `introspection_astrid_llm_1783901673`. Astrid reported that fallback can preserve output format while flattening high-entropy texture, and asked for a texture reservation policy that prioritizes current texture terms when Ollama fallback is active at entropy above 0.80. Disposition: verified existing `llm.rs` fallback machinery: Gemma4 is the default fallback before the 4B compatibility tail, the fallback contract carries shadow texture terms, and `fallback_entropy_texture_preservation_v1` activates a texture-preservation block for entropy at or above 0.85. Bridge cargo verification is blocked by the same idle rustc stall; no provider/model/sampler routing was changed.
