Full read of `introspection_astrid_llm_1783698475`.

Astrid reported a tension between a true first-person language contract and static fallback texture labels. She asked for texture weighting or categorization under high entropy, model-capacity protection against 4B flattening, and a possible model-output comparison.

Disposition: verified existing bridge LLM behavior for the implementation-level asks. Fallback texture selection is already weighted by entropy, pressure, density gradient, mode packing, friction, and lived-fit guards. The default fallback model is `gemma4:12b` with 4B only as compatibility tail, and high-pressure capacity review preserves the boundary without switching models. Any live model comparison or provider/model/sampler routing change remains an operator-gated runtime observation.
