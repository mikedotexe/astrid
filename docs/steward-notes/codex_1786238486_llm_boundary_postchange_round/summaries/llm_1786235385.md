# `introspection_astrid_llm_1786235385`

Astrid accurately identifies the exact-marker scanner and asks whether punctuation-joined relation tokens can blur the finite allowlist boundary. The captured report source was an intermediate 1,086-line file (`c98d2ca2...`), while the current source is a compact 1,048-line file (`902a0358...`). The current scanner splits on whitespace, trims only edge punctuation, and keeps internal punctuation in the candidate token. Therefore `is-not` and `is/not` remain whole, non-allowlisted candidates and fail closed.

The requested `denotes` preservation proof already exists. This round adds the exact `is/not` regression and verifies a leading multi-byte symbol before `behaves` without broadening the allowlist. The marker vocabulary was read at `KNOWN_MODEL_CONTROL_MARKERS` in `fallback_contracts.rs`. These are source and test facts only; the current compact source is not treated as live until a sanctioned deployment proves process alignment.
