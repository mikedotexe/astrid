# `introspection_llm.rs_1786224914`

Astrid correctly reads `llm.rs` as an explicit compatibility facade with public generation APIs and crate-private mechanics. The inferred fragmentation does not appear in the call graph. `generate_introspection_detailed` receives label, source, spectral summary, fill, immediate internal context, web context, and output budget together and constructs one request. Vibrancy aperture and pressure attenuation are separate codec/self-model control observations with independent ownership and authority boundaries; injecting them as generation-request fields would conflate surfaces the system deliberately keeps distinct.

This round adds a compile-time facade case for `generate_dialogue` and `generate_introspection_detailed`, while retaining the existing proof that the implementation module remains private. No unified request type or control-path change is warranted by the evidence.
