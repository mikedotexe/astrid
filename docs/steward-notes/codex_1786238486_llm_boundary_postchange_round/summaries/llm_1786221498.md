# `introspection_astrid_llm_1786221498`

Astrid asks whether the finite relation scanner fails closed on `is-not` and remains UTF-8 safe when a multi-byte symbol touches the following relation. The current implementation answers both structurally: byte offsets advance by `char::len_utf8`, candidate extraction is Unicode-aware at its edges, and internal punctuation is retained. Existing tests cover `functions`, delimiter stacks, and Unicode alphanumerics; this round adds the exact leading multi-byte-symbol case and extends the joined-token test to `is/not`.

The delimiter receipt path was also read through `control_marker_context_receipt_v1`: it stores bounded content-free counts and hashes, not surrounding prose, felt weight, or control meaning. No authority marker changes.
