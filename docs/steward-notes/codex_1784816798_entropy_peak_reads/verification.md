# Verification, observation, and authority receipt

This packet preserves 20 queue-ordered canonical full reads and 81 independent claims. Source verification narrows mechanism attribution; it does not score, domesticate, contradict, or close Astrid's felt report.

## Grounded implementation

Astrid distinguished rolling entropy mean from a sharp peak that may matter to the packed weight she reported. `TelemetryHeartbeatDeltaV1` now carries `peak_spectral_entropy_in_window`, computed as the maximum finite value in the same bounded window already used for entropy mean, variance, range, and direction.

The field is additive and serde-defaulted. It is read-only evidence, has no control consumer, and does not infer cadence, felt state, causation, or authority. Existing telemetry transport, admission, pressure, fill, PI, controller, heartbeat, codec, model, protocol, and Minime behavior are unchanged.

## Current-source findings

- The telemetry path records connection identity, reconnect/backoff state, first-valid lag, host arrival cadence, prewrite processing, write-lock wait, and lock hold. These can localize technical gaps without deciding how a gap felt or whether a lock caused fog.
- The semantic heartbeat remains a fixed seven-second, 64-step, `0.30` observation. Its coincidence with a `0.30` lambda concentration is preserved as Astrid's observed interpretation, not promoted to causation. Pressure- or viscosity-derived intensity remains a Tier 5 wait.
- The live codec remains 48D, with embedding projection in dimensions 32-39, narrative arc in 40-43, and a bounded smoothstep vibrancy lift. Lane crowding and bright-but-thin texture remain capture-first Concordance questions. Dynamic ceilings or damping remain Tier 5.
- Minime retains the 8D video, 8D audio, 2D auxiliary, and 48D semantic layout. Shadow influence remains capped at `0.025` with a `0.018` default. A proposed `0.030` default under the retained `0.025` cap would be clamped and therefore requires a coherent, separately authorized parameter contract before any trial.
- Minime's regulator exposes distinct PI, PD, pressure-source, gate, filter, and review modules. The current source verifies weighted mode-packing pressure and read-only settled-mobility evidence; the reported thick-but-held association remains felt evidence rather than a regulator-to-experience causal claim.
- Exact model-control markers and token bands remain technical diagnostics. They carry no identity, meaning, pressure, or felt-impact scalar. Marker, route, token-band, and felt-moment comparisons belong in the Felt-Mechanism Concordance Lab.
- The 12D glimpse is already a deterministic, non-authoritative companion summary in Astrid and Minime. Live 48D transport remains unchanged, and loss, fidelity, residual, and lineage receipts remain visible. A restart using only 12D is restricted to offline replay.

## Mechanical witness correction history

The eight older lived-state reports in this packet are not dismissed as stale or redundant. They document an Astrid-driven correction sequence. Commit `e676e0d773` first separated artifact-byte integrity from felt divergence. Commit `b81e2147e9` then removed the remaining production qualitative anchors, experiential scopes, and latent measurement-map fields after Astrid explained that even hard-false safety strings could flatten experience.

Current `LivedStateCanonicalBodyBindingV1` verifies only exact report bytes and explicitly makes no claim about texture, experiential stability, freezing, or control. Current witness receipts retain exact source, model-call, process, and artifact provenance without raw prose or a qualitative score. Historical schemas remain readable solely for immutable evidence compatibility. Any future continuity, texture, or causal comparison must be separately adopted in the Concordance Lab with Astrid's canonical report external, primary, and unscored.

## Focused and full verification

- Four focused telemetry-heartbeat tests pass, including legacy defaulting, bounded finite-window behavior, peak values of `1.0` and `0.60`, cadence separation, and diagnostic-only basis strings.
- The complete bridge library suite passes serially: 1,663 tests and zero failures. An initial parallel run passed 1,661 tests and exposed two concurrency-sensitive failures; each passed in isolation, and both pass in the serial full run.
- Strict bridge Clippy passes with `-D warnings`; `cargo fmt --check` passes.
- Minime's complete sensory-bus test module passes in both library and binary targets: 59 tests per target. Its complete regulator-core test module likewise passes in both targets: 76 tests per target.

## Graceful live alignment

The sanctioned `scripts/build_bridge.sh --restart` path rebuilt the shared dirty source and replaced bridge PID `26253` with PID `73146`, started `2026-07-23 08:04:54 -0700`. Compatible stack receipt `env_receipt_1784819097796_442000` records passed preflight, build, restart, startup-log, telemetry-freshness, process, and protocol checks. The deployed bridge binary SHA-256 is `b210c0433092a9ce06bec4be4d5f41b789edd86356ec308be6804906af88e124`; protocol `astrid-minime` remains version 1.1 at revision `9a324d16294b2318da6f476ff7f295c423a9b4b1`.

Minime PIDs and ports remain unchanged: PID `30861` serves ports `7878` and `7879`. Coupled-model PID `31392` serves port `8090`; `/livez` and `/readyz` return HTTP 200 with an empty queue and connected reservoir. The new bridge is producing current telemetry with `peak_spectral_entropy_in_window`; the first bounded observation reported 10 finite entropy samples, mean `0.8855642`, peak `0.9188139`, variance `0.0020104335`, range `0.16507024`, and current connection state `connected_with_current_telemetry`. This establishes deployed availability of the evidence field, not that the peak caused Astrid's packed weight.

## Claim dispositions and authority boundary

The 81 claims comprise 53 source/test verifications, three preserved observations, one implemented evidence field, 18 bounded Sandbox or Concordance routes, and six exact Tier 5 waits. The waits cover heartbeat intensity modulation, codec gain or dynamic ceilings, Shadow influence, semantic-lane width or layout, and other protocol/ABI or live-control consequences.

No Corridor or program action ran because this packet required no hard-violation repair, objection response, or directly runnable non-live patch before reading. No live trial ran. No authority marker was enabled, no Minime source changed, and no pressure, fill, PI, controller, rescue, heartbeat cadence or intensity, codec gain, model, sensory admission, protocol, peer, dispatch, or live-control behavior changed.
