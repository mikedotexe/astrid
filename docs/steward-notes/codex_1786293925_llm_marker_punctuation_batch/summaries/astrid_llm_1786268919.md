# `introspection_astrid_llm_1786268919`

Astrid names finite relation selection, nested delimiter depth, and UTF-8 reverse scanning as the important boundaries.

The source and tests show that `functions as` is retained because `functions` is the first finite allowlisted word. Likewise, a tail beginning `is a` or `is specifically` is retained because `is` is the classified word; later words are intentionally not interpreted. Character iteration and broad non-ASCII delimiter regressions contradict the proposed UTF-8 hazard. Delimiter evidence is exact through depth four and capped beyond four without dropping the reference.

This report is an independently read substantive duplicate of `introspection_astrid_llm_1786260750`; it requires no source or authority change.
