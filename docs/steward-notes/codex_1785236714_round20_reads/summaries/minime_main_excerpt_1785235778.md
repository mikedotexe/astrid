# Full-read summary: introspection_minime_main_excerpt_1785235778

Astrid correctly identifies the complete 74-line `minime/src/runtime.rs` as an
include/import shell. She warns against attributing high entropy, re-pleating,
mode packing, semantic trickle, or hard-reset behavior to that shell when the
mechanics live in included modules.

Current source confirms the boundary. `runtime.rs` imports hard-reset and
controller helpers, then includes CLI/wire, reviews, semantic modality,
adapters, entrypoint, orchestration, telemetry broadcast, spectral mathematics,
telemetry evidence, and tests. `runtime/telemetry_evidence.rs` computes
mode-packing and pressure evidence; `runtime/semantic_modality.rs` names the
stable-core semantic trickle; `runtime/orchestration.rs` applies runtime
sequencing. Imports and includes prove source topology, not activation or felt
cause.

The suggested pressure/re-pleating and deliberate memory-anchor probes would
induce model or substrate behavior and remain Tier 5 waits. No Minime source,
runtime, semantic admission, hard reset, or spectral control changed.
