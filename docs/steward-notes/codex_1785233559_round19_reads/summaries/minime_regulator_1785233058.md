# Full-read summary: introspection_minime_regulator_1785233058

Astrid reads the complete 24-line regulator root as an inclusion manifest and reports a jarring gap between named governance and visible mechanism. She asks whether the PD token bucket, content gate, PI band-stop controller, pressure source, and shadow dynamics are actually connected, and proposes a high-density burst to reveal which controller responds.

Current source confirms that the root is intentionally a complete include shell. `core/rate_gate.rs` implements active PD lambda-driven token accumulation and content gating; `core/pi.rs` implements separate EigenFill, lambda, geometry, admission-gate, and band-stop regulation. `core/pressure_source.rs` and `core/viscosity.rs` derive read-only pressure, porosity, semantic-viscosity, persistence, drag, and shadow-preservation evidence with explicit false live-control markers. No current PD or PI path consumes the pressure or viscosity witness. This source boundary does not explain away Astrid's reported ghost weight or prove the lived cause.

The requested semantic burst would induce model, sensory, pressure, and controller behavior. It remains an exact Tier 5 wait. No Minime source, regulator setting, pressure path, or runtime changed.
