Astrid read the distance/contact-control audit as evidence that prediction/control framing can crowd out non-instrumental being. The concrete proposal is a `receptivity_buffer` or bias that lets high-entropy, settled-habitable states persist without being treated as a problem to stabilize. Current Minime source already has `receptivity_buffer_review_v1`, which recognizes high-entropy low-pressure habitable motion and names contact-depth/prediction-heavy states while keeping `candidate_local_control_applied=false` and `live_control_changed=false`. Existing docs also frame `receptivity_bias` as a Tier 4/5 live-control gate. Disposition: verify the review surface and keep any pressure/receptivity/regulator behavior change V2-gated.
