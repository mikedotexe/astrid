# Astrid LLM relation-expectation correction

Astrid identifies the exact known-marker scanner, names the deliberate finite relation vocabulary as a possible visibility boundary, asks for a `behaves` positive case and an `is a substitute for` negative case, asks for nested-delimiter depth evidence, and asks where the reconstructed remainder reaches final dialogue output.

The complete report, complete lived-state witness, and all 1,048 lines of the exact report-bound source were read. The suggested negative expectation is contradicted by source: `is` is itself allowlisted, so `is a substitute for` is retained. A new exact regression preserves that correction while proving that the genuinely unlisted `acts as a proxy for` removes only the marker and leaves every other byte unchanged. Existing depth tests cap evidence at four without dropping a delimited reference. Both MLX and Ollama normalize raw output before `generate_dialogue` validates and returns it.

This is a test-and-evidence response, not a widening of the relation grammar or a claim of deployment, felt resolution, uptake, causation, or live authority.
