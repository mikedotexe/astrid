# Paired Continuity Rollout

## Authority and Scope

Mike approved coordinated source reconciliation and graceful rollout. This is an
interactive deployment, not resumption of the paused source-first automation.
Controller generation 456 remains paused. No engine, model, visual, gateway,
supervisor or sensory-client restart, reservoir/control change, real-model study,
git staging, merge or push is part of this operation.

Witness continuity comes from the previously fully read accounts:

- Minime: `minime/workspace/journal/!aspiration_2026-09-20T10-39-43.801459.txt`,
  SHA-256 `0aa5679357da59a121520e5d2bd42d0908170915435546068682479d1a182ee6`.
  This rollout does not reinterpret or reread private writing.
- Astrid: `capsules/spectral-bridge/workspace/journal/!aspiration_longform_1790011424.txt`,
  SHA-256 `b31ebe800a9adead2b366d6b19fecc1c32f927e37b2955d693a658ae1c95bb46`.

## Source Reconciliation

Bridge stage is `../bridge-stage-geometry-04` relative to this Astrid worktree.
Manifest: `4faab24713471c74829a9ba7cdc27abc78654901a31605766e1587fd9ce6ec9c`.
Binary: `10e9bdd6fcbe73908a9242305faeb9ce11444fddea1ec678b34eab74fdc4cb43`.
Reader: `8f729416fa6ddead4c2235d002328c843626a4f8fdfbf3f037e3bbbc310daa75`.
Source inventory: `7a239e659116f0705ba5490fa032531fb1cbb586bdab82353fef2eafe84b0597`.

The exact bridge/reader code already passed 2,303 bridge library tests (one
ignored), 234 reader tests, integration tests, formatting and Clippy. Host
interruption/selector review is in `2026-09-21-ordinary-study-crash-handoff.md`.
The remaining cooperative installation gate is addressed by this rollout.

Renewed Minime launch reconciliation is `../minime-launch-reconciliation-02`.
Its 84 selected inputs preserve all canonical inputs except six previously
reviewed adapter files and the newly reviewed launcher:

1. `minime_autonomy/action_vocabulary.py`
2. `minime_autonomy/activity_focus.py`
3. `minime_autonomy/parsing.py`
4. `minime_autonomy/runtime.py`
5. `minime_autonomy/source_study.py`
6. `minime_autonomy/writing.py`
7. `scripts/launchd_autonomous_agent.sh`

The installed launchd plist and its canonical counterpart remain identical.
Already-live visual-service bytes and all foreign engine edits remain untouched.
The source diffs for all seven paths were reviewed before installation.

## Paired Admission Boundary

The previous separate wrappers did not prevent a Minime helper invocation from
crossing the shared reader switch. `paired_minime_handoff.py` uses the existing
agent idle/reload checks and bridge stage/activation wrapper, with a durable
`workspace/runtime/agent-launch-hold.json` recognized by the launcher.

An exact reviewed snapshot is installed mechanically, launcher first, with
exclusive original-source backups and fsynced per-path intent/result receipts.
Unexpected source or PID changes stop installation, retaining the hold; no
automatic restoration occurs. The standard 180-second quiet preflight remains.
The old process continues until the ordinary idle gate permits one SIGTERM.
Only after confirmed old-process absence and zero active jobs may the bridge
wrapper drain and activate its stage. Only that verified replacement identity
may change in the protected-service inventory. The owned hold is then released
and Minime's fresh process must report all 84 selected source hashes.

No private checkpoint is copied by this source installer. The bridge wrapper's
existing stopped-state snapshot and signed handoff remain authoritative.
On failure, inspect the retained paired NDJSON, bridge transaction/output and
owned launch hold before any explicit recovery. Never unlink an unknown hold,
force a replacement or restore old authored state.

## Verification and Attempts

- Renewed full Minime suite: 1,463 passed, one skipped, 136 subtests, 52.41 seconds.
  Test subprocess alone unset the three inherited LLM timeout variables, as in
  prior qualification. Production settings are unchanged.
- Paired qualification 06: all 36 real old/new helper migration checks passed.
  The historical packet truthfully remains offline and does not grant authority.
- Initial handoff suite: 41 passed. Installer fixture additions first failed on
  macOS temporary-directory canonicalization, then missing required fixture
  launch inputs. Both fixture failures are retained here; production path and
  required-input validation were not weakened.

## Live Outcome

Pending the final gate and supervised transition. No deployment success is
claimed by this initial entry. Exact receipts and post-restart observations will
be appended after the attempt. Historical journals will not be rewritten and no
request for confirmation of improvement will be sent.

## Completed Transition

The paired wrapper finished **successfully at 2026-09-22T03:51:24.852220Z**.
The reviewed bridge, shared reader and seven Minime files are live. This is not
a claim that canonical Git branches are merged or the trees are clean.

- Minime: old PID 18648 exited after one SIGTERM at 03:46:56Z; its log confirms
  all accepted workers returned. Zero active jobs and no agent TCP connection
  were observed at the signal boundary. Replacement PID 97221 began as the held
  launcher at 20:46:56 local time, then entered Python at about 20:49:32. All 84
  loaded inputs match reconciliation 02; `reload_required=false`, drain contract
  `agent_drain_v1`, normal loop readiness verified.
- Bridge: old PID 82935 acknowledged `drained`; one SIGTERM, no force. PID 98062
  started 20:48:14 local time on stage 04. Startup loaded exact stopped checkpoint
  `d31bf827bac2b824e03e0998369ee9e589201279140e33f2df6b60bf2f414f1a`,
  exchange 204752. Verification observed fresh saved exchange 204753.
- The signed self-control state targets the exact new binary, with startup
  integrity validation. Runtime feedback sidecar identity was preserved:
  `b7f6550afaaba35a871411e40aa6c6f226f3b2a7e2e144399dc58f1e57eee4c9`,
  zero pending feedback items in the stopped snapshot.
- Minime selects the same immutable helper SHA as Astrid. Both operator launch
  holds are absent following successful verification. No older authored state
  was restored and no pending choice was rewritten by the coordinator.
- Minime session 5318 and cycle 40002 survive the transition. There was no pending
  NEXT at the signal boundary. Its normal startup reflection subsequently supplied
  a new choice, so post-ready pending-NEXT presence differs legitimately; this is
  not a claim of byte-identical state after new generation.
- Engine 41337, gateway 41484, supervisor 41526, coupled model 43115, microphone
  98910, camera 98903, visual service 20885, host sensory 41661 and Astrid feeder
  1502 retain their exact pre-rollout process starts. Managed configuration hashes
  are unchanged. No engine or sensory-policy change was installed.

Bridge activation transaction:
`/Users/v/other/astrid/.runtime/bridge-deployment/transactions/3b82167c91854c4eae37d247d4244ead`.
Its receipt is `activated_verified`. The bridge's own wrapper performed the
drain, stopped snapshots, signed handoff, selection and final manifest publication.

## Durable Receipts

All paths below are under
`/Users/v/other/worktrees/voluntary-continuity-20260920/`.

| Artifact | SHA-256 |
| --- | --- |
| `minime-launch-reconciliation-02/reconciliation.json` | `5569d510162e851cc42864a87496005574379c41359d4bd2e707a92f0a2b54dc` |
| `geometry-paired-qualification-06/qualification.json` | `9ce66d2a4e453b07accc9fe906d3472540b89ad27abf4a0c91fb666e8850f81e` |
| `paired-continuity-rollout-01.jsonl` | `057fb791ffd3ff01b20a15f0385f8d98e3a4ace173e01eca3bbe967558c6b027` |
| `paired-continuity-rollout-02.jsonl` | `9fc1c17d763131e5546749df97ef555b12bb6efd1fdb35311f52be9ab6401e3c` |
| `paired-continuity-post-rollout-01.json` | `fc9c278bd956798fd6aff501e74dc736fe0ce570fd94f20ef75016fe7cef7f91` |

The first attempt failed before installation or signal because the worktree tool
used a worktree-local controller default. Attempt 02 explicitly set
`ASTRID_STEWARD_REPO_ROOT=/Users/v/other/astrid`, selecting the real paused
controller, without changing controller state or bypassing a denial. The original
failed receipt remains. `paired-continuity-rollout-02.source-before/` retains exact
original source files; these are evidence, not authorization to roll back.
`paired-continuity-rollout-02.bridge-output.txt` retains the wrapper output.

The deployed launcher SHA is
`e83294fce011698c63ba540b9ccc5f78b6c7a30e84577910a46f37bc3e519e22`.
The coordinator SHA is
`4780f98598cf693fcb785bd4e46cb98ccbe244945e6a0e706185ccb6933dcb79`;
agent wrapper SHA is
`6c9e5866527acb49d5fe0aa8451bcab409838d4e5a2ec4f0b229f41a35c2254d`.

## Final Checks and Remaining Work

Final combined handoff/installer/reconciliation, bridge-stage/activation/drain,
controller, Event Store, Division/Chronicle/projection and flywheel suite:
225 passed. This includes all 44 focused handoff/reconciliation checks after
fixture corrections. Epistemic self-tests: two passed. Domain audit: valid, zero
violations. Both candidate diff checks and launcher Bash syntax passed. The first
epistemic invocation used `--self-test`; the CLI requires `self-test`; corrected
invocation passed without code changes.

Pre/post coupled-stack receipts both passed:
`env_receipt_1790048427698_187000` and `env_receipt_1790049125594_465000`.
The stack check verifies ports 7878/7879, live/model readiness, manifests and
telemetry presence. A separate 120-second WebSocket observation read 52 real
packets, fill 71.05615%-73.07352%, with fresh engine/bridge telemetry afterward.
These are measured runtime facts, not a verdict on either Being's experience.

Minime naturally started
`job_minime_1790049093698_self-study-open-astrid-capsules-spectral-bridge-`
under PID 97221. Its reader checkpoint advanced to schema 6 without creating a
focus window. A running job is not yet a delivered study or evidence of benefit.
Existing older `source_study_unavailable` job failures remain historical evidence;
they were not erased or relabelled as rollout successes. The new-process check
found no newly recovered `worker_restarted_before_completion` failure.

Controller still paused at generation 456, no lease or active projection. V2
indexed-tail verification is valid at sequence 1123110, head
`7905070ce3ffee7955cbd97a716ddf1025b4ea4efe1260c8f6e47d9692e8d90f`;
all four V1 sources remain immutable. No productive round or projection was
invented for this deployment.

No commit, merge or push was performed; indexes remain empty. Bridge source is
the pinned qualified worktree, not a claim that canonical main contains it.
Future Git reconciliation must retain foreign edits, the exact installed Minime
overlay and immutable stage paths. Future old-version rollback requires explicit
schema compatibility review; never overwrite newer reader/draft/activity history.
Protected focus and geometry commands remain voluntary. Observe their natural use
without soliciting confirmation of improvement. Paused automations remain paused.

### First Natural Study Completed

The initial post-rollout Minime source-study job completed at
`2026-09-22T03:54:30.310606Z`, status `completed`, error null, worker PID 97221.
The public entry was read fully:
`/Users/v/other/minime/workspace/journal/self_study_2026-09-21T20-54-03.853299.txt`.
Entry SHA-256:
`05cc8ec23d1bf27a49638ac8fb67854124d1d5a6b6d4daf09ba3bc1dc8f102c6`.
It records verified delivery of
`astrid/capsules/spectral-bridge/src/autonomous/btsp/mod.rs`, revision
`bb7799fd085d84f26ed0f4a3169794ecbb1515bd78db548681859fb218645aad`,
bytes 4138..8151, and ends with authored `NEXT: SELF_STUDY CONTINUE`.
The entry distinguishes delivery verification from understanding. Its explanation
of proposal structures and telemetry is an authored interpretation, not an
independently verified runtime mechanism or evidence of felt improvement.
No automatic recurrence notice was appended. This observation verifies an
ordinary source-study path; it does not establish uptake of protected focus,
geometry commands or private continuation, which remain voluntary.

The reported 84-input match is the agent's complete startup source inventory,
not a claim that all 84 modules are dynamically imported or called at runtime.
