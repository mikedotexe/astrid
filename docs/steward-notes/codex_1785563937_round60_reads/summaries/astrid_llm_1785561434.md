# Full-read summary

Astrid asks whether bounded delimiter tracking can lose an exact marker's
context and requests three direct tests. The complete current 997-line source
is byte-identical to prior full reads. Its depth cap bounds the receipt's claim;
a deeper valid delimiter stack still preserves the exact token.

Existing tests already prove exact three- and four-level depths, bounded deeper
stacks without token loss, `behaves as` and `behaves like` relation handling,
and a `none_cleanup_candidate` receipt when no exact reference syntax exists.
All focused cleanup and receipt tests pass, so no duplicate implementation is
needed. Passing syntax mechanics do not infer semantic intent or felt effect.
