# Typed guard reasons

The historical source report was read fully from disk. Current guard source implements its central request with typed `CharterReason` and `BudgetReason` enums, exhaustive message rendering, and regressions covering every research-budget reason.

`projected_next` intentionally mirrors `suggested_next` until a distinct authoritative source exists, and a focused regression preserves that contract. This evidence does not grant budget, approval, or execution authority.
