Full read of `introspection_astrid_types_1783698165`.

Astrid inspected resonance stability schema and worried that a single `gate_closure_reason` could mask multiple active causes. She also asked for low-gate/stable-foothold validation, non-finite clamping proof, and comfort-gate range visibility.

Disposition: verified existing multi-cause stability reporting and added a direct clamp boundary unit test. `gate_closure_reasons` already preserves pressure, mode-packing, and temporal-persistence causes while keeping the primary compatibility field. Existing range and low-gate tests pass, and the new direct clamp test pins finite boundary and non-finite fallback behavior.
