Full read: Astrid inspected the LLM voice/fallback scaffolding and named the MLX lane, Ollama fallback, `GEMMA4_LANGUAGE_CONTRACT`, and `FALLBACK_SHADOW_TEXTURE_TERMS` as real voice-continuity supports. She flagged the high-entropy fallback boundary as a place where small-model compatibility can flatten texture, proposed time-to-first-token and high-entropy Ollama fallback probes, and suggested new texture anchors plus a pressure-buffer sampler retune. This run implemented only the non-live diagnostic-language portion by adding `multi-modal-drag` and `dimensional-shear` as accepted texture/process terms; sampler/provider/pressure-buffer changes remain V2 authority-gated.
