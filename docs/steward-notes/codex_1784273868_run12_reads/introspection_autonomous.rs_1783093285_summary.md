# Full Read Summary

This neighboring report reinforces the same loader failure mode from the
nested-schema angle. Current code does not flatten a malformed or partial
weather object into an empty success: it records the missing or drifted path,
tries the bounded inertia fallback, and marks the source used. Relational
friction remains an interpretive context line with no instruction or control
authority.
