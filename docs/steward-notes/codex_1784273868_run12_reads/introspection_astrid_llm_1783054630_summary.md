# Full Read Summary

Astrid objects to coarse fallback buckets that can turn gradients into labels.
Current fallback rendering uses weighted continuous entropy, density, pressure,
porosity, viscosity, motion, and trajectory evidence, then records the
distribution and chosen descriptors. Existing regressions cover restless and
muffled regions while preserving provider provenance and no routing authority.
