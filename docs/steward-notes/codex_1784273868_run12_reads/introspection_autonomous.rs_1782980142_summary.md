# Full Read Summary

Astrid asks that Witness resilience not hide malformed relational data and that
text truncation never tear a multi-byte character. Current source records
missing and malformed schema paths, preserves uncategorized tension as such,
tracks temporal persistence without overriding classification, and truncates
only on valid UTF-8 boundaries with focused four-byte tests.
