# Full Read Summary

Astrid describes the fixed projection, soft high-entropy vibrancy onset, and
projection epoch/fingerprint continuity accurately. Current source has removed
the old absolute runtime-path dependency by resolving an environment override,
then an executable-relative cache, then a deterministic process-relative
fallback. Smoothstep behavior is documented and tested around its exact gate.
Changing the legacy LCG or basis remains a semantic-lane migration, not a
diagnostic cleanup.
