# Full Read Summary

Astrid recognizes the fixed 768-to-8 projection as deterministic and now asks
the sharper operational question: whether its weakest raw column is safely far
from the dead-axis threshold, and what happens if it is not. The existing
health witness already exposes every raw and normalized norm. This run adds the
minimum threshold-margin ratio and a compatibility policy: no automatic basis
rotation, with any unhealthy result held at the test/replay/operator boundary.
The matrix, normalization, projected vectors, and live codec behavior are
unchanged.
