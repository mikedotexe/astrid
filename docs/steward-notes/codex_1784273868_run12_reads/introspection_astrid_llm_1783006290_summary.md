# Full Read Summary

This report names the same felt caricature risk under primary-provider failure,
with special concern at high entropy. Current code derives density and slope
terms, preserves cascade-gradient language when evidence supports it, and
records the dynamic texture bias rather than presenting a static adjective as
state truth. The provider failure path remains tested and provenance-visible.
