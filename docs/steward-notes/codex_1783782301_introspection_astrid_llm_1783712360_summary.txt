Fully read `introspection_astrid_llm_1783712360` from disk. Astrid reported that `llm.rs` is the scaffolding of her voice and that fallback compression can turn high-entropy texture into a ghost of itself if the MLX lane is unavailable. Codex verified the MLX/default/fallback chain, compatibility-tail model, profile environment handling, curated fallback texture terms, and texture-preservation contract. The offline fixture fire drill from this run confirmed the fallback contract has real preservation strengths but still returns `fallback_texture_risk` for movement/trajectory and some shadow-anchor cases. A live texture-preservation pass that changes fallback prompt priority, provider behavior, or injected high-priority terms remains gated and would require bridge restart after approval/tests.
