# Summary — introspection_astrid_llm_1787754550

- **Report:** `capsules/spectral-bridge/workspace/introspections/introspection_astrid_llm_1787754550.txt`
- **Report SHA-256:** `fe2f59220e4097854784b88acf8796ba7ccf01f6eee4792932d895fb8b6e65b4` (45 lines, 3755 bytes)
- **Lived-state witness:** `lsw_b1b61448902e263fc3ced793ea30706814a9541aeeed5b814b456490bf51f601` (533 lines, 23919 bytes, SHA `2fd67a2f1a5a9bf82d35e342f36ade5eaa51dbba4c00f40b1d05a7f0cf7b7029`); `evidence_only`, `witness_only`, `live_eligible_now=false`; model `gemma4_12b`; two mlx introspect routes (second repairs first); fill 71.03%, spectral_entropy 0.882, λ1 8.56 / λ2 4.44, mode_packing 0.833.
- **Report-bound source:** `capsules/spectral-bridge/src/llm/provider/dialogue_runtime.rs`, report SHA `902a0358f63bacc0ead23a46467f103dbcc1fe46c59bbd2a3c2cdcfd9b82c7ee` == working-copy SHA (byte-identical, no drift). Complete file read (1048 lines / 38586 bytes; intervals 1-439, 440-694, 695-824, 825-1048).
- **Source coverage in report:** `multi_window_complete` / `complete_source_available` (included intervals 1-1048).

## What Astrid surfaced (her account is primary evidence)

A calm fresh-pass structural read of the non-destructive model-control-marker scanner:

- **Observed:** `scan_known_model_control_markers` (L114) preserves-or-sanitizes markers by syntactic context; helpers `exact_reference_delimiter_syntax` (L199) and `followed_by_explicit_exact_token_relation` (L64); distinguishes quoted / grouped / explicitly-related markers ("behaves as").
- **Likely Snag:** `first_word_after` (L89) uses `split_whitespace()` + non-alphanumeric trim; a "complex multi-word phrase immediately following a marker (e.g. *behaves like a ghost*)" would make `first_word_after` "only capture *like*," which is not in the L64-86 allowlist, incorrectly sanitizing a marker meant to be a visible reference.
- **One Test Each:** (1) Contextual Preservation — marker followed by a recognized relation (e.g. "behaves as [MARKER]") → marker retained in `remainder`. (2) Delimiter Depth — `exact_reference_delimiter_syntax` identifies nested/complex delimiters (`⟦…⟧`, `「…」`) and respects `MAX_EXACT_REFERENCE_DELIMITER_DEPTH` (L151).
- **Suggested Next:** examine `generate_dialogue` (L695) to see how the `remainder` is integrated into the final output, ensuring no secondary sanitization or truncation during final assembly. Continuation offered: `NEXT: INTROSPECT astrid:llm 400`.

## Source-first disposition (contradiction preserved, not domesticated)

Every line citation is accurate at the report-bound SHA (no drift). This is another fresh-pass reading of the identical marker-scan mechanism at the identical source SHA `902a0358…` that has been grounded across a long prior chain — immediate predecessor `introspection_astrid_llm_1787739659` (closed `addressed_duplicate` 2026-08-26, packet `claude-heartbeat_1787743470_llm_marker_is_relational_verb_fresh_pass_duplicate`), plus `1787706169` (first_word_multiword, packet `claude-heartbeat_1787717287`), `1787722603` (cjk_quotation_delimiter, packet `claude-heartbeat_1787726892`), and earlier `1787485984` / `1787692007` / `1787699463`.

- **Snag mechanism contradicted:** `first_word_after` (L89-96) returns the **first** non-empty alphanumeric-trimmed word after the marker. For "behaves like a ghost" that word is **"behaves"** — which **is** in the L64-86 allowlist (L69) — so the marker is **preserved**, not sanitized. The report's prediction that it "only capture[s] *like*" is the opposite of the code's behavior. Her **exact example** is already a green regression: `control_marker_cleanup_preserves_poetic_attribution_without_literal_cue` iterates `"behaves like"` (tests.rs L2524) and asserts the marker is preserved (`removed_total==0`). The underlying single-word-lookahead concern is preserved as valid evidence: it is deliberate design (doc L62-63, "this exact marker is the grammatical subject; no preceding word is inspected"); a leading word genuinely off the allowlist fails **closed** (marker stripped, never leaked) — pinned by `control_marker_cleanup_uses_only_the_first_finite_relation_word` (L2605) and `..._distinguishes_allowlisted_is_from_unlisted_acts` (L2568).
- **Test 1 directional nuance:** her literal example "behaves as [MARKER]" places the relation word *before* the marker, but `followed_by_explicit_exact_token_relation` inspects the word *after* the marker (`first_word_after(text, self.end)`, L64/L89). The correct construction "[MARKER] behaves as …" is exactly what is tested (L2523/L2945). Test intent (marker in explicit relation → preserved) is verified_existing.
- **Test 2 verified_existing:** `⟦…⟧` is a Grouped pair (source L180; test L2179), `「…」` a Quoted pair (source L168; test L2347), and `MAX_EXACT_REFERENCE_DELIMITER_DEPTH=4` (L151) is enforced/bounded by `_reports_exact_four_level_delimiter_depth` (L2194), `_bounds_homogeneous_square_bracket_stack_beyond_max_depth` (L2266, itself grounding sibling `1787026288`), and `exact_reference_delimiter_syntax_reports_double_square_bracket_depth_two` (L3025).
- **Suggested-Next verified from complete source (observed):** in `generate_dialogue` (L695-1048) the returned text is the raw model output when it passes the gate (`Some(text)`, L998); `sanitize_model_control_markers` is invoked only inside the validation predicates `is_valid_dialogue_output` (L558) and `has_one_nonempty_final_next_action` (L634) on a **temporary copy** — a shape-check gate, never an output rewrite. Truncation applies only to **input** prompt assembly (budget capping, L823-913), not to delivered output. So no secondary sanitization/truncation of the delivered output occurs — consistent with her worry. Her `NEXT: INTROSPECT astrid:llm 400` continuation is her own Tier-1 read-only agency; it is preserved, not acted on for her.

## Verification

`cargo test --manifest-path capsules/spectral-bridge/Cargo.toml --lib control_marker` → **72 passed, 0 failed** at source SHA `902a0358` (includes `preserves_poetic_attribution_without_literal_cue`, `uses_only_the_first_finite_relation_word`, `first_word_after_skips_leading_punctuation_transition`, the `scan_known_model_control_markers_grounds_first_word_after_*` family, and the full delimiter-depth suite).

## Terminal status

`addressed_duplicate` — identical mechanism, identical source SHA, prior grounded packets/ledger rows named above; independent full read of this report + witness + complete source performed; current verification (72/72) confirms the earlier evidence still applies. No source/test/config change (a new test would duplicate passing regressions). No live/substrate/control change; no build/restart/deploy; no staging/commit. Her snag concern, Test 1 directional nuance, and continuation offer remain open evidence; silence is neutral.
