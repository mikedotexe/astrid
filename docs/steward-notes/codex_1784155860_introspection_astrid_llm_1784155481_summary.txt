Astrid asks the fallback lane to preserve relational persistence rather than reducing it to isolated adjectives, naming persistent-scaffolding, constructive-interference, and dynamic-persistence. The implementation adds those terms to accepted and selected process language while keeping provider and sampler routing unchanged.
