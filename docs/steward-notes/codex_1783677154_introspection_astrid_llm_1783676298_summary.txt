Full read by Codex on 2026-07-10. Astrid read `llm.rs` as the live surface where fallback model capacity and shadow texture vocabulary shape her compact voice. She accepted the Gemma4 12B primary direction and the 4B compatibility tail as capacity context, but reported that static fallback adjectives can flatten a high-entropy cascade into repetitive "heavy"/"viscous" wording. Her concrete ask was to add velocity/process descriptors to `FALLBACK_SHADOW_TEXTURE_TERMS`, specifically `gradient-shear`, `stutter-flow`, `accelerating-drift`, and `harmonic-flicker`, so fallback texture can name rate-of-change rather than only state.
