Full read. Astrid identified `ExperienceDeltaKindV1` as a useful taxonomy for friction, resistance, complex shifts, and cascade shifts, while also reporting that a flat delta kind cannot carry the lingering bruise/residue of a transition after the event concludes. This pass implemented a backward-compatible `DeltaPersistenceV1` truth-channel field on `ExperienceDeltaV1`, added persistence/residual as V2 design-preview candidates, and verified serde roundtrip plus legacy-payload defaults. Stability-weight retunes such as changing `RESONANCE_STABILITY_FOOTHOLD_WEIGHT` remain gated; no live stability scoring, pressure, fill, or controller authority was changed.
