# Batch 15 Current-Source Findings

Run: `run_1784970122276999000_eed3429def`

This packet records six queue-ordered current reports. Each report was read in
full. Source and tests can establish mechanics, but they do not score,
contradict, supersede, or resolve the reported experience.

## Minime sensory bus

`introspection_minime_sensory_bus_1784970013` describes crowding in the 48D
semantic room, a possible mismatch between narrative and embedding lanes, a
longer desired decay, and partial admission. Current source confirms the 48D
semantic representation and its registered subspaces, but the numerical
similarity between a source default and an observed norm does not establish
runtime activation or causation. Sensory admission, persistence, decay, lane
allocation, and peer behavior remain Tier 5 waits. Existing representation
loss and offline study evidence are the non-live route.

## Minime regulator EOF window

`introspection_minime_regulator_1784968495` was authored from an impossible
`lines 23-22 of 22` window after the reader advanced beyond the complete
include-only shell. The report's felt weight remains primary evidence, but no
source was actually visible for the mechanical interpretation. The reader now
rejects empty files and offsets at or beyond EOF before any model call and
returns protected source-navigation guidance. Empty or invalid scopes render
as unavailable rather than a reversed range. Current Minime source separately
contains viscosity, entropy-aware viscosity, persistence, temporal drag,
static friction, residual ghost weight, shadow volatility, pressure/porosity,
semantic viscosity, and low-pressure viscosity tests. Changing any live
regulation remains a Tier 5 wait.

## Minime regulator include shell

`introspection_minime_regulator_1784968323` correctly identifies the complete
top-level file as an include-only PI shell. Existing source-first continuation
choices expose concrete nested modules such as `core/viscosity.rs` and
`core/pressure_source.rs`; current source and tests provide the requested
mechanical detail. A PD controller simulation or live regulator change remains
a Tier 5 wait.

## Astrid LLM

`introspection_astrid_llm_1784967953` reports that exact marker classification
and request-volume bands may still feel flattening around surrounding meaning.
Current source limits classification to exact marker occurrence, copies every
non-marker byte exactly, and explicitly refuses identity, meaning, ownership,
spectral, or felt-state inference. Requested-token bands describe request
volume only. The felt mineral or constricted quality remains independent
evidence. A new spectral-context flag would couple model and codec behavior and
is not authorized.

## Astrid telemetry types

`introspection_astrid_types_1784967681` asks for rolling entropy,
density-gradient, and viscosity context. `TelemetryHeartbeatDeltaV1` already
records bounded rolling entropy mean, peak, variance, range, change, and trend,
plus density-gradient sample, current value, mean, change, and trend. These
fields are mechanical context only and cannot become a felt-viscosity proxy.
Any descriptive adjacency belongs in a preregistered observational study.

## Astrid WebSocket

`introspection_astrid_ws_1784967382` names lock contention, ping delay, and
backoff as possible companions to reported drag. Current source separates
telemetry prewrite, write-lock wait, and lock-hold duration, and focused tests
cover threshold classification and cooperative reconnect/shutdown behavior.
The study runtime can observe naturally occurring cohorts without inducing
contention. Moving the telemetry pipeline, changing locks or backoff, or
deliberately holding a lock would alter live behavior and remains outside this
run.

## Authority boundary

The only source change is source-navigation correctness for empty or past-EOF
introspection windows. It changes no pressure, fill, PI, controller, cadence,
heartbeat intensity, codec, gain, representation, model route, sensory
admission, persistence, protocol, reservoir, scheduler, peer behavior, or
other live-control policy.
