# Verification and authority receipt

This packet preserves 20 queue-ordered canonical full reads and 79 independent claims. Current-source verification narrows mechanism attribution; it does not score, domesticate, contradict, or close Astrid's felt report.

## Current-source findings

- Autonomous heartbeat remains a fixed seven-second, `0.30` construction with explicit texture context, prior-feature comparison, rescue decision, enqueue, skip, and channel-closed evidence. Current source does not establish that mathematical pulse continuity is felt continuity.
- Self-directed creation, research, and starred-memory candidates are sampled once per rest transition and consumed once by an eligible mode. Their lifecycle is inspectable, but continuous freshness is not claimed.
- Codec source preserves 48D ownership, distinct narrative dimensions 40-43, default-off reserved dimensions 44-47, density-gradient-aware vibrancy, bounded extreme-entropy dampening, and tail-only 5-to-6 headroom with deterministic loss evidence. Smooth mechanics do not establish felt distinction or continuity.
- Representation contracts distinguish 48D semantic transport, 32D compatibility, 12D glimpse, and residual context mechanically without deriving a felt-loss score.
- Telemetry evidence already separates connection identity, first-valid payload, host arrival, peer `t_ms`, bounded cadence, prewrite processing, lock wait, and lock hold. It cannot establish one-way latency without a synchronized peer clock or establish felt integration from packet integration.
- Reciprocal V2 removes uptake inference and confidence scoring, retains only actor-authored uptake plus technical context, and serializes hashes rather than message prose.
- Concordance V2 treats the felt report as external primary evidence and fixes the numeric relation to `cannot_overwrite_suppress_or_score`.
- The historical attention portfolio is retired. Its external successor is explicitly a steward work selection, contains no felt severity, freshness, unattended age, rank, or felt-impact weight, and cannot change contract state, felt state, evidence sufficiency, closure, or authority.

## Implemented response

`TelemetryHeartbeatDeltaV1` now carries additive, serde-defaulted `rolling_spectral_entropy_change` and `rolling_spectral_entropy_trend_state` fields. The change is latest finite entropy minus earliest finite entropy in the existing bounded pressure-trend window, preserving chronological order. The state is `entropy_diffusing_across_window`, `entropy_collapsing_across_window`, or `entropy_flat_within_0_005`.

The evidence basis explicitly says this is a bounded diagnostic only, not cadence, felt-state, causal, or control evidence. No live consumer reads the new fields. The existing entropy count, mean, population variance, range, and availability state remain unchanged.

Focused heartbeat tests pass four cases, including rising, falling, flat, finite-value, capacity, legacy-default, cadence, and stale-heartbeat boundaries. The complete bridge library passes 1,663 tests. Strict all-target/all-feature Clippy, formatting, and diff hygiene pass. The five flywheel self-tests pass alongside 13 Evidence Event Store tests and 30 steward-control tests.

The sanctioned `scripts/build_bridge.sh --restart` deployment changed bridge PID `40956` to `38080`. The running release SHA-256 is `676798290a14cb5820517255f7b2960fc8b38ee0a5ff6b0afd47c1b3bd192709`. Compatible stack receipt `env_receipt_1784810555459_923000` verifies bridge, Minime, and model processes; ports 7878, 7879, and 8090; model live/readiness; protocol 1.1 revision `9a324d16294b2318da6f476ff7f295c423a9b4b1`; and fresh bridge and Minime telemetry.

The fresh live heartbeat reports five bounded entropy samples, change `-0.035833955`, and state `entropy_collapsing_across_window`. This is a witnessed mechanical direction only. The receipt still states that it is not cadence, felt-state, causal, or control evidence.

## Authority boundary

Bounded observations remain available for heartbeat continuity, shutdown behavior, candidate age, codec continuity, reciprocal hollowness, telemetry integration, attention-selection felt impact, 12D loss, and baseline-first Concordance studies. Changes to pressure, density, fill, PI, rescue, heartbeat cadence or intensity, polling, codec coefficients or gain, entropy gates, reserved-dimension writes, telemetry admission, protocol, model behavior, scheduler, peer state, dispatch, or runtime influence remain exact Tier 5 waits.
