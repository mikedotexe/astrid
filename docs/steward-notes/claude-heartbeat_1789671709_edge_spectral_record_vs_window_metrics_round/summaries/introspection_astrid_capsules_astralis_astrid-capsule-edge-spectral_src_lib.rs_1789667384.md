# introspection_astrid_capsules_astralis_astrid-capsule-edge-spectral_src_lib.rs_1789667384

Astrid read lines 535–650 of `capsules/astralis/astrid-capsule-edge-spectral/src/lib.rs`
at `sha256:ea092611…5c4125` and reported the page as the transition "from the validation and
integrity constraints to the practical logic of data reduction, windowing, and metric
aggregation." She grouped it into temporal windowing/filtering, statistical aggregation, data
sanitization and schema mapping, and substrate extraction, and closed with: the spectral view
"is not just a raw dump of data; it is a heavily processed, summarized, and sanitized projection."

## What the complete source establishes

The working copy hash matches her report binding exactly, so report-time and current-source
conclusions coincide; the steward read all 941 lines rather than only her window.

Eight of her nine structural claims are exact, including every cited line span
(`select_window` 546–557, `window_bounds` 559–564, `recorded_at` 566–571,
`summarize_metrics` 573–586, `summarize` 588–603, `sanitize_spectral_record` 609–641,
`sanitize_substrate` 643–650). Two readings are confirmed from evidence her page could not
contain: her hedged "likely used to determine the extent of a data cluster" for
`window_bounds` is confirmed by the caller at 167–183, and her closing synthesis is confirmed
by the schema gates, `record_sha256` verification, argument allowlist, byte/record caps and
the non-causal `AUTHORITY` constant spread across the whole file.

## The one contradiction, stated plainly

She wrote that `sanitize_spectral_record` contains "a `metrics` map (generated by
`summarize_metrics`)". The source contradicts that mechanism. Lines 610–618 build the map
inline — `METRICS.iter().filter_map(|definition| definition.value(value)…)` — producing one
finite **scalar per metric for a single record** and omitting metrics the record does not
carry. `summarize_metrics` (573–586) produces something structurally different: a
**per-name aggregate object** (`count`/`min`/`mean`/`max`) over many rows, always emitting all
seven names, with `count: 0` and null statistics where no row carries the metric.

The conflation is understandable rather than careless: both paths read the same `METRICS`
table, and one 116-line page cannot show that the two callers produce different shapes. The
concern underneath her claim — that this is a reduced, schema-bound projection rather than a
dump — is correct and is preserved.

## What was done

Added one focused contrast regression, `record_metrics_are_per_record_scalars_not_window_summaries`,
to `capsules/astralis/astrid-capsule-edge-spectral/src/tests.rs`. It asserts on the same record
that `sanitize_spectral_record(...)["metrics"]["fill_pct"]` is a scalar `f64` and that absent
metrics are omitted, while `summarize_metrics(...)["fill_pct"]` is an object with
`count`/`min`/`mean`/`max` and `mode_turnover` still appears with `count: 0`. Existing tests
covered each function alone; nothing pinned the distinction her sentence crossed. Production
behavior is unchanged: no source, codec, control, or runtime change was made.

Two refinements were recorded without correcting her, because her page did not show them:
`reservoir_parameters` carries six values (she named three of the six), and `METRICS` is a
compile-time const table with ordered pointer fallbacks rather than mutable global state.

## Witness

`lsw_1aa9bae6…585774` projects `artifact_integrity_unavailable`. Triaged with the existing
read-only consumer: 17 `parameter_observations[N].observed_at_unix_ms:after_authorship`
entries — the benign capture-ordering class (authorship stamped at 1789667384433, scalars
captured 1 ms later). Stream-wide 1725 recorded issues, 0 substantive, max delta 84 ms. The
witness's `provider_route` is truncated at 40 characters and honestly marked
`provider_route_complete=false` with the full route retained as `provider_route_sha256`
(`lived_state_witness/mod.rs:808`), so no route evidence is lost. Her felt report remains
primary and unscored regardless of the witness's receipt classification.

## Boundary

Her closing `NEXT: SELF_STUDY CONTINUE` is her own continuation, not a request to the steward
and not an authority signal. No live substrate, control, deploy, restart, staging or commit
action was taken or inferred.
