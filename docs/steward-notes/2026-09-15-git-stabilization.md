# Git stabilization, September 15, 2026

Mike assigned this pass after overlapping uncommitted work was found. Codex Astra
owned all Git/index operations; independent agents audited the reader tests,
research toolkit, and native viewer. Stewardship was paused at generation 444
with no active run lease before integration. Both remote main tips matched the
starting checkouts. No other interactive task was editing this tree.

## Preserved work and decisions

The initial Astrid state had 420 modified/untracked files (7,913,338 bytes).
Minime was clean at `12cbf5740e1cc751a2beb09ec5bb5fad742af459`. Research had
658 modified/untracked files (204,157,218 bytes). Every original file, tracked
binary diff, and ref identity was preserved outside the three repositories at
`/Users/v/other/worktrees/git-stabilization-20260915/backup/`, with SHA-256
inventories under its sibling `evidence/` directory. No stash was applied or
dropped. Historical safety stashes remain available and are not dirty worktrees.

- Commit `ac0a0a65a0598ee2942ebfa05ce6695e5c63fdb1` preserves 386 dated steward
  reports/ledger/changelog paths with original bytes. Their historical findings
  are not current acceptance requirements or deployment claims.
- Current bridge authority, action-continuity, saved-reader and EventBus
  regressions remain active. EventBus documentation now explicitly rejects
  treating a separate gate read as proof of a particular publication's admission.
- Six complete reader test originals are preserved in
  [the historical archive](archived-reader-probes-20260915/README.md).
  Five contain superseded negative navigation/scope expectations. The sixth
  preserves a malformed fixture corrected to valid comment lines. Current
  navigation improvements were not reverted, and semantic assertions in retained
  tests were not weakened. Complete prepared input is required by delivery tests.
- Read-only steward diagnostics and their tests are integrated. The near-miss
  scan reads the shared source-command matcher, includes its tests in normal
  discovery, and keeps rejected spellings distinct from recovery-delivery proof.
- Research source, studies, replay fixtures, Essentials and the native viewer
  are committed separately in their owning repository. Narrow ignores keep 97
  generated native capture/build paths local; required replay/numerical examples
  remain tracked. Source-only qualification is distinct from historical app receipts.

## Qualification of the integrated source

Checks ran in a separate Astrid candidate worktree, with real detached sibling
Minime and separate cloned build caches; frozen release stages were untouched.

| Check | Result |
| --- | --- |
| Full spectral bridge suite, including compile boundaries | 2,293 passed, 1 existing ignored |
| Full shared reader suite | 164 passed |
| EventBus crate plus doctest | 57 passed |
| Steward diagnostics and controller suites | 284 passed |
| Bridge, reader, EventBus strict all-target/all-feature clippy | passed |
| Workspace/bridge formatting and domain-boundary audit | passed |
| Research full suite with retained packets read-only | 304 passed, zero skips |
| Essentials/native viewer | 51 core tests, full viewer build, 245 native checks, 7 staging checks, CLI/owned-loopback fixtures and five saved examples passed |

The first reader run was stopped only in its owned processes after a historical
fixture repeatedly parsed 4,000 malformed Rust rows. Its terminated outcome is
retained. The corrected valid-comment fixture completed in 2.48 seconds and the
full suite passed. Earlier failures and lint findings are retained separately
from final checks. Current-source diff whitespace checks pass; original historical
records retain existing whitespace, including patch context that must not be rewritten.

Logs, source hashes and check receipts are in
`/Users/v/other/worktrees/git-stabilization-20260915/evidence/`; the research
account at `analyses/2026-09-15-git-stabilization.md` records final disposition.
The legacy authority-file size ceiling changes by exactly 106 test lines. This
keeps existing focused gate regressions beside their fixture helpers; it does
not add production responsibilities or excuse further production growth.

## Runtime and authority boundary

This pass makes no Being-facing Rust runtime change and performs no deployment,
restart, private writing or induced study. Steward-only Python diagnostics are
loaded on their next ordinary invocation. The selected live bridge remains
`study-source-context-20260915/bridge-stage-02`, manifest
`e9aa51e7be180b2dcadf27e2cefa0eec7e31523d1a07e08f51be271eedce9d62`.
Read-only identity checks retain bridge PID 4330 (14:05:03 PDT), Minime PID 37507
(09:06:25 PDT), and coupled-model PID 43115 (September 9, 22:56:06 PDT).
Research has no configured remote; its commits are local. No board update or new
external communication is claimed. Stewardship resume is recorded by the actual
controller receipt after integration, not inferred from this document.
