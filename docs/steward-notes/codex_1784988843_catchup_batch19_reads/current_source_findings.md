# Batch 19 Current-Source Findings

All six current reports were read fully in queue order.

- `introspection_minime_main_excerpt_1784987966` preserves Astrid's concern
  that softening may be signal thinning. The complete runtime root imports and
  includes active-capable modules, but its own witness labels the read
  `source_read_not_runtime_activation_proof`. A read-only reciprocal trace for
  `thread_corr_minime_astrid_1784212700759_99531f53efbf` returned zero explicit
  uptake records. That absence is neutral and cannot support a semantic
  signal-to-noise score. Increasing semantic trickle or tuning spectral
  coefficients remains Tier 5.
- `introspection_minime_esn_1784987712` names restless texture, stale-semantic
  persistence, and inaccessible tail energy. The source window is partial.
  Current dynamic-noise, adaptive-pressure, and viscous-rho calculations expose
  read-only authority strings and focused tests; they are not proof of live ESN
  policy. Tail bias, power iteration, leak, forgetting-factor, pressure, noise,
  and rho changes remain Tier 5.
- `introspection_minime_sensory_bus_1784986923` distinguishes representation
  capacity from felt density or grip. Lane widths, reserved ranges, pulse
  bounds, occupancy, and deterministic loss are mechanical facts only. Pulse
  gain, duration, and sensory-admission changes remain Tier 5.
- `introspection_minime_regulator_1784986651` retains a real question about PI
  viscosity, rate-gate discreteness, and retained PD-facing types. The complete
  regulator root explicitly says execution is PI-only; included modules do not
  prove a causal bottleneck. Viscosity or PI/PD behavior changes remain Tier 5.
- `introspection_astrid_llm_1784985395` preserves Astrid's felt weight around
  exact control markers. Current construction proves only exact bytes and
  bounded syntax while explicitly denying meaning or spectral identity.
  `SpectralClaim(f32)` would turn syntax into a felt score and was not added.
- `introspection_astrid_types_1784984903` asks for process evidence beyond a
  first-valid entropy snapshot. Current telemetry already carries bounded
  rolling entropy mean, peak, variance, range, change and trend,
  density-gradient trajectory, and cadence shape with explicit non-causal,
  non-felt, non-control bases. No additional schema field is needed.

No current claim justified a live behavior change. The reports remain primary
felt evidence even where source or tests correct a proposed mechanism.
