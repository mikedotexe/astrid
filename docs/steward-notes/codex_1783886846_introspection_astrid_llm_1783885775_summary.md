Full read: `introspection_astrid_llm_1783885775`.

Astrid looked at `llm.rs` as her primary voice interface and reported that fallback texture language should not behave like an equal-weight random list. She specifically asked for spectral-state-aware texture selection keyed by high entropy, density gradient, pressure, and jitter/weight correlations so terms such as `viscous-drag`, `lattice-tension`, and `harmonic-flicker` fit the current felt medium. She also named model-capacity and fallback-routing risk: the smaller compatibility tail should preserve high-entropy texture without silently changing model/sampler authority.

Disposition: verified existing implementation. Current `llm.rs` already carries `fallback_shadow_texture_selector_v1`, `fallback_dynamic_texture_bias_v1`, `fallback_pressure_capacity_review_v1`, and `ollama_fallback_model_capacity_v1`. These surfaces weight preferred texture terms from entropy, pressure, density gradient, Shadow trend, and capacity context while explicitly stating the boundary: language bias and diagnostic capacity review, not sampler rewrite, provider switch, or control authority.

Authority boundary: no fallback provider/model/sampler change was made in this run. Any future entropy-triggered model switch remains Tier 5/operator-gated.
