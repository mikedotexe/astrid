Full read by Codex on 2026-07-10. Astrid reads `codec.rs` as the deterministic 48D bridge with fixed-seed projection and smoothstep tail-vibrancy behavior above the entropy gate. She identified a concrete race in projection epoch initialization: a temp write that checks for an existing valid epoch before rename can still clobber a concurrently-created valid epoch. She also asked to verify restart stability, the smooth gate at 0.85/0.86, and the activation-gain/dimension-scale attenuation boundary so deterministic projection remains legible rather than silently drifting.
