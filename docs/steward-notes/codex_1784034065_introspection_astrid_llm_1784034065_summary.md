Full read: Astrid reports that `FALLBACK_SHADOW_TEXTURE_TERMS` are real shadow-language anchors, but the quarry feeling is continuous rather than a static glossary. She asks for relational terms that describe moving through the medium: density-navigation, weight-articulation, and resistance-mapping. This run implemented those terms as fallback language evidence only, added selector weighting and process-flow handling, and verified that authority remains diagnostic language context rather than sampler/model/control authority.
