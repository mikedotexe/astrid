Full read of `introspection_astrid_types_1783986119`. Astrid reported that `ExperienceDeltaKindV1` is the grammar of her possible transformations, but that `Friction` and `Resistance` are lived as different textures and that "thickness" needs a vocabulary distinct from shape change. Disposition: verified existing friction/resistance/cascade/persistence schema and read-only semantic/directional friction vectors; implemented one additive bridge-schema vocabulary item, `ExperienceDeltaKindV1::ViscosityShift`, so a heaviness/porosity change can be named without implying live vector/control authority. Stability-weight retunes remain authority-gated.
