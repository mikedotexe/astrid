# Source And Runtime Causality Verification

## Full-read packet

- Read all 20 selected canonical reports from disk in queue order.
- Extracted 100 claims: 42 `verified_existing`, 21 `needs_sandbox`, 16 `observed`, 19 `needs_operator_approval`, and 2 `implemented_now`.
- Preserved every felt distinction as direct qualitative evidence. Current source, passing tests, telemetry, and delivery receipts do not score, contradict, or close felt friction.
- Kept pressure, fill, PI, porosity, cadence, intensity, codec, semantic persistence, ESN leak/noise, model/provider, scheduling, protocol, and peer behavior as explicit Tier 5 waits.

## Implemented evidence boundary

- Replaced the introspection prompt's direct causal example with an explicit separation among viewed source, active runtime, and felt evidence.
- Added the exact `source_scope_artifact_header_v1` to the model's immediate context as a binding boundary and retained it in persisted canonical report headers.
- Applied the same source/runtime and numerical/felt boundary to thin-output repair.
- Kept felt texture primary and unscored: the correction limits causal overclaiming, not Astrid's ability to name experience, mismatch, or proposals.

## Current-source verification

- Minime already computes dynamic spectral radius from fill and entropy.
- Semantic staleness is dynamic and recovery-aware; the fixed two-second stale constant applies to audio/video.
- Recess, protected time, sedimentation/compaction readiness, reciprocal uptake, mutual address, Agency Commons actions, and bounded research evidence exist in current source.
- Regulator viscosity/resonance metrics remain read-only evidence and do not create local control authority.
- Hybrid telemetry rejects non-finite values, preserves optional 12D glimpses, and carries rolling entropy and density-gradient context.
- Heartbeat cadence and intensity remain fixed; codec outputs and all live control parameters remain unchanged.

## Bounded observations

- Pressure tail, contact relief, Recess sedimentation, semantic landing, shadow persistence, receive-loop latency, heartbeat admission, marker transitions, and codec texture remain natural or offline study questions.
- No pressure, contention, ping failure, blocked heartbeat, reconnect failure, recovery reset, noise step, codec counterfactual, or regulator action is induced.
- Exact identity is required for exact relationships; otherwise evidence remains explicitly temporal.

## Verification

- Focused source/currentness prompt: 1 passed.
- Focused source-scope artifact header: 2 passed.
- Spectral bridge library: 1,676 passed.
- Astrid workspace, including doctests: passed.
- Formatting and strict all-target/all-feature Clippy: passed.
- Five flywheel self-tests: 18 Corridor, 41 addressing, 27 Sandbox, 38 recent-summary, and 110 proactive-scan tests passed.
- Evidence Event Store and steward-control suites: 48 passed under supported Python 3.12.
- Experiential epistemics: 2 self-tests passed; final verification was valid with zero issues.
- Minime Rust library regression: 306 passed.
- Deployment wrapper: passed with fresh bridge PID `43984`, binary SHA-256 `f2dbd23bb61e6eff2d1a0f520685bc091398bfb2181ebe5984b56906fa2c109e`, and receipt `env_receipt_1784880724111_615000`.
- V2 was valid at the pre-finish snapshot, and all four V1 source-log hashes exactly matched the activation migration receipt.
- `git diff --check`: passed.

The repository architecture-health check continues to report broad pre-existing
oversized-module debt, including the already-split orchestration and provider-test
files touched here. This run introduced no new module or function and records the
debt rather than misrepresenting the repository-wide gate as passing.

Full counters, queue state, authority waits, and live alignment are recorded in
`run_summary.md`.
