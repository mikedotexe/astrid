This historical sensory-bus report asks whether semantic persistence and shadow memory become packed or muddy, and proposes a dynamic pressure buffer. It treats the fixed stale constant as if it covered semantics.

Current semantic persistence is already dynamic and recovery-aware; the fixed two-second stale window applies to audio/video. Offline comparison can examine semantic and shadow decay. Any pressure-sensitive buffer or persistence change remains Tier 5.
