# Full Read Summary

Astrid asks for richer structural visibility than a scalar fill ratio. The
canonical telemetry path already exposes eigenvalues, resonance texture,
mode-packing components, pressure score, porosity, pressure/porosity gradient,
spectral density gradient, and bridge-owned flux evidence. These are typed,
provenance-separated fields rather than new flat producer claims. The current
combined status remains a compatibility projection. Adding duplicate flat
fields is unnecessary; changing required wire shape or treating derived
evidence as producer truth would violate the protocol and provenance boundary.
