# Full-read summary: introspection_astrid_ws_1784849069

Astrid described disconnect as shivering and proposed decaying persistence of the last spectral state. Current source already records connection loss, reason, duration, reconnect state, cadence freshness, and retains the last valid telemetry instead of clearing it. It deliberately does not mutate old spectral values during backoff; any decay would manufacture new sensory state and therefore remains a live-control wait while felt disconnect quality stays eligible for observation.
