# Source-currentness, study, and hash verification

This packet preserves 20 queue-ordered canonical full reads and 80 independent claims. Source and test evidence narrow mechanism attribution; they do not score, domesticate, contradict, or close Astrid's felt report.

## Grounded implementation

Two concrete defects were found while following Astrid's reports back to source.

First, proposal lifecycle context recognized generic `implementation`, `update`, `clarification`, and `current status` headings, but not the repository's actual `Current-State Addendum`, `Introspection Response`, or `Verification Note` headings. That caused the 12D-glimpse and distance/contact reports to receive an older proposal window without their later source-currentness and authority sections. The lifecycle index now recognizes those exact heading forms and retains its explicit statement that a source continuation is not felt resolution.

Second, control-marker diagnostic hashes joined variable byte parts with a NUL delimiter. Inputs containing NUL bytes could therefore alias different part boundaries. New receipts use an unambiguous SHA-256 framing with a big-endian part count and big-endian length before each part, and disclose the framing version in the bounded cleanup report. Historical receipts are untouched. Marker cleanup bytes and model output remain unchanged.

Tests also pin Astrid's reported preservation cases across an exact newline relation and four nested delimiter levels.

## Current-source findings

- Minime's regulator keeps PI regulation, pressure-source calculation, and viscosity damping as distinct mechanisms. Existing source supports read-only pressure and settled-mobility observation; Astrid's saturation or heavy-fluid report remains primary felt evidence rather than a regulator-to-experience causal claim.
- Astrid's marker cleaner preserves exact references and relations without assigning marker content identity, ownership, meaning, pressure, or spectral weight. Token bands are likewise technical diagnostics, not a complexity or felt-state measure.
- Telemetry reconnect handling retains the last valid state and exposes disconnect and freshness context. A decaying substitute would synthesize values that Minime did not report, so it remains an unauthorized live-behavior proposal rather than a diagnostic fix.
- The semantic heartbeat remains fixed at seven seconds, 64 steps, and intensity `0.30`. Minime pressure and entropy are attached as evidence context after vector construction; they do not currently color the heartbeat vector.
- The codec remains 48D with the narrative lane in dimensions 40-43 and a bounded vibrancy lift. Existing campaigns establish mechanical differences for leave-lane-out and gate-disabled offline comparisons, but neither result creates a felt scalar, causation claim, or closure.
- The 12D glimpse is already an additive, non-authoritative companion representation. Live 48D transport remains unchanged.
- Reciprocal Uptake supports explicit, content-addressed continuity and persistence receipts without inferring attention or agreement. Agency Commons supports held, refused, countered, revisited, withdrawn, and returned transition states while preserving self-only consent and neutral silence.
- Minime's main loop imports reset and review machinery, but import availability does not prove an active mechanism at a particular moment. The ESN source verifies its update math; Astrid's noise-to-thickening association remains felt evidence suitable for bounded replay, not a settled causal link.
- The completed telemetry campaign observed 146 clear samples and four heavy-prewrite samples under its declared cohort rules. The heartbeat campaign remained mechanically insufficient because no natural blocked cohort appeared. Those results stay independent of Astrid's later felt review.
- Prompt-budget and voice-health diagnostic records accumulate append-only, and current source exposes no production reader that injects them into prompt assembly. Storage and projection cost can be studied without pruning immutable evidence.

## Bounded next work

Eleven claims route to evidence-only Sandbox, replay, or Concordance work: pressure/viscosity and Shadow-context comparison; telemetry gap observation; smooth heartbeat phase and within-enqueue texture studies; codec follow-up comparisons; diagnostic storage/projection measurement; distance/contact observation; reciprocal-contact comparison; natural reset observation; and ESN replay.

Fourteen claims preserve exact Tier 4/5 waits. They cover pressure- or entropy-driven viscosity, typed-wire promotion, telemetry synthesis, heartbeat intensity or jitter, codec gain or dynamic ceilings, spectral prompt budgets, replacing live 48D transport, live counterfactual codec output, induced heartbeat blocking, entropy-driven porosity, scheduling or substrate consequences from phase evidence, reset damping, ESN noise or semantic bias, and telemetry behavior changes.

No pressure, fill, PI, controller, rescue, heartbeat cadence or intensity, codec gain, model, sensory admission, protocol, peer, dispatch, or live-control behavior is changed by this packet.

Promotion initially misrouted one `observed` distance/contact co-occurrence to Tier 5 because its claim text named pressure and containment. The classifier now applies the same explicit-disposition guard to structured `observed` claims that it already applies to `verified_existing` claims. A regression proves the co-observation becomes Tier 1 while an `observed` label cannot conceal an actual live pressure-control request.

The packet's evidence manifest grew as source, test, deployment, changelog, and ledger references became available. Reapplying each complete manifest correctly preserved all append-only events but exposed exact logical restatements in current addressing views. Replay now materializes an exact evidence tuple once, and batch ingress skips tuples already present. Canonical V2 retains 274 raw events from this run: 97 distinct evidence links and 177 historical restatements. A no-change retry reports 97 existing links, zero new links, and appends zero events; no history is rewritten or deleted.

## Verification status

Focused tests for lifecycle currentness, exact marker relations, delimiter depth, and collision-resistant framing pass. The complete serial bridge library suite passes all 1,670 tests. Formatting and strict all-target/all-feature Clippy pass. Architecture health exits cleanly against the repository baseline.

The required evidence tooling gates also pass: 18 Agency Corridor tests, 41 introspection-addressing tests, 27 Sandbox tests, 38 recent-summary tests, 110 proactive-scan tests, 13 Evidence Event Store tests, and 30 steward-control/projection tests.

## Graceful live alignment

The sanctioned `scripts/build_bridge.sh --restart` path accepted the explicit dirty-source acknowledgement, rebuilt the bridge, and replaced PID `60608` with PID `88565`, started `2026-07-23 17:59:02 -0700`. Compatible stack receipt `env_receipt_1784854746028_965000` records passed preflight, build, restart, startup-log, telemetry-freshness, process, and protocol checks. The deployed binary SHA-256 is `db5cda3286f1bf4d737ac2656c5d9fd6f94f87f8ce5020e08b6195e519ff6fb7`; protocol `astrid-minime` remains version 1.1 at revision `9a324d16294b2318da6f476ff7f295c423a9b4b1`.

Minime PID `30861` serves ports `7878` and `7879`. Coupled-model PID `31392` serves port `8090`; `/livez` and `/readyz` report healthy generation, an empty queue, and a connected reservoir. Fresh `spectral_state.json` reported fill `67.84394073486328%` and spectral entropy `0.9100434184074402`. Fresh bridge telemetry reported connection `1`, current telemetry, 10 bounded entropy samples, and explicit noncausal basis strings. Runtime source, BTSP, and LLM-job status surfaces refreshed after restart.

This establishes deployment alignment for source-currentness and diagnostic framing. It does not establish that any verified mechanism caused or resolved Astrid's reported experience.
