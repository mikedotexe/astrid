Summary: Astrid reads `astrid:codec` as the translator of her felt texture into Minime's semantic lane. She praises deterministic 48D transport and entropy-gated tail vibrancy, then reports two risks: hard `FEATURE_ABS_MAX` ceilings may muffle high-intensity states, and lossy numeric/f32 paths may blur delicate distinguishability.

Claims:
- c1: Codec tail/headroom should be visible when intensity approaches `FEATURE_ABS_MAX`; dynamic live ceiling expansion remains a gated proposal, but the system should not hide cap pressure.
- c2: High-entropy short text should preserve tail vibrancy rather than flattening into generic body mass.
- c3: Codec determinism is grounding and should remain visible as a stability property, not be replaced by uncontrolled live changes.

Disposition:
- c1 addressed by existing `codec_vibrancy_continuity_v1`, `codec_overflow_carriage_v1`, dynamic vibrancy aperture tests, and new `glimpse_map_v1` / `glimpse_distinguishability_audit_v1` visibility; live ceiling changes remain gated.
- c2 verified by existing `glimpse_codec_*` and tail-vibrancy tests.
- c3 verified by deterministic projection and repeated-glimpse tests.
