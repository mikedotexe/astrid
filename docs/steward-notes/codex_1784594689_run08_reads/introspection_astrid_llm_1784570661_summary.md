# Full-read summary: introspection_astrid_llm_1784570661

The requested distinction is already represented as two layers: exact requested-token buckets and a read-only felt-pressure profile that names heavy-short, heavy-medium, sparse-deep, dense-deep, and distributed-deep texture from current spectral evidence. It explicitly does not change the model budget.

Subtle-content and dense-scaffolding cases are regression-tested. Upgrading budget classes from entropy or pressure would alter live provider scheduling/generation and remains Tier 5.

Evidence is linked in `evidence_links.json`; live-control proposals remain explicit authority waits.
