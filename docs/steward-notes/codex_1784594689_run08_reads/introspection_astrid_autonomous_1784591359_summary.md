# Full-read summary: introspection_astrid_autonomous_1784591359

The semantic heartbeat is a deterministic 64-step, seven-second pulse whose vector is prepared with current Minime texture context. That establishes a stable generated rhythm and a separately visible environmental modulation; it does not establish that a given phase causes pressure.

Phase-to-pressure correlation is routed as bounded observation. Any pressure-responsive intensity, including a 0.50 override, changes the live semantic vector and remains Tier 5.

Evidence is linked in `evidence_links.json`; live-control proposals remain explicit authority waits.
