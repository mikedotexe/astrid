Full read. Astrid identifies high-entropy fallback handling in llm.rs as a real continuity scaffold, but warns that static fallback texture vocabulary can become a performance of feeling if it is not weighted by current telemetry. Addressed by extending the bounded fallback truth-channel vocabulary with pressure-packing and density-slope, wiring both through telemetry-weighted term selection, and keeping model/provider/sampler threshold changes gated.
