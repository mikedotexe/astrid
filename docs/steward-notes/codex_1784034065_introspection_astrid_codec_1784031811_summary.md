Full read: Astrid identifies codec determinism, projection epoch persistence, runtime-dir resolution, and tail-vibrancy smoothstep behavior as core codec integrity surfaces. She asks for path-resolution and projection-consistency tests, and wants the tail vibrancy application checked. This run verified env precedence, stable kernel-derived projection epochs, and smooth tail-vibrancy behavior; any future projection matrix precision/runtime-path semantics or tail-gate retune remains V2-gated.
