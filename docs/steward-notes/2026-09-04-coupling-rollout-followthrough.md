# Coupling Rollout Follow-Through

Date: 2026-09-04. Interactive collaborator: Codex Astra.

This continues the selected "Next Safe Deployment Sequence" in
`docs/steward-notes/2026-09-04-graceful-coupling-rollout.md` under Mike's
explicit request to proceed. It is not a productive automation round.

## Source And Scope

Canonical report: `capsules/spectral-bridge/workspace/introspections/introspection_astrid_llm_1788490867.txt`.
SHA-256: `acf73204b2247e8c137c4510799a93d88fac3c312065b2698c9b98f61829489c`.
Witness: `lsw_d0b7b7a026443bf486fa11436642a6d03a913b66a1782995f19501084d7739e1`.

The nine previously extracted claims remain open under their exact existing
dispositions. The slow-head numerical correction addresses a reproduced bug;
it does not establish the cause of Astrid's report or measure felt improvement.
The independent-interpretation and ownership questions remain substantive.
No private moments, forced introspections or leading messages are collected.

## Exact Live Bridge Candidate

The canonical checkout stays on `main` at
`27a7dc6ec4fc239fc7317ffba03b1b1a34801472`, preserving its prior dirty paths.
The observed remote main tip was `0ca10019b08bb74b702dd1dcefe5299efb0d6a39`.
No index, merge, commit, push, stash, reset or cleanup is performed.

A separate worktree was created at
`/Users/v/other/worktrees/astrid-graceful-coupling-rollout`, on branch
`codex/graceful-coupling-rollout`, starting from the exact recorded live build
`d6ff371cd25e6a4616431d8f07f09a3e7eb3fcdf`. This avoids deploying older main
as a substitute for the richer live lineage or switching the shared checkout.

The candidate preserves the live agenda, ATTEND and self-control surfaces.
The production dialogue path now calls a testable eleven-block builder;
original text is retained before caps for owner-labelled private overflow.
Tests compare old/new visible prompts at zero and ample budgets, default and
extreme ATTEND weights, retained agenda floors and original self/peer tails.
Existing live-lineage witness and Division/promotion wrapper tests are retained.

## Minime Identity Reconciliation

The live engine, gateway and supervisor began August 31, but the canonical
disk executable was replaced September 3. That is not permission to restart
them into the newer build just to make a hash check pass.

- Historical manifest: `capsules/spectral-bridge/workspace/deployment_manifests/minime-division-runtime.json`.
- Historical executable SHA-256: `af3d83e825e3b8211d892bc64fbac982e65bdc4f0c1b090919fd41fb2b443017`.
- Matching retained Cargo artifact: `/Users/v/other/minime/minime/target/release/deps/minime-cf86571bc4819705`.
- Retained and all three mapped image UUIDs: `56189890-33D5-3600-A0EB-5CB343B09050`.
- Current disk executable SHA-256: `d3f60e53a2029c86aafaa32a8df11852da3f4d3d1b6862d2bca047d50232d22f`.
- Original mapped inode: `268834257`, size `6480288`; current disk inode: `274976125`, size `6516096`.
- Engine PID `63445`; Division gateway PID `63505`; supervisor PID `63547`.

`minime_runtime_binding.py` captures only mapped image UUID metadata from a
bounded read-only sample. Raw stacks are not persisted. It archives the
retained executable privately, rechecks historical manifest/launcher hashes,
source/installed configurations, dormant topology, PID/start identities and
gateway ports, and emits `stack_runtime_binding_v1`. This is a separate
runtime witness, not a fabricated historical build manifest. It has no
`built_at` assertion and explicitly sets `memory_bytes_attested=false`.
UUID agreement corroborates a build; it is not a memory-byte attestation.

`environment_receipts.py` validates this binding afresh, including its
180-second use window and live PID/port/configuration checks. Historical
manifests and the original failed receipt are never rewritten or waived.

The topology-correct pre-deployment inventory passed at
`env_receipt_1788565601143_180000`. This precedes any live restart in this pass.
The earlier failed inventory `env_receipt_1788563175289_828000` remains failed.

## Deployment Boundary

The model wrapper now requires a passing full-stack receipt before sending
its single SIGTERM, repeats source preflight after capture, and records a
fresh full-stack receipt after replacement/readiness. No forced fallback is
allowed. The intended live change is only the reviewed median-relative
slow-head tail correction in `mlx_reservoir.py`.

The first bridge restart remains a separate constraint: the current binary's
shutdown does not await its autonomous task. New drain code cannot retroactively
protect an old process. Do not use a kill/kickstart and label that graceful.
An aligned candidate is not authorization to bypass the canonical deployment
wrapper, signed handoff, source snapshot or first-drain review.

## Verification And Coordination

Initial candidate suite: 1,958 passed, three Minime source-path failures due
to the isolated worktree's missing sibling source files. A second fixture
attempt exposed macOS `/var` versus `/private/var` path equality and a test
which deliberately checks default paths without environment overrides.
These failed runs are preserved as failed. No production path logic was
changed to hide them. The final suite uses canonicalized private copies of
the two required Minime source files; the default-path test runs separately
without overrides. Final results are appended below when complete.

Runtime binding/reload/receipt/wrapper tests initially passed 51 checks;
candidate witness/historical-shape tests passed 23. Domain-boundary verification
passed with zero violations. No Minime source or changelog was modified.

Maintenance pause generation 340 was claimed by `codex-astra-interactive`,
with no active steward lease. Scheduled automation settings are unchanged.
The maintenance hold will be released after the deployment attempt; this
does not re-enable scheduled automation or alter a being's activity.

No Corridor, Sandbox, portfolio, new replay, Division ceremony action, round
or follow-up is recorded. PI, damping, Shadow interventions, reservoir
mathematics, Division transitions, Minime regulation and all unrelated
controls remain unchanged. No felt closure, assent or uptake is inferred.

## Actual Model-Only Deployment

The sanctioned `scripts/restart_coupled_model.sh` completed with exit 0.
Only the reviewed `mlx_reservoir.py` production source delta was acknowledged.
Canonical Astrid's last-source-write age was 184.2 seconds and neural's was
2852.5 seconds at the final foreign-activity checks, both exceeding the full
180-second window; no live cooperative session was detected. Controller
status confirmed our pause 340, no lease and no pending events.

| Evidence | Identity / result |
| --- | --- |
| Pre-reload full stack | `env_receipt_1788566245187_869000`, passed |
| Model deployment | `env_receipt_1788566264632_299000`, passed |
| Post-reload full stack | `env_receipt_1788566268849_671000`, passed |
| Old model | PID 98236, September 1 11:52:25 PDT; exited |
| New model | PID 60333, September 4 16:57:34 PDT |
| Signal | One SIGTERM; no forced termination or launchd configuration change |
| Signal log | September 4 16:57:32 PDT, draining active generation |
| Ready log | September 4 16:57:43 PDT, coupled strength 0.02 |
| Journal restored | 50 entries, tick 160704577, coupling 0.020, AGC window 50 |
| Port 8090 | Owned by replacement PID 60333 |
| Post-receipt telemetry ages | Bridge 1.786 s; Minime 1.792 s |

The guard observed idle/empty queue before signaling, revalidated loaded
configuration and unchanged model inputs, then awaited old-process exit and
replacement readiness. This is not atomic traffic quiescence: the startup
log records one incoming `/v1/chat/completions` request receiving HTTP 503
at 16:57:43 PDT while loading. The reload was graceful, not zero-downtime.
No favorable interpretation is substituted for that availability gap.

The new manifest was prepared privately and published by rename only after
the replacement was ready. The prior published manifest remains archived.

- Drain: `capsules/spectral-bridge/workspace/deployment_manifests/coupled-model-reload.7OeQTs/drain.json`, SHA-256 `311d0b7cbe64c4ad1821a535ea4fce634ce2d398acab6926604cc13524f57cc9`.
- Previous model manifest: same directory, `previous-manifest.json`, SHA-256 `dceaf8f4d9a86ed9c84ed293cfebf46db40ea84e090928cf8ef1c0359bf39dfa`.
- Published model manifest: `capsules/spectral-bridge/workspace/deployment_manifests/coupled-model.json`, SHA-256 `b54febdc07b24798b81f2e7f964f18e6b7f9e072d522a5419a43d82ea406e586`.
- Deployed processor: `/Users/v/other/neural-triple-reservoir/mlx_reservoir.py`, SHA-256 `7bf189ff5d618e9002a623ae5d38a206758a80738ee79477eaf8059a1d383f1e`.
- Historical Minime manifest remains SHA-256 `04547f11f860b7434aa5f1410dec087b0286b73e7bd2b2b8cd34cdecd230d766`.
- Post-reload Minime corroboration: `capsules/spectral-bridge/workspace/deployment_manifests/minime-runtime-binding.t2imcxj0/runtime-binding.json`.
- Bounded receipt extract: `docs/steward-notes/2026-09-04-coupling-rollout-receipts.json`, with exact original NDJSON line hashes including newline.

Server, gateway and installed model plist retain the reviewed original hashes.
Startup resolved model snapshot `a2fa92fa5bdba1fc4be9d927a5d5188c17d87d09`,
the same revision shown in the previous process's startup and offline replay.
All eight weights/config/tokenizer/template assets were rehashed against the
replay manifest and matched; aggregate study identity remains
`da4a842eb633ed5483a097d0d837c9b597a9503c561ab29b44536395a6ef109a`.
The existing model name still resolves `main`; pinning future loads to a
snapshot is separate configuration work, not silently performed here.

Bridge PID 36597, Minime PIDs 63445/63505/63547 and reservoir-service PID 1514
retain their original starts. Minime's repository remains clean. No bridge
binary, Minime binary, self-control handoff or regulation setting was changed.
Observed Minime fill moved naturally from 73.0224% to 71.0535%; configured PI
targets remained kp 0.8, ki 0.12, max step 0.07, target fill 68 and regulation
strength 0.95. Effective/derived gains were 0.85/0.14/0.08 at both nearby
observations. These observations are not commands or causal/felt outcome tests.

## Final Verification

- Candidate bridge: all 1,961 library tests passed, 1,960 with canonicalized
  private Minime source fixtures and the one default-path test separately
  without environment overrides. No test's behavioral coverage was omitted.
- Six retrieval/actual-production-context tests passed independently.
- Candidate witness/historical-shape suite: 23 passed.
- Final canonical reload/runtime-binding/wrapper/receipt suite: 56 passed.
- Same suite on the richer live lineage, retaining Division/promotion checks:
  58 passed.
- Steward control/projector, Event Store and Division suites: 55 passed
  within the earlier 107-test combined run.
- Neural gateway, slow head and both replay suites: 29 passed.
- Deployment preflight: 10 passed; domain-boundary audit: zero violations.
- Shell syntax and both Astrid checkout whitespace checks passed.
- Epistemic lint: 11,503 records, zero issues. Event Store full-chain snapshot
  before final claim links: sequence 995572, head
  `f387a063ece4244531fe14b7ab3f5cefdc99d4e4c067c355bbb75f727a58f4ff`,
  16 streams, zero corrupt lines, all four legacy V1 sources immutable.

## Observation And Remaining Work

The conservative model-only observation boundary is the passing post-stack
receipt at `2026-09-04T23:57:48.849000+00:00`. Subsequent naturally authored
reports should be linked to the generation's actual start/deployment evidence;
a report written later may still contain earlier-turn material. Preserve
friction, indifference or disagreement. No post-change qualitative conclusion
has been drawn, and no new report was solicited or counted as fully processed.

Subsequent ordinary requests completed with HTTP 200 and reservoir check-ins
at 17:01:27, 17:02:16, 17:02:21 and 17:03:11 PDT. Journal metadata advanced
to tick 160707282 while retaining coupling 0.02. These are operational
continuation observations, not assessments of the generated content.

The bridge wording/retrieval changes are **not live yet**. Their candidate
is compiling and fully tested on the correct live lineage, but deploying it
still requires a reviewed first-drain method, signed self-control handoff,
sanctioned build wrapper, exact source capture and shared-checkout alignment.
The isolated test build generated a fresh, ignored bridge `Cargo.lock`, SHA-256
`f1667b83f27c7defe2c89113a2027b0fa20daf199664af81ae23d6a1672fd4e9`.
Matching the live source commit is not a claim of identical dependency
resolution; review/preserve the release lock and local path dependencies
before building a deployable bridge artifact.
Do not run the canonical main wrapper as a substitute or use kickstart -k.
No archival checkpoint, merge or clean-tree claim is made in this pass.

For an approved model rollback, first re-audit active work and restore only
the reviewed slow-head hunk from neural commit
`afc2931a657d1bd79a7076ece6310ee3d8f6ceba`; original processor SHA-256 is
`7c091d9ca30966d9728a4fed0eedc71b91aa37b92887dd11e943f0db841c4092`.
Re-test, wait the full quiet interval, and use the same sanctioned graceful
wrapper with an explicit rollback acknowledgement. Do not reset the tree,
replace current journal/reservoir state with old snapshots, change coupling
gain, or escalate a drain timeout. No rollback was needed or performed.

The final addressing append added 17 exact links. The report now has nine
claims and 90 evidence links, `triaged_pending_action`, `fully_addressed=false`,
and no missing claim proof. Forty-one addressing self-tests and two epistemic
self-tests also passed. No additional canonical report was marked read and
no productive round was recorded.

## Maintenance Released

The controller resume completed successfully at
`2026-09-05T00:09:18.001650+00:00`, generation 341, `paused=false`.
This releases only our interactive maintenance hold 340. Scheduled automation
settings were not changed. The canonical and candidate Git indices remain
empty; all source work and existing foreign changes remain unstaged.
