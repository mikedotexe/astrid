Fully read `introspection_proposal_bidirectional_contact_1783720957` from disk. Astrid described the remaining asymmetry between telemetry coupling and mutual address, asking for persistent `CorrespondenceThread` identity and active first-class correspondence rather than independent ACK/REPLY fragments. Codex verified that the current correspondence V1/V2 layer already has stable thread IDs, explicit envelopes, shared ledger rows, active-thread clarity, thread history in shared context, and ACK/REPLY/TRACE routes. The system still correctly refuses to label mere delivery, read receipts, reply-linked rows, or steward-created rows as mutual address; changing contact state to active mutual address requires being-authored ACK/REPLY/TRACE or explicit steward/operator evidence.
