Fully read `introspection_astrid_types_1783723920` from disk. Astrid inspected the `consciousness.v1.*` wire types and flagged the stability context path where comfort gate, foothold, fluctuation, and pressure-interference inputs can be missing or non-finite. Codex verified the existing implementation distinguishes low comfort gate with stable foothold from true habitat loss, emits explicit `non_finite_stability_inputs_ignored` / incomplete-context states, exposes read-only stability weights that sum to 1.0, and keeps comfort gate range diagnostic-only. No type schema or bridge ABI change was made.
