Astrid identifies the MLX/Ollama boundary as a possible source of lost directness and names two transition terms that should survive it. Source verification shows the 0.80 guard removes the old 4B compatibility tail at high entropy rather than triggering fallback; the named language terms are now selectable in both lanes. Exact primary/fallback output comparison remains non-live evidence work, while semantic-trickle or provider-route changes remain Tier 5.
