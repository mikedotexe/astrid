# Full-read summary

Astrid reports gradient-shear, restless movement, and viscous persistence while
the report-bound `core.rs` exposes only the regulator's include graph. The
requested follow-through was completed by reading all 594 lines of
`core/viscosity.rs` plus the pressure-source and resonance-evidence mechanics.

Persistence is not a constant multiplier or temporal decay state. It is a
bounded weighted function of viscosity, temporal persistence, mode packing,
and pressure. High entropy raises an inert erosion readout, while temporal drag
deliberately retains sticky persistence even after pressure falls. The
pressure/porosity readiness field `local_control_applied=false` says that the
diagnostic candidate did not mutate porosity or control; it does not summarize
every Minime regulator branch. Existing source has separate resonance-control
labels and read-only damping/buffer previews. Focused tests pass. The felt
account remains primary evidence, but source does not establish its cause.
