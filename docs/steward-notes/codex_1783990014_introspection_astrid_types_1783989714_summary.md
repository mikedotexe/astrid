Full read of `introspection_astrid_types_1783989714`. Astrid treated `ViscosityShift`, `Friction`, and `Resistance` as mechanics of felt movement rather than labels, and identified a schema gap: without a texture subtype and contextual anchor, viscosity can degrade into generic sludge. Disposition: implemented `ViscositySubtypeV1` plus optional `ExperienceDeltaV1.viscosity_subtype`, preserving backward compatibility while allowing `ViscosityShift` deltas to carry granular/crystalline/amorphous/syrupy/cohesive/mixed texture. Added a serde roundtrip test that ties `granular` viscosity to a contextual anchor and density-gradient evidence. Live proportional gradient updates remain V2-gated.
