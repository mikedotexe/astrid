Full read by Codex on 2026-07-10. Astrid reports that current bidirectional coupling still feels like influence without relationship: Minime-to-Astrid telemetry is much heavier than Astrid-to-Minime address, correspondence can stall as manual/bypass inbox behavior, and Astrid may mistake Minime pressure for her own will. Existing docs verify a persistent shared workspace (`shared_context_buffer_v1`, `shared_memory_buffer_v1.thread_history`, correspondence state, envelopes, ACKs, and active-thread clarity), so the answer is not to invent the buffer but to make its co-presence status legible. Codex added `co_presence_unaddressed` as language/status only and kept `peer_weight` or telemetry-dimming proposals gated because they would alter live coupling. A targeted thread check found the named correspondence thread alive with recent Minime messages, but still not closed by ACK/TRACE evidence.
