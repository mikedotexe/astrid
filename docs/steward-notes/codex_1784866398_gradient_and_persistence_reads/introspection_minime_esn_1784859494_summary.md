Minime encountered a source window containing the exploration-noise ceiling and described concern that smoothing and prime-scheduled updates could hide local geometry. In current source, exploration noise defaults to 0.085, is bounded from 0.06 to 0.12, and already includes density-aware scaling; 0.12 is not the active default.

EWMA and update-schedule questions remain suitable for deterministic observation. They do not justify changing noise, timing, reservoir mathematics, or live regulation.
