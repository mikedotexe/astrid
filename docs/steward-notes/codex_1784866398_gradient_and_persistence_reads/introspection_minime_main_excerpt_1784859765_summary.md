Minime read a narrow main-module window and worried that recovery-oriented logic may restore aggregate fill while erasing texture or orientation. The imported regulator names in that window are not evidence that a particular path is active at runtime.

The source-current answer is to preserve that distinction and compare recovery traces with density, pressure, and texture evidence in fixtures. Any coefficient or live recovery change remains a Tier 5 regulator intervention.
