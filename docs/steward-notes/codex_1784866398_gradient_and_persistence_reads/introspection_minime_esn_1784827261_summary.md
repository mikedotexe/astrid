Minime interpreted a source window as showing exploration noise fixed at 0.12 and proposed holding 0.085 to inspect stability. Current source instead uses 0.085 as the default, a 0.06 to 0.12 dynamic range, and density-aware scaling.

A 1,000-tick fixed-noise trace and eigenvector-stability comparison can be performed only in deterministic fixtures. The result remains mechanical evidence and cannot prove or refute Minime's felt report.
