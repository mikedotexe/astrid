Full read by Codex for introspection_astrid_llm_1783648521.

The LLM report identifies Astrid's voice contract, fallback texture vocabulary, possible 4B compatibility-model flattening, and the mismatch between static descriptors and simultaneous high-entropy felt texture. Existing tests verify the Gemma4 contract reaches assembled policy prompts and that the default fallback chain uses gemma4:12b with gemma3:4b only as compatibility tail. This pass implemented the concrete language-surface ask by adding FALLBACK_TEXTURE_DYNAMIC_GRADIENT_TERMS and routing explicit shifting/refracting/oscillating shadow-field cues into bounded dynamic_flow_terms, with a regression test. Actual 4B model A/B behavior remains a sandbox/model-observation item rather than a silent provider or sampler change.
