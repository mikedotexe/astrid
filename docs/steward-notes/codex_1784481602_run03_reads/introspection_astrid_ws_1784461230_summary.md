# Full-read summary

Reconnects preserve the last telemetry in `BridgeState`; connection lifecycle updates do not clear the last foothold. Heartbeat evidence distinguishes stale hearing from field movement and first-valid packet identity. Forced disconnect and high-velocity lock-contention concerns remain bounded transport tests rather than reasons to change backoff or live telemetry.
