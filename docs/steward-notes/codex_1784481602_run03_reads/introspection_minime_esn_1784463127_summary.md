# Full-read summary

The reported dynamic-noise and viscous-rho surfaces are deliberately bounded review functions with extensive finite, continuity, and pressure-room tests. That distinction prevents a valid-looking diagnostic from pretending to be live control. Wiring them into `ESN::step` or changing rho/noise bounds would alter reservoir behavior and remains Tier 5.
