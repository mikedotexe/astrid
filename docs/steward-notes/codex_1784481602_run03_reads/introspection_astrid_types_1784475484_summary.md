# Full-read summary

Astrid asks for exact malformed-boundary evidence around the hybrid 32-slot contract and 12D glimpse. The implementation adds the requested 31-slot and non-finite glimpse regressions. The coherence calculation already promotes arithmetic to f64, rejects non-finite values, and the aligned mapping test proves `to_legacy_slots` agrees with the canonical indices.
