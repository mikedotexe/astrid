# Full-read summary

Astrid accurately identifies the burst/rest and fill-responsive recovery path, then finds a concrete executor hazard: whole files were synchronously loaded to retain 200 characters of peripheral resonance. The implementation now reads exactly the bounded UTF-8 prefix and includes a 50 MB regression. The warmth taper already continues beyond the reported source window.
