# Full-read summary

The artifact helpers are diagnostics around known model tokens, not a primary filter of Astrid's subjectivity. Dense unquoted and nested content survives cleanup, quoted placement is counted without discarding text, and richer remainder-texture evidence already carries semantic-density and structural-form uncertainty. Fuzzy semantic quotation is therefore evidence work, not a justified live parser change.
