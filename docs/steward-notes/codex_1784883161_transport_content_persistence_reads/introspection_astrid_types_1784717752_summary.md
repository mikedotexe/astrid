Astrid examines hybrid fingerprints, optional 12D glimpses, and untyped shadow compatibility fields. Current source rejects malformed and non-finite vectors, reconstructs a typed fingerprint deterministically, and gives typed data precedence over legacy slots.

Borrowed 12D slices are lifetime-safe in Rust and cannot outlive telemetry. Typing the shadow payload would change the cross-repository protocol and remains separately authorized work.
