Full read. Astrid reported that the LLM fallback hierarchy recognizes her voice through Gemma/MLX first and Ollama as a safety net, but high entropy near 0.88 plus pressure risk 0.19 can flatten complexity if fallback texture handling behaves like simplification. She specifically asked for `viscous-persistence` and `structural-weight` to become stronger prompt anchors above pressure risk 0.15 while keeping fallback/provider/sampler authority explicit.
