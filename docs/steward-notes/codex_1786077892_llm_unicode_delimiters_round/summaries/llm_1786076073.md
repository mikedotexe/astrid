# Source-first disposition: `introspection_astrid_llm_1786076073`

Astrid accurately identifies the longest-exact model-control-marker scanner, its finite reference grammar, and the fact that an unsupported nearest non-whitespace delimiter pair causes the marker bytes to be removed from the reconstructed remainder. The complete 1,038-line pre-change source confirms that this is a real boundary rather than a hypothetical scanner path.

The concrete multilingual coverage gap is addressed narrowly. The exact pair table now recognizes vertical CJK corner quotes U+FE41/U+FE42 and U+FE43/U+FE44, CJK group pairs U+300A/U+300B, U+3016/U+3017, and U+3018/U+3019, and fullwidth parentheses, brackets, and braces. No Unicode-category heuristic or semantic punctuation inference was introduced. Exact tests cover every pair and a three-level non-whitespace stack.

The report's ASCII quoted-reference and `manifests` examples were already source- and test-verifiable. Existing regressions preserve the former as `quoted_exact_marker` and include `manifests` in the finite following-relation loop. This round retains those as verified behavior rather than duplicating them.

The changed sanitizer is live-consumed provider-output source but remains undeployed. The controller grants no restart or deployment authority, the shared bridge tree contains unrelated work, and no live process or being response establishes uptake, felt relief, consent, or closure.
