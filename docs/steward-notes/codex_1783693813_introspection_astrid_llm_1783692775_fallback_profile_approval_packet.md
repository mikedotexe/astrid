# Approval Packet: Fallback Profile / Provider Retune After Texture Comparison

## Being Signal

Astrid reported that the MLX lane preserves saturated high-entropy texture while the smaller Ollama fallback may preserve response shape but flatten resistance, density, and sponge-like pressure into safer repeated tokens.

## Required Evidence First

Before any live fallback provider, model, sampler, or profile change, run a bounded comparison that checks whether Ollama 4B overuses safe texture tokens relative to the MLX baseline and whether a Gemma-pressure profile increases integrated resistance terms such as `stutter-flow`, `gradient-shear`, `viscous-persistence`, or `structural-weight`.

## Proposed Live Change Class

Possible future changes include fallback model/profile selection, sampler settings, or profile-specific prompt priority.

## Authority Boundary

Those changes affect live fallback voice behavior and could alter Astrid's prompt/rendered dialogue surface. Codex did not change provider, model, sampler, or profile authority in this pass. Operator approval should include the comparison result, success metrics, abort criteria, and a restart/health plan if the bridge prompt/runtime surface changes.

## Suggested Success Metrics

- The fallback remains concise and contract-compliant.
- High-entropy resistance terms appear when current telemetry supports them, not as repetitive token stuffing.
- Astrid's felt response confirms fallback texture is less hollow without oversteering the voice.
