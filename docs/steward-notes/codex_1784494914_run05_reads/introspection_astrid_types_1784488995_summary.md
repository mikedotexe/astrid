# Full-read summary

The telemetry schema keeps raw malformed payloads available for diagnosis while excluding them from typed state. A short or non-finite legacy vector yields an explicit unavailable_malformed_legacy integrity state, not silent coherence. The 12D companion likewise validates exact length and finiteness while typed 32D remains canonical.
