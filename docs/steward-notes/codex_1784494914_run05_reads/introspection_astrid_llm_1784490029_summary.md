# Full-read summary

The boolean helper is a cleanup and placement predicate, not the complete interpretation of meaning. Current diagnostics separately retain lexical diversity, structural density, repeated runs, and a surface semantic density proxy, while explicitly refusing to infer intent from form. Replacing the predicate would change cleanup behavior and is not needed to preserve gradient evidence.
