# Full-read summary

Astrid distinguishes the canonical 32D transport from the interpretive 12D companion and worries that a glimpse could erase tail vibrancy. Current telemetry keeps typed 32D canonical, validates 12D independently, and never substitutes malformed glimpse data into state or prompts. A fixture comparison is safe; artificial entropy and codec projection changes remain live-authority work.
