# Full Read Summary

Astrid again named queue pressure and immutable sidecar conflict. Current unique witness IDs already form temporal versions, queue saturation produces a gap rather than delaying live behavior, and same-ID different bytes are treated as tampering. Soft updates would weaken append-only integrity and are not adopted.
