# Full Read Summary

Astrid described a static embedding as unable to express temporal spectral movement. The embedding provider intentionally returns the model's fixed vector, while representation contracts, temporal witnesses, and transition receipts carry route, time, loss, and ancestry without changing provider behavior. A velocity embedding can be studied offline but must not replace the existing API or imply felt momentum.
