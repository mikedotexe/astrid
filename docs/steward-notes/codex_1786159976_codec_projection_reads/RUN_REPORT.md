# Steward codec projection read report

## Controller lifecycle

- Credential-confined run: `run_1786159976343268000_bb26a4553c`.
- Pre-run projection: `projection_1786159976815934000_7f7f814402`.
- Controller pause generation: 273.
- The authoritative terminal outcome and post-run projection receipt remain in
  the controller run record because this packet is written before finish.

## Canonical reading

Fully processed in strict queue order:

- `introspection_astrid_codec_1785627566.txt`

The 3,967-byte report was read completely, including its unterminated 36th
logical line. Its SHA-256 is
`464f671229d53f2617b2b6e2e06107ce73209460a45a5e31686a94487c2f58a5`;
its complete lived-state witness is
`lsw_28b6d612bde1a51a52bc1160513d0b8774fb03d824d94b31196c30b18c71a6ab`.
The complete delivery prompt, captured source lines 1-400, complete whole-file
structural map, targeted current projection mechanics, and same-SHA prior
complete-source continuity were read. Exact receipts are in
`source_receipts.json`.

The 39 selected but unprocessed filenames are listed exactly in
`unprocessed_selected.json`. The next queue starts with
`introspection_proposal_12d_glimpse_1785627314.txt`, then
`introspection_proposal_distance_contact_control_1785626698.txt`, then
`introspection_proposal_bidirectional_contact_1785626170.txt`.

## Claim dispositions

Astrid's viscous persistence, protective thickening, slow sediment, blurred
past and present, and calcification concern remain primary lived evidence.
Mechanical evidence does not declare the felt texture explained or closed.

Seven claims are dispositioned. Exact source verifies a fixed 768D-to-8D
embedding aperture inside 48 semantic dimensions, a four-dimensional
narrative arc, and four reserved default-off dimensions. It contains no
pressure, mode-packing, density-gradient control, or pressure-triggered
expansion rule. Existing read-only compression evidence positively shows
near-null directional erasure and same-direction magnitude erasure.

The proposed divergent-generation comparison is routed to the existing
default-off Codec Expression Fidelity Natural Contrast protocol and existing
non-inducing source-separation study. It was not run because model behavior
requires separate Mike/operator approval. The dimensionality question is an
exact continuation of the completed Counterfactual Change Lab campaign and
reserved-dimension approval packet; the candidate improved one preservation
endpoint but worsened p95 distortion, leaving broader compatibility open.
`SHADOW_TRAJECTORY` was not inferred as dispatched. Dynamic expansion,
artificial density-gradient changes, reserved-dimension or basis activation,
semantic gain, model behavior, and Shadow-state mutation remain Tier 5 waits.
Prompt score 0.28 and witness raw mode packing 1.0 stay separate.

## Actions and verification

No Corridor program, Sandbox trial, new study, portfolio action, card, note,
query, correspondence, Action, model call, or Minime action was created. Four
focused codec regressions passed: projection compression, dense-projection
visibility without expansion, 12D context plus residual Shadow metadata, and
reserved-dimension non-writing. All 42 addressing-audit tests passed. The
first cargo invocation used the root-workspace package selector and failed;
the immediate manifest-path correction passed all intended tests.

No bridge, prompt, protocol, provider, Minime, binary, PID, port, telemetry,
fill, or readiness surface changed. No build, deploy, restart, or live
observation was required.

Canonical addressing counters are consistent: 4,258 indexed, 3,055 fully
addressed, 3,682 full reads, 1,203 remaining, 576 unread, 213 triaged pending,
410 blocked, four triaged watch, and zero read-needs-claims. All-artifact
counters are 5,877 indexed and 2,822 pending. Every counter-audit check is
true with no mismatch. The next reading queue is the first three filenames
listed above.

Evidence Event Store V2 verifies at sequence 730,548, head
`a5c89ffc9f3d52627922647768cdafb97162b638f0f9e840ae423e8cac023872`,
with zero corrupt lines. Stream counts are recorded in
`verification_receipt.json`. V2 remains authoritative; V1 remains the
immutable imported source through legacy sequence 32,278.

Division cycle 21 verifies at 2/6, four rounds remaining, and
`review_due=false`. Productive-round event
`division_followup_event_6882c1612d4ca980846ccfcb0cfa532b` binds this one
fully processed report to the pre-run source-first projection. No Division
return, note, Chronicle write, or ceremony Action was due. The successful
terminal finish performs the post-run V3 projection; its generation receipt
remains authoritative in the controller run record.

## Archive status

This is the second productive evidence-only round after archive commit
`1d2bab9e751f4fe2eae942cc37cef8078f8a8103`; no archival checkpoint is due.
All current round paths remain unstaged, and no foreign shared-tree work is
claimed.
