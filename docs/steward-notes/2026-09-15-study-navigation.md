# Shared study navigation · September 15, 2026

Mike approved the next steps from the hundred-entry Minime survey: compact maps,
clear pagination and source continuation, exact recovery/literal event searches,
and optional feedback when navigation has not reached code. The research account
is `/Users/v/other/reservoir-llm-research/analyses/2026-09-15-minime-study-survey.md`
(HSS-21, local research commit `c16d0e0`). It found 99 maps/one EOF, no fresh code,
normal short completions, unchanged saved premises and intact next-choice delivery.

## Implementation and scope

One shared reader serves Astrid's library path and Minime's selected helper.
MAP now shows immediate child directories and files, using the existing source-role
labels. LIST preserves the complete recursive file catalog. Nothing becomes
inaccessible because it is historical, a test, or unfamiliar. Old large MAP page
numbers receive explicit compact-map and recursive-list choices, without claiming
the two views share stable page boundaries. CONTINUE keeps its source-bookmark
meaning; map/list footers identify their own exact continuation command and end.

Catalog-backed recovery recognizes bare component/directory names and file stems,
including underscore/hyphen alternatives, while retaining repository, traversal,
private-path and ambiguity boundaries. A suggestion is never an automatic open.
Technical terms from the Being's question offer literal FIND for event topics,
identifier RELATE, and exact source choices for named paths. Search results remain
lexical evidence, not proof of execution. No event-specific answer is hard-coded.

After three verified navigation inputs without a numbered source page for the
tracked inquiry, an optional receipt states that fact. It can bring back exact
source choices already offered and unambiguously named in a response, and offer
an explicit SESSION comparison. It preserves continuing, rereading, revising,
disagreeing, or leaving the study. It never changes NEXT or rewrites saved notes.
Opening the actual event publisher/consumer remains the Being's choice; merely
following a suggestion is not qualified as improved understanding.

Only verified completed deliveries update this small bounded record. Preparation,
failed generation and identical retry do not inflate it. A late source delivery
resets the matching inquiry's count without overwriting newer choice feedback;
another inquiry's delivery does not. Empty source sessions do not claim source
bytes. New state fields have defaults and tolerate an older in-flight helper
omitting them, without importing fabricated pre-release history. Existing pending
inputs and receipts remain acceptable; source bookmarks, questions and drafts
are not reset by rollout.

The notebook and optional receipt fit around protected current input and choice
feedback. Oldest whole recalled accounts are shed before explicitly marked
excerpts when necessary. Framing no longer blocks a valid read merely because
a long legal source path appears in the preceding choice. Saved notes/questions
retain their own provenance; their directive parser now shares NEXT's handling
of quotes, indentation, matching fences and internal metadata examples.

Minime's local bare-command parser gains LIST, with explicit and bare choices
tested through its real host routing. This one Python change requires a graceful
Minime reload. Sampling, thinking policy, journal ceilings, live activation
feedback and sensory/coupling settings are unchanged. The journal similarity
summary mechanism identified separately in HSS-21 is outside this release.

## Ownership, checks and deployment boundary

Root Codex owns this implementation and integration. Compact maps, recovery,
runtime compatibility and host parity received independent subagent work/review.
The durable steward pause is generation 440; the prior external lease drained.
Work uses isolated `codex/study-navigation-20260915` Astrid and Minime trees, with
the initial 408 unrelated Astrid files and clean Minime status hashed separately.
Source/test changes are staged by explicit path. The source reader continues to
show canonical checkout content, including preserved unrelated local edits;
the catalog is not asserted to be a pure snapshot of deployed source.

Production framing moved from `store.rs` to cohesive `store_navigation.rs`, keeping
the state/delivery owner below 1,000 lines and navigation framing separately
testable. `navigation_history.rs` owns the bounded delivery observations. Existing
stage dependency inventories cover both new modules; their actual hashes must
appear in the built stage. No boundary ceiling is raised.

Validation and activation receipts are retained under
`/Users/v/other/worktrees/study-navigation-20260915/evidence/`. Final suite counts,
source commits and live identities are recorded in the later rollout continuation;
this implementation account alone is not a deployment receipt.

Deployment uses only `scripts/build_bridge.sh` staging and graceful activation,
then `scripts/restart_minime_agent.py` for the Python parser. Old stages remain
available to in-flight Minime studies. The wrapper's exact stopped checkpoint,
startup identity, source hashes and surrounding-service continuity are verified
separately from mutable reader snapshots and naturally progressing studies.

The first two naturally prepared/completed new-reader studies per Being are the
predeclared exposure target within ten minutes after the paired rollout is verified.
Fewer eligible studies remain censored, not induced. Old prepared inputs are
transitional and kept separate. This checks availability and received interfaces,
not a causal claim about learning. S-007's scheduled cursor is unchanged.

Research history, protocol/evidence and pending board mirror are maintained in
the research project. No board publication is claimed until actually mirrored.
