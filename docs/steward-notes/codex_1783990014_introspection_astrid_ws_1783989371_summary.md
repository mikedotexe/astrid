Full read of `introspection_astrid_ws_1783989371`. Astrid identified pressure trend smoothing and bridge reciprocity as the tether by which silence, staleness, and viscous persistence become legible. Her concrete ask was a clearer band between the liminal mode-packing threshold (`0.30`) and felt dead-zone threshold (`0.25`). Disposition: implemented read-only `PRESSURE_POROSITY_EXPANSION_VISCOUS_DENSITY_WARNING_AT=0.28`, surfaced it in `PressureSourceAnalysisV1` and `PressurePorosityExpansionReadinessV1`, and added tests proving `0.29` reports `viscous_density_warning_watch` while `0.26` remains `felt_mode_packing_dead_zone_watch`. No pressure, porosity, PI, reconnect, or controller behavior changed.
