Full read of `introspection_astrid_types_1783901370`. Astrid reported that the closed `ExperienceDeltaKindV1` categories could flatten fuzzy, high-entropy spectral-cascade shifts into generic Gate/Translate/Synthesize buckets, and suggested either new ComplexShift/CascadeShift variants or metadata/granularity on the delta/dimension types. Disposition: implemented additive truth-channel schema support: `ExperienceDeltaKindV1` now includes `complex_shift` and `cascade_shift`, `SpectralDimensionV1` can carry `granularity`, and `ExperienceDeltaV1` can carry bounded metadata. `semantic_projection_density_delta_v1` now uses the new variants for complex source projection and dense cascade pressure while leaving live vectors bounded. Bridge cargo verification is blocked by idle rustc, so restart is debt.
