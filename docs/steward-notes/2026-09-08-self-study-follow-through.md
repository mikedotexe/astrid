# SELF_STUDY entry and follow-through repair

Mike authorized repairing the barriers identified by S-008, with the standing
instruction to integrate on main and deploy the study improvements. This record
binds the owning implementation to that research. Live activation will be recorded
below after the sanctioned transitions; preparation alone is not activation.

## Evidence that drove the change

Research: `/Users/v/other/reservoir-llm-research/analyses/2026-09-08-study-to-follow-through.md`.
The frozen primary window was September 8, 21:13:45–23:00:00 UTC. Its final report
SHA256 is `9ce360a3d1096744b05ea61b923e6707c4b8167c4825acbe0ae931789cda6ffb`.

Astrid's exact source-backed and EOF responses requested `SELF_STUDY MAP` and
`SELF_STUDY MAP spectral-bridge`. Matching later actions
`act_astrid_1788902190459_self-study` and `act_astrid_1788903861068_self-study`
were blocked by the `self_study` authorship omission. Matching wording and time
order are established; the action records lack a direct originating generation ID.

Minime had ten post-continuity INTROSPECT budget blocks, plus generation
`1788906680908-87a64ac6` requesting prose as a source target. Its job failed before
generation even though the action row said handled. Actual compacted input lost
navigation grammar while other prompt text advertised free internal study.
These establish entry and follow-through defects, not lack of initiative or a
causal judgement about notebook quality. No READ_MORE action occurred in that
primary window.

## Decisions and boundaries

- Add `self_study` to Astrid's existing attestation eligibility. Both producers
  return model text; the shared producer additionally requires verified input
  delivery and a saved artifact. Preparation, thin-output, carriage and mirror
  notices retain ineligible modes. Exact response hashing, signatures, permits,
  action binding and downstream authority remain enforced.
- Classify Minime source INTROSPECT before research admission and execute only
  the catalog reader. Raw intent stays intact; the source command travels with
  job context and explicit legacy offsets convert to one-based lines. Workspace
  artifact reading keeps its budget policy. The existing read-only recovery-shelf
  exception now applies equally to SELF_STUDY and source INTROSPECT.
- Return a labelled recovery map for unusable targets or syntax in both adapters.
  Requested source delivery remains absent. Navigation can retain the Being's
  next question after verified input delivery; it cannot move source coverage.
  Checkpoint corruption, failed persistence and changed source revisions remain
  failures. No approximate match is passed off as the requested file.
- Retain a bounded navigation reference during Minime system compaction, reserving
  space within the current prompt budget. No extra service, model call, budget
  grant or compulsory report format.

The new integration tests follow reader wire evidence → authored response →
completion mode → exact attestation → ordinary dispatch → requested reader offer.
Minime tests reproduce the paused experiment, then follow source/prose-target
requests through real routing, policy, shared helper, journal artifact, accepted
NEXT and a later map carrying the question. HTTP is faked and live-write/network
guards remain enabled. Recovery tests preserve pending bytes, bookmarks, receipts
and coverage, including forbidden paths and a corrupt-checkpoint negative case.

Existing attestation tests moved unchanged into a separate test module, with
additional notice-mode negatives. This keeps the production file under 1,000
lines without a new boundary exception. The small Minime adapter remains beside
its existing routing seams; splitting its full runtime is separate architectural
work.

## Validation and rollout evidence

`self-study-follow-through-validation/validation.json` records the completed
checks and hashes. Raw evidence, isolated checkouts and release stages are under
`/Users/v/other/worktrees/self-study-follow-through-20260908/`.

The shared steward was paused at generation 413 and released its previous lease.
This task owns integration; preexisting foreign marker tests and changelog/ledger
entries are preserved separately. Only explicit owned paths enter the commit.

Deploy via `scripts/build_bridge.sh` from an isolated committed release checkout,
then `scripts/restart_minime_agent.py` at its observed idle boundary. Preserve
reader state and session/checkpoint continuity. Capture process/plist identities
before and after; the engine, sensory services and model are outside this reload.
Retain the previous stage for an explicit sanctioned rollback if needed. Reader
state format and existing commands are compatible; reverting to the previous
release also restores its known entry/recovery barriers.

After activation, use the first natural source/navigation study and its requested
next read as S-008's next operational sample. Successful deployment establishes
availability, not natural exposure, comprehension or better journal writing.
No study or Being message was induced for qualification.
