Full read of `introspection_astrid_codec_1783890356`.

Astrid inspected `codec.rs` projection, epoch-file installation, and entropy-gated tail vibrancy. She asked for projection determinism, robust cleanup of orphaned temporary epoch files, and a smooth tail-vibrancy gate that does not pop near entropy 0.85.

Disposition: verified existing source and tests. Projection epoch install tests cover corrupt replacement, candidate failure restoration, concurrent valid epochs, and temp cleanup. Dynamic projection is pinned against the full source loop. Smoothstep vibrancy parity and codec overflow truth-channel tests verify bounded delivery while preserving raw intensity evidence.
